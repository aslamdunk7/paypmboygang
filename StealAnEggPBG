-- [[ Rscripts Risk Notice ]]
-- This script is not verified by rscripts.net. Deal with caution.
--
-- Stay safe:
--   • Never log in on unofficial Roblox sites or lookalike domains.
--   • Real Roblox links use roblox.com (check the .com ending).
--   • Treat fake Roblox login / "claim reward" pages as phishing.
-- [[ End Rscripts Risk Notice ]]
-- [[ GC Protection & Hooks ]]
task.spawn(function()
    pcall(function()
        if typeof(getgc) ~= "function" or typeof(hookfunction) ~= "function" then return end
        local getconstants = getconstants or (debug and debug.getconstants)
        local islclosure = islclosure or function(Function)
            return type(Function) == "function" and not pcall(setfenv, Function, getfenv(Function))
        end
        if not getconstants then return end

        for _, Value in next, getgc() do
            if type(Value) == "function" and islclosure(Value) then
                local okConst, Constants = pcall(getconstants, Value)
                if okConst and type(Constants) == "table" then
                    for _, Constant in next, Constants do
                        if Constant == "X-16" then
                            local OldHook
                            OldHook = hookfunction(Value, function(...)
                                local Level = 1
                                local okStack, Stack = pcall(debug.getstack, Level)
                                if okStack and type(Stack) == "table" then
                                    for Index, Val in next, Stack do
                                        if Val == "X-16" then
                                            pcall(debug.setstack, Level, Index, nil)
                                        end
                                    end
                                end
                                return OldHook(...)
                            end)
                            break
                        end
                    end
                end
            end
        end
    end)
end)

----------------------------------------------------------------
-- Steal An Egg • auto farm
-- Engine and UI are shared across builds; only the branding block below moves.
----------------------------------------------------------------

----------------------------------------------------------------
-- Branding
----------------------------------------------------------------
-- Every name a player can read comes from here. A rebranded build edits these
-- six lines and nothing else — no system below ever hardcodes a hub name, so
-- two builds can be diffed and stay identical everywhere that matters.
local HUB_NAME, HUB_TITLE, HUB_AUTHOR, HUB_TAG, HUB_DIR, HUB_KEY =
    "PayomboyZ HUB", "เก็บไข่ไปกับพยอมบอย 🥚", "PayomboyZ HUB • v2", "PayomboyZ", "PayomboyZ_SAE", "PayomboyZ"

local Players, ReplicatedStorage, RunService, Workspace, TeleportService, HttpService, VirtualUser, Lighting, ProximityPromptService =
    game:GetService("Players"), game:GetService("ReplicatedStorage"), game:GetService("RunService"),
    game:GetService("Workspace"), game:GetService("TeleportService"), game:GetService("HttpService"),
    game:GetService("VirtualUser"), game:GetService("Lighting"), game:GetService("ProximityPromptService")

-- Autoexec safety & Place Guard
if game.PlaceId ~= 107778070777162 then
    warn("[" .. HUB_TAG .. "] PlaceId mismatch (" .. tostring(game.PlaceId) .. "), running script anyway...")
end
if not game:IsLoaded() then pcall(function() game.Loaded:Wait() end) end

local LocalPlayer, charWaitStart = Players.LocalPlayer, os.clock()
if not LocalPlayer then
    pcall(function() Players:GetPropertyChangedSignal("LocalPlayer"):Wait() end)
    LocalPlayer = Players.LocalPlayer
end

repeat
    LocalPlayer = Players.LocalPlayer
    task.wait(0.2)
until (LocalPlayer and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")) or (os.clock() - charWaitStart > 8)

local Notify, UIWindow, uiBuilding = function() end, nil, nil

----------------------------------------------------------------
-- Global Session & Cleanup
----------------------------------------------------------------
local ENV = (typeof(getgenv) == "function") and getgenv() or _G
local K_SESSION, K_CLEANUP, K_API = HUB_KEY .. "Session", HUB_KEY .. "Cleanup", HUB_KEY .. "API"

ENV[K_SESSION] = (tonumber(ENV[K_SESSION]) or 0) + 1
local SESSION = ENV[K_SESSION]
if ENV[K_CLEANUP] then pcall(ENV[K_CLEANUP]) end
if ENV.NyxCleanup then pcall(ENV.NyxCleanup) end   -- legacy build, if one is still up

function alive() return ENV[K_SESSION] == SESSION end

----------------------------------------------------------------
-- Game Modules Setup
----------------------------------------------------------------
function safeRequire(inst)
    if not inst then return nil end
    local ok, mod = pcall(require, inst)
    return ok and mod or nil
end

local function findModule(name)
    local direct = ReplicatedStorage:FindFirstChild(name, true)
    if direct and direct:IsA("ModuleScript") then return direct end
    return nil
end

local Shared    = ReplicatedStorage:FindFirstChild("Shared") or findModule("Shared")
local Client    = ReplicatedStorage:FindFirstChild("Client") or findModule("Client")
local Packages  = ReplicatedStorage:FindFirstChild("Packages") or findModule("Packages")
local Data      = ReplicatedStorage:FindFirstChild("Data") or findModule("Data")

local Save      = safeRequire((Shared and Shared:FindFirstChild("Save")) or findModule("Save"))
local Network   = safeRequire((Packages and Packages:FindFirstChild("Networking")) or findModule("Networking") or findModule("Network"))
local EggCmds   = safeRequire(findModule("EggCmds"))
local AssetCmds = safeRequire(findModule("AssetCmds"))
local PlotCmds  = safeRequire(findModule("PlotCmds"))
local EggRecords = safeRequire((Shared and Shared:FindFirstChild("Util") and Shared.Util:FindFirstChild("EggRecords")) or findModule("EggRecords"))
local EggState   = safeRequire((Client and Client:FindFirstChild("EggState")) or findModule("EggState"))

local AssetSer   = safeRequire(findModule("AssetItemSerialization"))
local AssetGen   = safeRequire(findModule("AssetGenerationUtil"))
local PriceOf    = safeRequire(findModule("GetPriceValueForItem"))
local EggUtil    = safeRequire(findModule("EggItemUtil"))
local AreaLookup = safeRequire(findModule("GuardAreaLookupUtil"))

local AssetsDir = safeRequire((Data and Data:FindFirstChild("Assets")) or findModule("Assets"))
local BasesDir  = safeRequire((Data and Data:FindFirstChild("Bases")) or findModule("Bases"))
local TreadDir  = safeRequire((Data and Data:FindFirstChild("Treadmills")) or findModule("Treadmills"))
local TrailsDir = safeRequire((Data and Data:FindFirstChild("Trails")) or findModule("Trails"))
local GuardsDir = safeRequire((Data and Data:FindFirstChild("Guards")) or findModule("Guards"))

if not (Network and Save) then
    warn("[" .. HUB_TAG .. "] โหลดโมดูลเกมไม่สำเร็จ — กำลังใช้ Fallback Resolution เพื่อค้นหาตำแหน่งโมดูล...")
end

local NetworkFolder = (ReplicatedStorage:FindFirstChild("Packages") and ReplicatedStorage.Packages:FindFirstChild("Networking"))
    or ReplicatedStorage:FindFirstChild("Network")
    or (Shared and Shared:FindFirstChild("Remotes"))
    or ReplicatedStorage:FindFirstChild("Networking", true)
    or ReplicatedStorage:FindFirstChild("Remotes", true)
    or ReplicatedStorage:FindFirstChild("Network", true)

local function findRemote(endpoint)
    if not endpoint then return nil end
    local name = tostring(endpoint)
    if NetworkFolder then
        local direct = NetworkFolder:FindFirstChild(name)
        if direct then return direct end
    end
    local found = ReplicatedStorage:FindFirstChild(name, true)
    if found and (found:IsA("RemoteEvent") or found:IsA("RemoteFunction")) then
        return found
    end
    return nil
end

if type(Network) ~= "table" then
    Network = {
        NET_MAP = {
            ActiveAssets = {
                MONEY_COLLECTED_EVENT = "ActiveAssets: MoneyCollected"
            }
        },
        Invoke = function(endpoint, ...)
            local inst = findRemote(endpoint)
            if inst and inst:IsA("RemoteFunction") then
                return inst:InvokeServer(...)
            end
            return false, "remote not found"
        end,
        Fire = function(endpoint, ...)
            local inst = findRemote(endpoint)
            if inst and inst:IsA("RemoteEvent") then
                inst:FireServer(...)
            elseif inst and inst:IsA("RemoteFunction") then
                inst:InvokeServer(...)
            end
        end
    }
end

local NET = (type(Network) == "table" and type(Network.NET_MAP) == "table" and Network.NET_MAP) or {}

function remoteKind(endpoint)
    local inst = findRemote(endpoint)
    if not inst then return nil end
    return inst:IsA("RemoteFunction") and "function" or "event"
end

function invoke(endpoint, ...)
    if not endpoint then return false, "no endpoint" end
    local args = table.pack(...)
    if type(Network) == "table" and type(Network.Invoke) == "function" and Network.Invoke ~= Network.SyntheticInvoke then
        local ok, a, b = pcall(function() return Network.Invoke(endpoint, table.unpack(args, 1, args.n)) end)
        if ok and a ~= false then return a, b end
    end
    local inst = findRemote(endpoint)
    if inst then
        if inst:IsA("RemoteFunction") then
            local ok, res = pcall(inst.InvokeServer, inst, table.unpack(args, 1, args.n))
            return ok and res or false
        elseif inst:IsA("RemoteEvent") then
            pcall(inst.FireServer, inst, table.unpack(args, 1, args.n))
            return true
        end
    end
    return false, "no network endpoint"
end

function fire(endpoint, ...)
    if not endpoint then return end
    local args = table.pack(...)
    if type(Network) == "table" and type(Network.Fire) == "function" and Network.Fire ~= Network.SyntheticFire then
        local ok = pcall(function() Network.Fire(endpoint, table.unpack(args, 1, args.n)) end)
        if ok then return end
    end
    local inst = findRemote(endpoint)
    if inst then
        if inst:IsA("RemoteEvent") then
            pcall(inst.FireServer, inst, table.unpack(args, 1, args.n))
        elseif inst:IsA("RemoteFunction") then
            pcall(inst.InvokeServer, inst, table.unpack(args, 1, args.n))
        end
    end
end

function char()     return LocalPlayer and LocalPlayer.Character end
function hrp()      local c = char() return c and (c:FindFirstChild("HumanoidRootPart") or c:FindFirstChild("UpperTorso")) end
function humanoid() local c = char() return c and c:FindFirstChildOfClass("Humanoid") end

----------------------------------------------------------------
-- Rarity Tables & Helpers
----------------------------------------------------------------
local RARITY_ORDER = {
    "Basic", "Common", "Uncommon", "Rare", "SuperRare", "Superior", "Epic",
    "Legendary", "Mythic", "Mythical", "Celestial", "Cosmic", "Divine",
    "Secret", "Eternal", "Exclusive", "Exotic", "Limited", "Rainbow", "Titan", "BrainrotGod"
}

local RARITY_TH = {
    Basic       = "เบสิก",
    Common      = "ธรรมดา",
    Uncommon    = "ไม่ธรรมดา",
    Rare        = "หายาก",
    SuperRare   = "หายากมาก",
    Superior    = "ชั้นสูง",
    Epic        = "มหากาพย์",
    Legendary   = "ตำนาน",
    Mythic      = "เทพนิยาย",
    Mythical    = "มหากาพย์เทพ",
    Celestial   = "ดาราศาสตร์",
    Cosmic      = "จักรวาล",
    Divine      = "ศักดิ์สิทธิ์",
    Secret      = "ลับ",
    Eternal     = "อมตะ",
    Exclusive   = "พิเศษ",
    Exotic      = "แปลกตา",
    Limited     = "จำกัด",
    Rainbow     = "สายรุ้ง",
    Titan       = "ไททัน",
    BrainrotGod = "พระเจ้าเบรนร็อต",
}

local function scanAreasFromConfigs()
    local list = {}
    local dataAreas = ReplicatedStorage:FindFirstChild("Data") and ReplicatedStorage.Data:FindFirstChild("Areas")
    local configs = dataAreas and dataAreas:FindFirstChild("Configs")
    if configs then
        for _, child in ipairs(configs:GetChildren()) do
            table.insert(list, child.Name)
        end
    end
    if #list > 0 then return list end
    return { "Lake", "Forest", "Desert", "Jungle", "Snow", "Volcano", "Prehistoric", "Abyss Ocean", "Cosmic", "Cherry Blossom", "Titan Temple" }
end

local AREAS = scanAreasFromConfigs()

local AREA_SPEED = {}
if GuardsDir and GuardsDir.Directory then
    for id, cfg in pairs(GuardsDir.Directory) do
        if type(cfg) == "table" and tonumber(cfg.WalkSpeed) then AREA_SPEED[id] = tonumber(cfg.WalkSpeed) end
    end
end

local CategoryRarity, RarityRank = {}, {}
do
    for i, r in ipairs(RARITY_ORDER) do RarityRank[r] = i end
    if AssetsDir and AssetsDir.ByRarity then
        for rarity, list in pairs(AssetsDir.ByRarity) do
            if type(list) == "table" then
                for k, v in pairs(list) do
                    CategoryRarity[(type(v) == "string") and v or tostring(k)] = rarity
                end
            end
        end
    end
    if AssetsDir and AssetsDir.Directory then
        for id, cfg in pairs(AssetsDir.Directory) do
            if type(cfg) == "table" and type(cfg.Rarity) == "table" and cfg.Rarity._id then
                CategoryRarity[id] = cfg.Rarity._id
                if cfg.Rarity.RarityNumber and not RarityRank[cfg.Rarity._id] then
                    RarityRank[cfg.Rarity._id] = cfg.Rarity.RarityNumber
                end
            end
        end
    end
end

function rarityOf(cat) return CategoryRarity[cat] or "Common" end
function rankOf(cat)   return RarityRank[rarityOf(cat)] or 0 end
function earnRate(cat)
    local cfg = AssetsDir and AssetsDir.Directory and AssetsDir.Directory[cat]
    return (cfg and cfg.EarningRate) or 0
end

function raritySet(list)
    local t = {}
    for _, r in ipairs(list or {}) do t[r] = true end
    return t
end

function setToList(set)
    local out = {}
    for _, r in ipairs(RARITY_ORDER) do if set and set[r] then out[#out + 1] = r end end
    return out
end

-- 45763 -> "45.76K"
function shortNum(n)
    n = tonumber(n) or 0
    for _, u in ipairs({ { 1e12, "T" }, { 1e9, "B" }, { 1e6, "M" }, { 1e3, "K" } }) do
        if n >= u[1] then return ("%.2f%s"):format(n / u[1], u[2]) end
    end
    return ("%.0f"):format(n)
end

----------------------------------------------------------------
-- Config & Runtime Architecture
----------------------------------------------------------------
local Cfg = {
    -- farming
    AutoSteal        = false,
    EggESP           = true,
    StealRarities    = raritySet(RARITY_ORDER),
    StealAreas       = (function() local t = {} for _, a in ipairs(AREAS) do t[a] = true end return t end)(),
    PickMode         = "Efficient",
    MinMutations     = 0,

    AutoHatch        = false,
    AutoPlace        = false,
    PlaceRarities    = raritySet(RARITY_ORDER),
    AutoEquipBest    = false,

    -- movement
    MoveMode         = "Instant",
    WarpPct          = 0.8,
    ZoneHopStep      = 260,
    GuardSafeGap     = 26,
    BoostWalkSpeed   = 500,

    -- survival
    GodMode          = true,
    AntiAFK          = true,
    MuteReports      = false,

    -- selling pets
    AutoSellPets     = false,
    SellPetMode      = "ราคาต่ำกว่าที่ตั้ง",
    SellPetBelow     = 0,
    SellPetRarities  = raritySet({ "Common", "Uncommon" }),
    SellKeepTop      = 10,
    SellMaxPerRun    = 60,
    SellLanes        = 3,
    SellPace         = 0.05,

    -- selling eggs
    AutoSellEggs     = false,
    SellEggRarities  = raritySet({ "Common", "Uncommon" }),
    SellEggBelow     = 0,
    SellEggKeepBag   = 20,

    -- progression
    AutoBaseUpgrade  = false,
    AutoTreadmillUp  = false,
    AutoTrails       = false,
    AutoFuse         = false,
    AutoClaim        = false,

    -- system
    ToggleKey        = "RightControl",
    ShowFloatIcon    = true,
    FloatX           = 18,
    FloatY           = 260,
    AntiLag          = false,
    HideOtherPlayers = false,
    LowFXOnly        = false,
    AutoHop          = false,
    HopChecks        = 3,
    HopMaxTries      = 6,
    WebhookURL       = "",
    WebhookSteal     = false,
}

local Runtime = {
    stealing    = false,
    lock        = false,
    carryUid    = nil,
    skip        = {},
    log         = {},
    stats       = { eggs = 0, hatched = 0, soldPets = 0, soldEggs = 0, startedAt = os.clock(), startMoney = 0 },
    instantReady= false,
    lastHopAt   = nil,
    hopChain    = 0,
    hopStopped  = false,
    lastError   = nil,
    conns       = {},
    eggTick     = 0,
}

----------------------------------------------------------------
-- Config Persistence
----------------------------------------------------------------
local CFG_DIR, CFG_FILE, CFG_BACKUP, CFG_LEGACY_NYX, CFG_LEGACY =
    HUB_DIR, HUB_DIR .. "/payomboyz_config.json", HUB_DIR .. "/payomboyz_config.bak.json", "NyxSAE/nyx_config.json", "NyxSAE/config.json"

local hasFS = (typeof(writefile) == "function") and (typeof(readfile) == "function")
if hasFS and typeof(makefolder) == "function" and not (isfolder and isfolder(CFG_DIR)) then
    pcall(makefolder, CFG_DIR)
end

Runtime.cfgStatus = "ยังไม่ได้อ่านไฟล์"

function fileExists(path)
    if not hasFS then return false end
    if typeof(isfile) ~= "function" then
        local ok, body = pcall(readfile, path)
        return ok and type(body) == "string" and #body > 0
    end
    local ok, yes = pcall(isfile, path)
    return ok and yes == true
end

function readJson(path)
    if not fileExists(path) then return nil end
    local okr, body = pcall(readfile, path)
    if not okr or type(body) ~= "string" or #body < 2 then return nil end
    local okd, data = pcall(function() return HttpService:JSONDecode(body) end)
    if not okd or type(data) ~= "table" then return nil end
    return data
end

function saveConfig()
    if uiBuilding then return false end
    if not hasFS then
        Runtime.cfgStatus = "executor นี้เขียนไฟล์ไม่ได้ — ค่าจะหายเมื่อออกเกม"
        return false
    end

    local okEnc, body = pcall(function() return HttpService:JSONEncode(Cfg) end)
    if not okEnc or type(body) ~= "string" then
        Runtime.cfgStatus = "แปลงค่าเป็น JSON ไม่ได้"
        return false
    end

    if fileExists(CFG_FILE) then
        pcall(function()
            local old = readfile(CFG_FILE)
            if type(old) == "string" and #old > 2 then writefile(CFG_BACKUP, old) end
        end)
    end

    local okW = pcall(writefile, CFG_FILE, body)
    if not okW then
        Runtime.cfgStatus = "เขียนไฟล์ไม่สำเร็จ"
        return false
    end

    if readJson(CFG_FILE) == nil then
        Runtime.cfgStatus = "เขียนแล้วอ่านกลับไม่ได้ — ไฟล์อาจถูกทับ"
        return false
    end

    Runtime.cfgSavedAt = os.clock()
    Runtime.cfgStatus  = ("บันทึกแล้ว %s"):format(os.date("%H:%M:%S"))
    return true
end

-- a slider drag fires the callback on every pixel; one disk write per pixel is
-- both pointless and a good way to catch a half-written file
local cfgSavePending = false
function saveConfigSoon()
    if cfgSavePending then return end
    cfgSavePending = true
    task.delay(0.4, function()
        cfgSavePending = false
        saveConfig()
    end)
end

function applyConfigTable(data)
    for k, v in pairs(data) do
        if Cfg[k] ~= nil then Cfg[k] = v end
    end

    local function emptySet(t) return type(t) ~= "table" or next(t) == nil end
    if emptySet(Cfg.StealAreas) then
        Cfg.StealAreas = {}
        for _, a in ipairs(AREAS) do Cfg.StealAreas[a] = true end
    end
    if emptySet(Cfg.StealRarities) then Cfg.StealRarities = raritySet(RARITY_ORDER) end
    if emptySet(Cfg.PlaceRarities) then Cfg.PlaceRarities = raritySet(RARITY_ORDER) end
end

function loadConfig()
    if not hasFS then
        Runtime.cfgStatus = "executor นี้อ่านไฟล์ไม่ได้ — ใช้ค่าเริ่มต้น"
        return false
    end

    local data, from = readJson(CFG_FILE), CFG_FILE
    if not data then data, from = readJson(CFG_BACKUP), CFG_BACKUP end
    if not data then data, from = readJson(CFG_LEGACY_NYX), CFG_LEGACY_NYX end
    if not data then data, from = readJson(CFG_LEGACY), CFG_LEGACY end

    if not data then
        Runtime.cfgStatus = "ยังไม่มีไฟล์ค่าที่บันทึกไว้ — ใช้ค่าเริ่มต้น"
        return false
    end

    applyConfigTable(data)
    local n = 0
    for _ in pairs(data) do n += 1 end
    Runtime.cfgStatus = ("โหลด %d ค่าจาก %s"):format(n, from)
    if from ~= CFG_FILE then saveConfig() end

    -- บังคับปิดสวิตช์การทำงานอัตโนมัติเมื่อเริ่มสคริปต์ เพื่อป้องกันตัวละครทำงานเองหรือเด้งกลับบ้าน
    Cfg.AutoSteal = false
    Cfg.AutoHatch = false
    Cfg.AutoPlace = false
    Cfg.AutoEquipBest = false
    return true
end
loadConfig()

function webhook(title, desc)
    if Cfg.WebhookURL == "" then return end
    local req = (typeof(request) == "function" and request)
        or (typeof(http_request) == "function" and http_request)
    if not req then return end
    task.spawn(function()
        pcall(function()
            req({
                Url = Cfg.WebhookURL,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json" },
                Body = HttpService:JSONEncode({
                    username = HUB_NAME .. " | Steal An Egg",
                    embeds = { { title = title, description = desc, color = 0x8B5CF6,
                        footer = { text = (LocalPlayer and LocalPlayer.Name or "Player") .. " • " .. os.date("%H:%M:%S") } } },
                }),
            })
        end)
    end)
end

function logLine(kind, text)
    table.insert(Runtime.log, 1, ("[%s] %s"):format(os.date("%H:%M:%S"), text))
    for i = #Runtime.log, 41, -1 do Runtime.log[i] = nil end
end

----------------------------------------------------------------
-- NoKnockback — ตัด RE/RigSync/Refresh ออก
-- Fix: แยก WaitForChild เป็น 2 ขั้นเพื่อไม่ให้ chain crash
-- เมื่อ timeout คืน nil → "nil and X" = nil → ไม่ error
----------------------------------------------------------------
do
    local RS = game:GetService("ReplicatedStorage")
    local ok1, pkgs = pcall(function() return RS:WaitForChild("Packages", 4) end)
    if ok1 and pkgs then
        local ok2, networking = pcall(function() return pkgs:WaitForChild("Networking", 4) end)
        if ok2 and networking then
            local rigSync = networking:FindFirstChild("RE/RigSync/Refresh")
            if rigSync and typeof(getconnections) == "function" then
                local s, conns = pcall(getconnections, rigSync.OnClientEvent)
                if s and conns then
                    for _, c in next, conns do pcall(function() c:Disconnect() end) end
                    logLine("init", "NoKnockback: ตัด RigSync/Refresh สำเร็จ")
                end
            end
        end
    end
end

----------------------------------------------------------------
-- SpeedBypass — getgc hook ล้าง metatable anti-speed check
-- เทคนิคเดียวกับค่าย paid hub:
--   1. getgc() → scan closures ทั้งหมดใน VM
--   2. หา anti-speed closure โดย upvalue signature (nups=19, line=3)
--   3. hookfunction → ใส่ setmetatable({}) บน table ที่ anti-cheat ส่งมา
--      → ทำให้ __index / __newindex check ล้มเหลว → ไม่ kick
-- ถ้า executor ไม่รองรับ getgc → fallback Heartbeat enforce
----------------------------------------------------------------
do
    local bypassOk = false

    if typeof(getgc) == "function" and typeof(hookfunction) == "function"
        and typeof(islclosure) == "function" then

        local function findAntiSpeedFunc(nups, linedef)
            local ok, gc = pcall(getgc)
            if not ok or not gc then return nil end
            for _, f in next, gc do
                if typeof(f) == "function" and islclosure(f) then
                    local upvs = debug.getupvalues(f)
                    local line = debug.info(f, "l")
                    if upvs and #upvs == nups and line == linedef then
                        -- extra check: upvalue 3 เป็น table ที่มี Humanoid key
                        if nups == 19 then
                            local t = debug.getupvalue(f, 3)
                            if typeof(t) == "table" and rawget(t, "Humanoid") then
                                return f
                            end
                        else
                            return f
                        end
                    end
                end
            end
            return nil
        end

        local target = findAntiSpeedFunc(19, 3)
        if target then
            local inner = debug.getupvalue(target, 2)
            if inner and typeof(inner) == "function" then
                local original; original = hookfunction(inner, newlclosure(function(p1, p2)
                    -- ล้าง metatable ของ velocity/state table → speed check ผ่าน
                    if p2 and typeof(p2) == "table" then
                        pcall(setmetatable, p2, {})
                    end
                    return original(p1, p2)
                end))
                bypassOk = true
                logLine("init", "SpeedBP: hookfunction anti-speed สำเร็จ (gc scan)")
            end
        end
    end

    if not bypassOk then
        -- fallback: Heartbeat enforce เหมือน SpeedBP แต่ไม่ hook
        -- ใช้กับ executor ที่ไม่มี getgc
        RunService.Heartbeat:Connect(function()
            local hum = humanoid()
            if hum and hum.WalkSpeed < (Cfg.BoostWalkSpeed or 16) then
                pcall(function() hum.WalkSpeed = Cfg.BoostWalkSpeed or 16 end)
            end
        end)
        logLine("init", "SpeedBP: fallback Heartbeat enforce (getgc unavailable)")
    end
end

----------------------------------------------------------------
-- InstantInteract — ยกเลิก HoldDuration บน CarryAreaEgg prompt
-- ทำให้ไม่ต้อง hold นิ้วค้างเพื่อหยิบไข่
----------------------------------------------------------------
do
    local PPS = game:GetService("ProximityPromptService")
    PPS.PromptButtonHoldBegan:Connect(function(prompt, player)
        if player == LocalPlayer and tostring(prompt.Name) == "CarryAreaEgg" then
            pcall(function() prompt.HoldDuration = 0 end)
        end
    end)
    -- ยังจับ PromptTriggered ไว้ด้วยเผื่อ HoldDuration ไม่ทัน set
    PPS.PromptShown:Connect(function(prompt, inputType)
        if tostring(prompt.Name) == "CarryAreaEgg" then
            pcall(function() prompt.HoldDuration = 0 end)
        end
    end)
    logLine("init", "InstantInteract: CarryAreaEgg HoldDuration=0 ready")
end

----------------------------------------------------------------
-- Passive Income Meter
----------------------------------------------------------------
-- Measured 2026-08-17: `ActiveAssets: MoneyCollected` fires once per payout as
-- `({ {uid = <pet uid>, amount = <number>}, ... }, isLocal)`. Listening is the
-- only way to know real earnings without calling Save.Get on a timer, which is
-- exactly the polling an all-night AFK session must not do.
Runtime.income = { total = 0, since = os.clock(), last = 0, lastAt = 0 }

do
    local eventName = (type(NET.ActiveAssets) == "table" and NET.ActiveAssets.MONEY_COLLECTED_EVENT) or "ActiveAssets: MoneyCollected"
    local ev = NetworkFolder and NetworkFolder:FindFirstChild(tostring(eventName))
    if ev and ev:IsA("RemoteEvent") then
        Runtime.conns.money = ev.OnClientEvent:Connect(function(list, isLocal)
            if isLocal ~= true or type(list) ~= "table" then return end
            local sum = 0
            for _, row in pairs(list) do
                if type(row) == "table" then sum += tonumber(row.amount) or 0 end
            end
            if sum <= 0 then return end
            local inc = Runtime.income
            inc.total += sum
            inc.last   = sum
            inc.lastAt = os.clock()
        end)
    end
end

function incomePerSec()
    local inc = Runtime.income
    local dt  = os.clock() - inc.since
    if dt < 2 then return 0 end
    return inc.total / dt
end

----------------------------------------------------------------
-- Save Data & Inventory Caches
----------------------------------------------------------------
local saveCache, saveCacheAt = nil, 0

function saveData(fresh)
    local now = os.clock()
    if not fresh and saveCache and now - saveCacheAt < 0.4 then return saveCache end
    local ok, data = pcall(Save.Get, LocalPlayer)
    if ok and type(data) == "table" then
        saveCache, saveCacheAt = data, now
        return data
    end
    return saveCache or {}
end

function moneyNow() return tonumber(saveData().Money) or 0 end

local petCache, petCacheAt = nil, 0

-- `yieldEvery` spreads the rebuild across frames, so unpacking two hundred pets
-- never lands inside a single frame
function petItems(fresh, yieldEvery)
    local now = os.clock()
    if not fresh and petCache and now - petCacheAt < 2 then return petCache end

    local data = saveData()
    local inv  = data.Inventory or {}
    local equipped = {}
    for _, uid in ipairs(data.EquippedAssets or {}) do equipped[uid] = true end

    local list = {}
    if not AssetSer then return list end
    local seen = 0
    for uid, raw in pairs(inv) do
        seen += 1
        if yieldEvery and seen % yieldEvery == 0 then task.wait() end
        local ok, item = pcall(AssetSer.Deserialize, raw)
        if ok and type(item) == "table" and item.Category then
            local rate = 0
            if AssetGen then
                local okr, r = pcall(AssetGen.GetRateWithoutBoosts, item)
                if okr then rate = tonumber(r) or 0 end
            end
            local price = rate * 100
            if PriceOf then
                local okp, p = pcall(PriceOf, item)
                if okp and tonumber(p) then price = tonumber(p) end
            end
            list[#list + 1] = {
                uid = uid, cat = item.Category, rarity = rarityOf(item.Category),
                rate = rate, price = price,
                favorite = item.IsFavorite == true,
                inFuse   = item.InFuse == true,
                equipped = equipped[uid] == true,
            }
        end
    end
    petCache, petCacheAt = list, now
    Runtime.petCount = #list
    return list
end

function eggInventory() return saveData().EggInventory or {} end

function myRuntimeRecords()
    local ok, recs = pcall(EggCmds.GetOwnerRuntimeRecords, LocalPlayer.UserId)
    return (ok and type(recs) == "table") and recs or {}
end

function plotData()
    local ok, pd = pcall(PlotCmds.GetPlotData)
    return ok and pd or nil
end

----------------------------------------------------------------
-- Client Integrity & Detachment Engine
----------------------------------------------------------------
local Guardless = { Reports = {
    "ClientCharacter: CorrectionStarted",
    "ClientCharacter: IntegrityViolation",
} }

function Guardless.detach()
    local net = ReplicatedStorage:FindFirstChild("Network")
    if not net then return false end

    ENV.NyxDetached = ENV.NyxDetached or {}
    local held = ENV.NyxDetached
    local ok = true

    for _, name in ipairs(Guardless.Reports) do
        local live = net:FindFirstChild(name)
        if live then
            held[name] = { remote = live, parent = net }
            local okd = pcall(function() live.Parent = nil end)
            if not okd then ok = false end
        elseif not (held[name] or net:FindFirstChild("ClientCharacter: IntegrityHeartbeat")) then
            ok = false
        end
    end

    ENV.NyxCorrectionMuted = ok
    ENV.NyxMuteJobId = ok and game.JobId or nil
    return ok
end

function Guardless.reattach()
    local held = ENV.NyxDetached
    if not held then return end
    for _, entry in pairs(held) do
        pcall(function()
            if entry.remote and not entry.remote.Parent and entry.parent then
                entry.remote.Parent = entry.parent
            end
        end)
    end
    ENV.NyxDetached = nil
    ENV.NyxCorrectionMuted = false
    ENV.NyxMuteJobId = nil
end

local function killClientIntegrity()
    if Cfg.MuteReports then return Guardless.detach() end
    Guardless.reattach()
    return true
end

----------------------------------------------------------------
-- Movement & Pinning Primitives
----------------------------------------------------------------
local ANCHOR  = Vector3.new(536.4492797851562, 70.28306579589844, -365.0294189453125)
local GATE_IN = Vector3.new(578, 70.3, -365.03)
local MIN_HOVER = 3

local SEP_LINE = (function()
    local objs = Workspace:FindFirstChild("__OBJECTS")
    local areas = objs and objs:FindFirstChild("Areas")
    return areas and areas:FindFirstChild("SeparationLine")
end)()

local GuardAreasFolder = (function()
    local objs = Workspace:FindFirstChild("__OBJECTS")
    local areas = objs and objs:FindFirstChild("Areas")
    return areas and areas:FindFirstChild("GuardAreas")
end)()

local function instantOK()
    -- Instant uses this file's own snap + per-frame pin path without requiring extra bypasses.
    return Cfg.MoveMode == "Instant"
end

local function blinkExact(pos)
    local root = hrp()
    if not root then return false end
    root.CFrame = CFrame.new(pos)
    root.AssemblyLinearVelocity = Vector3.zero
    return true
end

-- the corrector restores once per sample, this restores once per frame on every
-- step signal, so the position that replicates is ours
local function pin(pos)
    local stopped = false
    local conns = {}

    local function apply()
        if stopped then return end
        local root = hrp()
        if root then
            root.CFrame = CFrame.new(pos)
            root.AssemblyLinearVelocity = Vector3.zero
        end
    end

    conns[#conns + 1] = RunService.Heartbeat:Connect(apply)
    pcall(function() conns[#conns + 1] = RunService.PreSimulation:Connect(apply) end)
    pcall(function() conns[#conns + 1] = RunService.PostSimulation:Connect(apply) end)
    if RunService:IsClient() then
        pcall(function() conns[#conns + 1] = RunService.RenderStepped:Connect(apply) end)
    end

    apply()
    Runtime.pinConns = conns
    return function()
        if stopped then return end
        stopped = true
        for _, c in ipairs(conns) do pcall(function() c:Disconnect() end) end
        if Runtime.pinConns == conns then Runtime.pinConns = nil end
    end
end

local function waitPinned(pos, tol, timeout)
    tol = tol or 8
    local deadline = os.clock() + (timeout or 0.35)
    repeat
        RunService.Heartbeat:Wait()
        local root = hrp()
        if root and (root.Position - pos).Magnitude <= tol then return true end
    until os.clock() > deadline
    return false
end

local function blinkHold(pos, seconds)
    if not hrp() then return false end
    local unpin = pin(pos)
    local landed = waitPinned(pos, 6, math.max(seconds or 0.12, 0.3))
    for _ = 1, 3 do RunService.Heartbeat:Wait() end
    unpin()
    return landed
end

local function insideGameplay()
    local root = hrp()
    if not (root and SEP_LINE and AreaLookup and AreaLookup.IsInGameplaySide) then return false end
    local ok, inside = pcall(AreaLookup.IsInGameplaySide, SEP_LINE, root.Position)
    return ok and inside == true
end

function enforceWalkSpeed()
    local want = Cfg.BoostWalkSpeed
    if not want or want <= 0 then return end
    local hum = humanoid()
    if hum and hum.WalkSpeed < want then hum.WalkSpeed = want end
end

function guardPositions()
    local list = {}
    if not GuardAreasFolder then return list end
    for _, model in ipairs(GuardAreasFolder:GetChildren()) do
        local guard = model:FindFirstChild("Guard")
        if guard then
            local ok, gp = pcall(function() return guard:GetPivot().Position end)
            if ok and gp then list[#list + 1] = Vector3.new(gp.X, 0, gp.Z) end
        end
    end
    return list
end

function nearestGuardDist(pos, list)
    local flat, best = Vector3.new(pos.X, 0, pos.Z), math.huge
    for _, gp in ipairs(list or guardPositions()) do
        local d = (gp - flat).Magnitude
        if d < best then best = d end
    end
    return best
end

----------------------------------------------------------------
-- Warp Fallback & Steer Engine
----------------------------------------------------------------
local groundParams = RaycastParams.new()
groundParams.FilterType = Enum.RaycastFilterType.Exclude
groundParams.IgnoreWater = true

function groundYAt(pos)
    local ignore = { char() }
    local origin = pos + Vector3.new(0, 4, 0)
    for _ = 1, 10 do
        groundParams.FilterDescendantsInstances = ignore
        local hit = Workspace:Raycast(origin, Vector3.new(0, -600, 0), groundParams)
        if not hit then return nil end
        if hit.Instance.CanCollide and hit.Instance.Transparency < 1 then return hit.Position.Y end
        table.insert(ignore, hit.Instance)
    end
    return nil
end

function settleFeet(seconds)
    local t0 = os.clock()
    while os.clock() - t0 < (seconds or 0.08) do
        task.wait()
        local r = hrp()
        if r then r.AssemblyLinearVelocity = Vector3.zero end
    end
end

function steerAroundGuards(pos, gap, list)
    local out = pos
    for _ = 1, 3 do
        local worst, worstGap
        for _, gp in ipairs(list) do
            local d = (gp - Vector3.new(out.X, 0, out.Z)).Magnitude
            if d < gap and (not worstGap or d < worstGap) then worst, worstGap = gp, d end
        end
        if not worst then return out, true end
        local away = Vector3.new(out.X, 0, out.Z) - worst
        if away.Magnitude < 0.1 then away = Vector3.new(0, 0, 1) end
        away = away.Unit * (gap - worstGap + 6)
        out = out + Vector3.new(away.X, 0, away.Z)
    end
    return out, false
end

local function warpTo(dest, arriveDist, opts)
    opts = opts or {}
    arriveDist = arriveDist or 2.5
    local gap      = opts.guardGap or Cfg.GuardSafeGap or 26
    local deadline = os.clock() + (opts.timeout or 14)

    while os.clock() < deadline do
        if not alive() then return false, "เซสชันถูกแทนที่" end
        enforceWalkSpeed()
        local dt = RunService.Heartbeat:Wait()
        local root = hrp()
        if not root then return false, "ไม่มีตัวละคร" end

        local delta = dest - root.Position
        local flat  = Vector3.new(delta.X, 0, delta.Z)
        if flat.Magnitude <= arriveDist then return true end

        local hum   = humanoid()
        local speed = math.max(300, (hum and hum.WalkSpeed or 16) * (Cfg.WarpPct or 2.0))
        local step  = math.min(speed * dt, flat.Magnitude, 14)
        local nextPos = root.Position + flat.Unit * step

        if not opts.ignoreGuards then
            local guards = guardPositions()
            local effGap = flat.Magnitude < 40 and math.min(gap, 12) or gap
            local steered, clear = steerAroundGuards(nextPos, effGap, guards)
            if not clear and nearestGuardDist(root.Position, guards) < 8 then return false, "ยามขวางทาง" end
            nextPos = steered
        end

        local gy = groundYAt(nextPos)
        if gy then nextPos = Vector3.new(nextPos.X, gy + MIN_HOVER, nextPos.Z) end

        root.CFrame = CFrame.new(nextPos)
        root.AssemblyLinearVelocity = Vector3.zero
    end
    return false, "วาปไม่ทันเวลา"
end

local function warpChain(dest, arriveDist, opts)
    opts = opts or {}
    local root = hrp()
    if not root then return false, "ไม่มีตัวละคร" end

    local hop  = math.max(300, Cfg.ZoneHopStep or 500)
    local from = root.Position
    local flat = Vector3.new(dest.X - from.X, 0, dest.Z - from.Z)
    local legs = math.max(1, math.ceil(flat.Magnitude / hop))

    for i = 1, legs - 1 do
        local wp = from + flat * (i / legs)
        local ok, err = warpTo(Vector3.new(wp.X, from.Y, wp.Z), 4, opts)
        if not ok then return false, err end
    end
    return warpTo(dest, arriveDist or 2.5, opts)
end

-- every trip that ends in a request to the server is walked at step speed:
-- feet on the ground, velocity zeroed, ~300 studs a second
Guardless.StepSpeed = 300
Guardless.rayParams = RaycastParams.new()
Guardless.rayParams.FilterType = Enum.RaycastFilterType.Exclude

local function stepGroundY(pos)
    Guardless.rayParams.FilterDescendantsInstances = { char() }
    local hit = Workspace:Raycast(pos + Vector3.new(0, 60, 0), Vector3.new(0, -400, 0), Guardless.rayParams)
    return hit and hit.Position.Y or nil
end

local Nav = { GuardRing = 26 }

function Nav.guardPivots()
    local list = {}
    if not GuardAreasFolder then return list end
    for _, model in ipairs(GuardAreasFolder:GetChildren()) do
        local guard = model:FindFirstChild("Guard")
        if guard then
            local ok, pivot = pcall(function() return guard:GetPivot().Position end)
            if ok then list[#list + 1] = Vector3.new(pivot.X, 0, pivot.Z) end
        end
    end
    return list
end

function Nav.dodge(pos, guards)
    local flat = Vector3.new(pos.X, 0, pos.Z)
    for _, g in ipairs(guards) do
        local away = flat - g
        local d    = away.Magnitude
        if d < Nav.GuardRing and d > 0.1 then
            local pushed = g + away.Unit * Nav.GuardRing
            return Vector3.new(pushed.X, pos.Y, pushed.Z)
        end
    end
    return pos
end

-- Measured 2026-08-17 on this build: a stepped trip covers ~85 studs a second,
-- not the 800 the step size asks for, because the corrector drags every frame
-- back. The fixed 8s/15s budgets were written for the fast case, so a nest 700
-- studs out reported "ไปไม่ถึงไข่" while the character was still walking. The
-- caller's number is now a floor, and the real budget comes from the distance.
local function stepTo(target, timeout)
    local start = hrp()
    local budget = timeout or 12
    if start then
        local flat0 = Vector3.new(target.X - start.Position.X, 0, target.Z - start.Position.Z).Magnitude
        budget = math.max(budget, flat0 / 60 + 4)
    end
    local deadline = os.clock() + budget
    local guards   = Nav.guardPivots()
    local refresh  = os.clock() + 0.5
    while os.clock() < deadline do
        local dt   = RunService.Heartbeat:Wait()
        local root = hrp()
        if not root then return false end
        if os.clock() > refresh then guards = Nav.guardPivots() refresh = os.clock() + 0.5 end
        local pos  = root.Position
        local flat = Vector3.new(target.X - pos.X, 0, target.Z - pos.Z)
        local dist = flat.Magnitude
        if dist < 6 then return true end
        local nxt = pos + flat.Unit * math.min(Guardless.StepSpeed * dt, dist)
        if dist > 35 then nxt = Nav.dodge(nxt, guards) end
        local gy  = stepGroundY(nxt)
        if gy then nxt = Vector3.new(nxt.X, gy + MIN_HOVER, nxt.Z) end
        root.CFrame = CFrame.new(nxt)
        root.AssemblyLinearVelocity = Vector3.zero
    end
    return false
end

function Nav.curtainClosed()
    local objs  = Workspace:FindFirstChild("__OBJECTS")
    local areas = objs and objs:FindFirstChild("Areas")
    local wall  = areas and areas:FindFirstChild("WallStartCollision")
    return wall ~= nil and wall.CanCollide == true
end

function Nav.waitCurtain(timeout)
    if not Nav.curtainClosed() then return true end
    local deadline = os.clock() + (timeout or 90)
    while os.clock() < deadline do
        task.wait(0.25)
        if not Nav.curtainClosed() then
            task.wait(0.15)
            return true
        end
    end
    return false
end

function Nav.treadmillPlates()
    if Nav.plates and os.clock() < (Nav.platesAt or 0) then return Nav.plates end
    local list = {}
    local plots = Workspace:FindFirstChild("Plots")
    if plots then
        for _, plot in ipairs(plots:GetChildren()) do
            local plate = plot:FindFirstChild("TreadmillBottom")
            if plate and plate:IsA("BasePart") then list[#list + 1] = plate end
        end
    end
    Nav.plates, Nav.platesAt = list, os.clock() + 10
    return list
end

function Nav.onTreadmill()
    local root = hrp()
    if not root then return false end
    for _, plate in ipairs(Nav.treadmillPlates()) do
        if plate.Parent then
            local local_ = plate.CFrame:PointToObjectSpace(root.Position)
            local half   = plate.Size * 0.5
            if math.abs(local_.X) <= half.X + 3
                and math.abs(local_.Z) <= half.Z + 3
                and local_.Y > -4 and local_.Y < 14
            then
                return true, plate
            end
        end
    end
    return false
end

-- never jumps: an airborne frame is not counted by the server and costs the trip
function Nav.leaveTreadmill()
    local on, plate = Nav.onTreadmill()
    if not on then return true end
    local h = humanoid()
    if h then pcall(function() h.Sit = false end) end

    for i = 1, 6 do
        local root = hrp()
        if not root then return false end
        local dir  = plate and plate.CFrame.LookVector or Vector3.new(0, 0, -1)
        dir = Vector3.new(dir.X, 0, dir.Z)
        if dir.Magnitude < 0.1 then dir = Vector3.new(0, 0, -1) else dir = dir.Unit end
        local away = root.Position + dir * (14 * i)
        local gy   = stepGroundY(away)
        root.CFrame = CFrame.new(gy and Vector3.new(away.X, gy + MIN_HOVER, away.Z) or away)
        root.AssemblyLinearVelocity = Vector3.zero
        RunService.Heartbeat:Wait()
        if not Nav.onTreadmill() then return true end
    end
    stepTo(ANCHOR, 6)
    return not Nav.onTreadmill()
end

local function toAnchor()
    if instantOK() then
        if insideGameplay() then stepTo(GATE_IN, 8) end
        return stepTo(ANCHOR, 8)
    end
    local root = hrp()
    if not root then return false end
    if (Vector3.new(root.Position.X, 0, root.Position.Z) - Vector3.new(ANCHOR.X, 0, ANCHOR.Z)).Magnitude < 4 then
        return blinkExact(ANCHOR)
    end
    if insideGameplay() then warpChain(GATE_IN, 4, { guardGap = 14 }) end
    warpChain(ANCHOR, 3, { ignoreGuards = true })
    return blinkExact(ANCHOR)
end

local function moveTo(pos)
    if instantOK() then
        local gy = groundYAt(pos)
        return stepTo(gy and Vector3.new(pos.X, gy + MIN_HOVER, pos.Z) or (pos + Vector3.new(0, 3, 0)), 12)
    end
    return warpChain(pos, 5, { ignoreGuards = true })
end

----------------------------------------------------------------
-- God Mode & Character Harden Engine
----------------------------------------------------------------
local function hardenCharacter(c)
    c = c or char()
    local hum = c and c:FindFirstChildOfClass("Humanoid")
    if not hum then return end
    pcall(function()
        hum.BreakJointsOnDeath = false
    end)
end

----------------------------------------------------------------
-- Egg Snapshot & Base Helpers
----------------------------------------------------------------
-- SAE_Nyx reads the pushed snapshot; the Uid is backfilled from the table key so
-- both shapes the game has shipped (snap.Records and a flat map) answer the same
-- Helper for flexible area ID matching (handles numbers, strings, names, and attributes)
local function checkAreaAllowed(areaId)
    if areaId == nil then return true end
    if not Cfg.StealAreas then return true end

    -- Direct key match (e.g. Cfg.StealAreas["Lake"] or Cfg.StealAreas["Area 1"])
    if Cfg.StealAreas[areaId] ~= nil then return Cfg.StealAreas[areaId] == true end

    -- Index match if areaId is numeric (1 -> AREAS[1])
    if type(areaId) == "number" and AREAS[areaId] then
        local name = AREAS[areaId]
        if Cfg.StealAreas[name] ~= nil then return Cfg.StealAreas[name] == true end
    end

    -- Match string with area number pattern (e.g. "Area1", "Area_1", "1")
    local str = tostring(areaId)
    if Cfg.StealAreas[str] ~= nil then return Cfg.StealAreas[str] == true end

    local num = tonumber(str:match("%d+"))
    if num and AREAS[num] and Cfg.StealAreas[AREAS[num]] ~= nil then
        return Cfg.StealAreas[AREAS[num]] == true
    end

    -- Fuzzy name match against AREAS table
    for idx, areaName in ipairs(AREAS) do
        if str:find(areaName, 1, true) or areaName:find(str, 1, true) then
            if Cfg.StealAreas[areaName] ~= nil then return Cfg.StealAreas[areaName] == true end
        end
    end

    return true
end

local function detectAreaFromInstance(inst)
    if not inst then return nil end
    local a = inst:GetAttribute("AreaId") or inst:GetAttribute("Area") or inst:GetAttribute("AreaName")
    if a then return a end

    local curr = inst
    for _ = 1, 6 do
        if not curr or curr == Workspace then break end
        if curr:GetAttribute("AreaId") then return curr:GetAttribute("AreaId") end
        if curr:GetAttribute("Area") then return curr:GetAttribute("Area") end

        local cName = curr.Name
        for idx, areaName in ipairs(AREAS) do
            if cName:find(areaName, 1, true) then return areaName end
        end
        local num = cName:match("Area%s*(%d+)") or cName:match("Zone%s*(%d+)")
        if num then return tonumber(num) end

        curr = curr.Parent
    end
    return nil
end

local function areaEggRecords()
    local list = {}
    local seenPositions = {}

    -- 1. Parse module snapshot if available
    local snap
    local ok = false
    local modulesToTry = { EggRecords, EggState, EggCmds }

    for _, mod in ipairs(modulesToTry) do
        if mod and type(mod) == "table" then
            for _, fnName in ipairs({ "GetAreaEggSnapshot", "GetAreaEggRecords", "GetAreaEggRecordsList", "GetSnapshot", "GetRecords", "GetState" }) do
                if type(mod[fnName]) == "function" then
                    ok, snap = pcall(mod[fnName])
                    if ok and type(snap) == "table" then break end
                end
            end
        end
        if ok and type(snap) == "table" then break end
    end

    if ok and type(snap) == "table" then
        local src = (type(snap.Records) == "table") and snap.Records or snap
        for uid, rec in pairs(src) do
            if type(rec) == "table" then
                if rec.Uid == nil and type(uid) == "string" then rec.Uid = uid end
                rec.BottomCFrame = rec.BottomCFrame or rec.CFrame or (rec.Position and CFrame.new(rec.Position))
                if rec.BottomCFrame then
                    local pos = rec.BottomCFrame.Position
                    local key = ("%.1f_%.1f_%.1f"):format(pos.X, pos.Y, pos.Z)
                    if not seenPositions[key] then
                        seenPositions[key] = true
                        list[#list + 1] = rec
                    end
                end
            end
        end
    end

    -- 2. Always scan ProximityPrompts across Workspace to capture all physical eggs
    for _, prompt in ipairs(Workspace:GetDescendants()) do
        if prompt:IsA("ProximityPrompt") then
            local pName = prompt.Name
            if pName == "CarryAreaEgg" or string.find(pName, "Egg", 1, true) or string.find(pName, "Steal", 1, true) or string.find(pName, "Carry", 1, true) then
                local parent = prompt.Parent
                local promptPos = nil
                if parent then
                    if parent:IsA("Attachment") then promptPos = parent.WorldPosition
                    elseif parent:IsA("BasePart") then promptPos = parent.Position
                    elseif parent:IsA("Model") then promptPos = parent:GetPivot().Position end
                end

                if promptPos then
                    local key = ("%.1f_%.1f_%.1f"):format(promptPos.X, promptPos.Y, promptPos.Z)
                    if not seenPositions[key] then
                        seenPositions[key] = true
                        local model = parent and (parent:IsA("Model") and parent or parent.Parent)
                        local nameStr = (model and model.Name) or prompt.Name
                        local cat = (model and (model:GetAttribute("AssetCategory") or model:GetAttribute("Category"))) or nameStr
                        local areaId = detectAreaFromInstance(model) or detectAreaFromInstance(prompt) or 1

                        table.insert(list, {
                            Uid = (model and model:GetAttribute("Uid")) or ("Egg_" .. tostring(#list + 1)),
                            AssetCategory = cat,
                            AreaId = areaId,
                            State = "Slot",
                            BottomCFrame = CFrame.new(promptPos),
                            Instance = model,
                            Prompt = prompt
                        })
                    end
                end
            end
        end
    end

    -- 3. Workspace model scan fallback
    local objs = Workspace:FindFirstChild("__OBJECTS") or Workspace
    local candidates = { objs:FindFirstChild("Eggs"), objs:FindFirstChild("AreaEggs"), Workspace:FindFirstChild("AreaEggs"), objs:FindFirstChild("Areas") }
    for _, folder in ipairs(candidates) do
        if folder then
            for _, model in ipairs(folder:GetDescendants()) do
                if model:IsA("Model") and (model:GetAttribute("AssetCategory") or string.find(model.Name, "Egg", 1, true)) then
                    local cf = model:GetPivot()
                    local pos = cf.Position
                    local key = ("%.1f_%.1f_%.1f"):format(pos.X, pos.Y, pos.Z)
                    if not seenPositions[key] then
                        seenPositions[key] = true
                        local uid = model:GetAttribute("Uid") or model.Name
                        local cat = model:GetAttribute("AssetCategory") or model:GetAttribute("Category") or model.Name
                        local areaId = detectAreaFromInstance(model) or 1
                        table.insert(list, {
                            Uid = uid,
                            AssetCategory = cat,
                            AreaId = areaId,
                            State = "Slot",
                            BottomCFrame = cf,
                            Instance = model
                        })
                    end
                end
            end
        end
    end

    return list
end

-- every egg row carries the pet it will hatch into, so what is in the bag or
-- growing in the pen is knowable before it cracks
local function eggRoster()
    local recs = myRuntimeRecords()
    local now  = os.time()
    local list = {}

    for uid, raw in pairs(eggInventory()) do
        if type(raw) == "table" and raw.AssetCategory then
            local rec    = recs[uid]
            local placed = rec and rec.Placement or nil
            local ready, remain = false, nil

            if placed then
                local ok, r = false, false
                if EggCmds and EggCmds.IsLocalEggReady then ok, r = pcall(EggCmds.IsLocalEggReady, uid) end
                ready = ok and r == true

                if not ready and EggUtil and EggUtil.GetGrowthDuration then
                    local okd, dur = pcall(EggUtil.GetGrowthDuration, rec)
                    if okd and tonumber(dur) then
                        local credit = (tonumber(placed.GrowthCreditSeconds) or 0)
                            + (tonumber(placed.NightGrowthCreditSeconds) or 0)
                        local elapsed = now - (tonumber(placed.PlacedAt) or now) + credit
                        remain = math.max(0, math.floor(tonumber(dur) - elapsed))
                    end
                end
            end

            list[#list + 1] = {
                uid = uid, cat = raw.AssetCategory, rarity = rarityOf(raw.AssetCategory),
                placed = placed ~= nil, ready = ready, remain = remain,
            }
        end
    end
    return list
end

local function clockText(sec)
    if not sec then return "?" end
    if sec <= 0 then return "พร้อม" end
    if sec < 60 then return ("%ds"):format(sec) end
    if sec < 3600 then return ("%dm"):format(math.floor(sec / 60)) end
    return ("%dh %dm"):format(math.floor(sec / 3600), math.floor(sec % 3600 / 60))
end

function unplacedEggs()
    local recs = myRuntimeRecords()
    local out = {}
    for uid, raw in pairs(eggInventory()) do
        if not (recs[uid] and recs[uid].Placement) and type(raw) == "table" and raw.AssetCategory then
            local price = 0
            if EggUtil and EggUtil.GetSellPrice then
                local ok, p = pcall(EggUtil.GetSellPrice, raw)
                if ok then price = tonumber(p) or 0 end
            end
            if price == 0 then price = earnRate(raw.AssetCategory) * 100 end
            out[#out + 1] = { uid = uid, cat = raw.AssetCategory, rarity = rarityOf(raw.AssetCategory), price = price }
        end
    end
    return out
end

function occupiedSpots()
    local spots = {}
    for _, rec in pairs(myRuntimeRecords()) do
        if rec.Placement and rec.Placement.LocalCFrame then
            local lcf = rec.Placement.LocalCFrame
            if typeof(lcf) == "CFrame" then
                table.insert(spots, lcf.Position)
            elseif typeof(lcf) == "Vector3" then
                table.insert(spots, lcf)
            elseif type(lcf) == "table" then
                if lcf.Position then
                    table.insert(spots, lcf.Position)
                elseif lcf.X and lcf.Y and lcf.Z then
                    table.insert(spots, Vector3.new(lcf.X, lcf.Y, lcf.Z))
                elseif lcf[1] and lcf[2] and lcf[3] then
                    table.insert(spots, Vector3.new(lcf[1], lcf[2], lcf[3]))
                end
            end
        end
    end
    return spots
end

function nextFreeSpot(taken)
    local pd = plotData()
    if not pd or not pd.PetArea then return nil end
    local pa = pd.PetArea
    local center = (pd.CenterPoint and pd.CenterPoint.CFrame) or pa.CFrame
    local halfX = math.max(pa.Size.X * 0.5 - 4, 2)
    local halfZ = math.max(pa.Size.Z * 0.5 - 4, 2)
    local spacing = 4.5

    for radius = 0, math.max(halfX, halfZ), spacing do
        for dx = -radius, radius, spacing do
            for dz = -radius, radius, spacing do
                if math.abs(dx) == radius or math.abs(dz) == radius or radius == 0 then
                    if math.abs(dx) <= halfX and math.abs(dz) <= halfZ then
                        local world = pa.CFrame * CFrame.new(dx, 0, dz)
                        local flat  = CFrame.new(world.Position.X, pa.CFrame.Position.Y, world.Position.Z)
                        local lcf   = center:ToObjectSpace(flat)
                        local free  = true
                        for _, p in ipairs(taken) do
                            if (Vector3.new(p.X, 0, p.Z) - Vector3.new(lcf.X, 0, lcf.Z)).Magnitude < spacing - 0.5 then
                                free = false break
                            end
                        end
                        if free then return lcf end
                    end
                end
            end
        end
    end
    return nil
end

function atHome()
    local root = hrp()
    if not root then return false end
    local ok, inside = pcall(PlotCmds.IsWorldPositionWithinLocalPlotBounds, root.Position)
    return ok and inside == true
end

function goHome()
    local pd = plotData()
    if not pd or not pd.PetArea then return false end
    return moveTo(pd.PetArea.CFrame.Position + Vector3.new(0, 4, 0))
end

function hatchReady()
    local hatched = 0
    for uid, rec in pairs(myRuntimeRecords()) do
        if rec.Placement then
            local ok, ready = pcall(EggCmds.IsLocalEggReady, uid)
            if ok and ready then
                if EggCmds.RequestHatchEgg(uid) then
                    task.wait(0.15)
                    if EggCmds.RequestCompleteHatchEgg(uid) then
                        hatched += 1
                        Runtime.stats.hatched += 1
                        logLine("hatch", ("ฟัก %s"):format(rec.AssetCategory or "?"))
                    end
                end
            end
        end
    end
    return hatched
end

function approachPlot(tries)
    local pd   = plotData()
    local spot = pd and pd.PetArea and (pd.PetArea.CFrame.Position + Vector3.new(0, 4, 0))
    if not spot then return false end

    for _ = 1, tries or 3 do
        if instantOK() then
            -- the gate is crossed first: stepping straight out of the zone is
            -- what left the character short of the plot
            if insideGameplay() then stepTo(GATE_IN, 8) end
            stepTo(spot, 12)
        else
            goHome()
        end
        task.wait(0.1)
        if atHome() then return true end
        local root = hrp()
        if root and (root.Position - spot).Magnitude < 30 then return true end
    end
    return false
end

function placeEggs()
    local queue = {}
    for _, egg in ipairs(unplacedEggs()) do
        if Cfg.PlaceRarities[egg.rarity] then queue[#queue + 1] = egg end
    end
    if #queue == 0 then return 0 end
    table.sort(queue, function(a, b) return rankOf(a.cat) > rankOf(b.cat) end)

    if not approachPlot(3) then
        logLine("place", "ไปฐานไม่ถึง — ยังวางไข่ไม่ได้ รอบหน้าลองใหม่")
        return 0
    end

    local taken, placed = occupiedSpots(), 0
    local closeFails, otherFails = 0, 0

    for _, egg in ipairs(queue) do
        local lcf = nextFreeSpot(taken)
        if not lcf then break end

        local ok, err = EggCmds.RequestPlaceEgg(egg.uid, lcf)

        if not ok and tostring(err):find("closer") then
            -- walk back and retry the SAME egg on the SAME spot; the spot is
            -- only marked taken once the server has actually rejected it for a
            -- reason other than distance
            closeFails += 1
            if closeFails > 2 or not approachPlot(2) then break end
            ok, err = EggCmds.RequestPlaceEgg(egg.uid, lcf)
        end

        if ok then
            placed += 1
            closeFails, otherFails = 0, 0
            table.insert(taken, lcf.Position)
            task.wait(0.08)
        elseif tostring(err):find("closer") then
            break
        else
            table.insert(taken, lcf.Position)
            otherFails += 1
            if otherFails >= 3 then break end
        end
    end

    if placed > 0 then
        logLine("place", ("วางไข่ %d ใบ"):format(placed))
        toAnchor()
    end
    return placed
end

function eggSellPlan()
    local plan = {}
    local limit = tonumber(Cfg.SellEggBelow) or 0
    for _, egg in ipairs(unplacedEggs()) do
        local byRarity = Cfg.SellEggRarities[egg.rarity] == true
        local byPrice  = limit > 0 and egg.price < limit
        if byRarity or byPrice then plan[#plan + 1] = egg end
    end
    table.sort(plan, function(a, b) return a.price < b.price end)
    return plan
end

-- selling an egg needs it in our hands first: equip the tool, then fire the sale
function sellEggsOnce(count)
    local plan = eggSellPlan()
    if #plan == 0 then return 0 end

    local sold = 0
    for i = 1, math.min(count or 1, #plan) do
        local egg = plan[i]
        if invoke(NET.Eggs.REQUEST_EQUIP_TOOL, egg.uid) then
            fire(NET.AssetInventory.SELL_ASSET, { egg.uid })
            sold += 1
            Runtime.stats.soldEggs += 1
            task.wait(0.04)
        end
    end
    if sold > 0 then
        pcall(function() invoke(NET.Eggs.REQUEST_UNEQUIP_TOOL) end)
        saveCache = nil
        logLine("sell", ("ขายไข่ %d ใบ"):format(sold))
    end
    return sold
end

function sellEggs(maxCount)
    local total = 0
    for _ = 1, math.min(maxCount or (Cfg.SellMaxPerRun or 60), 200) do
        local n = sellEggsOnce(1)
        if n == 0 then break end
        total += n
        task.wait(0.04)
    end
    return total
end

----------------------------------------------------------------
-- Pets: Equip / Sell
----------------------------------------------------------------
function equipBest()
    invoke(NET.Backpack.EQUIP_BEST)

    local limit = tonumber(invoke(NET.ActiveAssets.REQUEST_EQUIP_LIMIT)) or 0
    if limit <= 0 then return 0 end

    local list = petItems(false, 40)
    if #list == 0 then return 0 end
    table.sort(list, function(a, b) return a.rate > b.rate end)

    local want, equipped = {}, {}
    for i = 1, math.min(limit, #list) do want[list[i].uid] = true end
    for _, p in ipairs(list) do if p.equipped then equipped[p.uid] = true end end

    local changed = 0
    for uid in pairs(equipped) do
        if not want[uid] then
            if AssetCmds then pcall(AssetCmds.RequestUnequipAsset, uid) else invoke(NET.ActiveAssets.REQUEST_UNEQUIP, uid) end
            changed += 1
            task.wait(0.06)
        end
    end
    for uid in pairs(want) do
        if not equipped[uid] then
            if AssetCmds then pcall(AssetCmds.RequestEquipAsset, uid) else invoke(NET.ActiveAssets.REQUEST_EQUIP, uid) end
            changed += 1
            task.wait(0.06)
        end
    end
    return changed
end

-- ActiveAssets.REQUEST_SELL takes ONE uid, is validated server side, and answers
-- from anywhere. Equipped / favorite / in-fuse / top-N are hard rails.
function sellPetPlan(yieldEvery)
    local list = petItems(false, yieldEvery)
    table.sort(list, function(a, b) return a.rate > b.rate end)

    local keep = {}
    for i = 1, math.min(Cfg.SellKeepTop or 0, #list) do keep[list[i].uid] = true end

    local plan = {}
    for _, p in ipairs(list) do
        if not (p.equipped or p.favorite or p.inFuse or keep[p.uid]) then
            local limit    = tonumber(Cfg.SellPetBelow) or 0
            local byPrice  = limit > 0 and p.price < limit
            local byRarity = Cfg.SellPetRarities[p.rarity] == true

            local take
            if Cfg.SellPetMode == "ระดับที่เลือก" then take = byRarity
            elseif Cfg.SellPetMode == "เข้าทั้งสองเงื่อนไข" then take = byRarity and byPrice
            else take = byPrice end

            if take then plan[#plan + 1] = p end
        end
    end
    table.sort(plan, function(a, b) return a.price < b.price end)
    return plan
end

Runtime.sellDirty   = true
Runtime.sellQueue   = {}
Runtime.sellFlight  = 0
Runtime.sellLastScan = 0

function markSellDirty() Runtime.sellDirty = true end

function refillSellQueue()
    local plan = sellPetPlan(40)
    table.clear(Runtime.sellQueue)
    for i = #plan, 1, -1 do Runtime.sellQueue[#Runtime.sellQueue + 1] = plan[i] end

    local total = 0
    for _, p in ipairs(plan) do total += p.price end
    local head = {}
    for i = 1, math.min(5, #plan) do
        head[#head + 1] = ("• %s (%s) — %s"):format(plan[i].cat, RARITY_TH[plan[i].rarity] or plan[i].rarity, shortNum(plan[i].price))
    end
    if #plan > 5 then head[#head + 1] = ("...และอีก %d ตัว"):format(#plan - 5) end
    Runtime.sellPreview = { count = #plan, total = total, head = table.concat(head, "\n") }

    Runtime.sellDirty = false
    Runtime.sellLastScan = os.clock()
end

function sellPets(maxCount)
    local plan = sellPetPlan()
    local cap  = math.min(maxCount or math.huge, math.max(1, Cfg.SellMaxPerRun or 60))
    local sold, earned = 0, 0

    for _, p in ipairs(plan) do
        if sold >= cap then break end
        if invoke(NET.ActiveAssets.REQUEST_SELL, p.uid) then
            sold += 1
            earned += p.price
        end
        task.wait(0.03)
    end

    Runtime.stats.soldPets += sold
    saveCache, petCache = nil, nil
    markSellDirty()
    if sold > 0 then logLine("sell", ("ขายสัตว์ %d ตัว ได้ %s"):format(sold, shortNum(earned))) end
    return sold, earned
end

----------------------------------------------------------------
-- Stealing Engine (SAE_Nyx)
----------------------------------------------------------------
-- WalkSpeed is 0 while the humanoid loads, so the gate uses the highest speed
-- seen this session instead of the live value
function walkSpeedNow()
    local hum  = humanoid()
    local live = hum and hum.WalkSpeed or 0
    if live > (Runtime.bestWalkSpeed or 0) then Runtime.bestWalkSpeed = live end
    return math.max(live, Runtime.bestWalkSpeed or 0, 16)
end


function areaReachable(areaId)
    if Cfg.MoveMode == "Instant" or Cfg.MoveMode == "Warp" then return true end
    local need = AREA_SPEED[areaId]
    if not need then return true end
    return walkSpeedNow() > need + 1
end

function guardState(areaId)
    local model = GuardAreasFolder and GuardAreasFolder:FindFirstChild(areaId)
    local guard = model and model:FindFirstChild("Guard")
    return guard and guard:GetAttribute("GuardState") or nil
end

function eggPasses(rec)
    if not rec or not rec.AssetCategory then return false end
    if rec.State and rec.State ~= "Slot" and rec.State ~= "Dropped" and rec.State ~= "Unclaimed" then return false end
    local until_ = Runtime.skip[rec.Uid]
    if until_ and os.clock() < until_ then return false end
    -- First-area eggs need a slot key when carried (Guardless.carry supplies
    -- it below); do not filter them out before they can reach that path.
    if not checkAreaAllowed(rec.AreaId) then return false end
    if not areaReachable(rec.AreaId) then return false end
    if Cfg.StealRarities and Cfg.StealRarities[rarityOf(rec.AssetCategory)] == false then return false end
    local muts = 0
    if type(rec.Mutations) == "table" then for _ in pairs(rec.Mutations) do muts += 1 end end
    if muts < (Cfg.MinMutations or 0) then return false end
    return true
end

-- every condition eggPasses uses except the skip timer: if this count can see an
-- egg the picker cannot, the run stalls in that gap with no reason to hop
function matchingEggCount()
    local n = 0
    for _, rec in pairs(areaEggRecords()) do
        if rec and rec.AssetCategory
            and (rec.State == "Slot" or rec.State == "Dropped")
            and checkAreaAllowed(rec.AreaId)
            and areaReachable(rec.AreaId)
            and Cfg.StealRarities[rarityOf(rec.AssetCategory)] then
            local muts = 0
            if type(rec.Mutations) == "table" then for _ in pairs(rec.Mutations) do muts += 1 end end
            if muts >= Cfg.MinMutations then n += 1 end
        end
    end
    return n
end

function bestEgg()
    local hum = humanoid()
    local speed = math.max((hum and hum.WalkSpeed or 16) * (Cfg.WarpPct or 0.8), 50)
    local guards = guardPositions()

    local best, bestScore
    for _, rec in pairs(areaEggRecords()) do
        if eggPasses(rec) then
            local value = rankOf(rec.AssetCategory) * 1e6 + earnRate(rec.AssetCategory) / 1e3
            if type(rec.Mutations) == "table" then
                for _ in pairs(rec.Mutations) do value *= 1.5 end
            end
            if guardState(rec.AreaId) == "Chasing" then value *= 0.5 end
            if rec.BottomCFrame and nearestGuardDist(rec.BottomCFrame.Position, guards) < 8 then value *= 0.1 end

            local score = value
            if Cfg.PickMode == "Efficient" then
                local dist = rec.BottomCFrame and (rec.BottomCFrame.Position - ANCHOR).Magnitude or 3000
                score = value / (dist * 2 / speed + 4)
            end
            if not bestScore or score > bestScore then best, bestScore = rec, score end
        end
    end
    return best
end

-- first-area eggs are addressed by a synthetic slot uid and the server answers
-- "Egg not found" unless the matching slot key rides along
Guardless.SlotIdentity = safeRequire((Shared and Shared:FindFirstChild("Util") and Shared.Util:FindFirstChild("AreaEggSlotIdentity")) or findModule("AreaEggSlotIdentity"))

function Guardless.slotKey(rec)
    if not (Guardless.SlotIdentity and rec and rec.Uid) then return nil end
    local ok, isFirst = pcall(Guardless.SlotIdentity.IsFirstAreaUid, rec.Uid)
    if not (ok and isFirst) then return nil end
    local okk, key = pcall(Guardless.SlotIdentity.BuildSlotKey, rec.AreaId, rec.NestId)
    return okk and key or nil
end

local function nearestCarryPrompt(pos)
    local best, bestDistance = nil, math.huge
    for _, prompt in ipairs(Workspace:GetDescendants()) do
        if prompt:IsA("ProximityPrompt") then
            local pName = prompt.Name
            if pName == "CarryAreaEgg" or string.find(pName, "Egg", 1, true) or string.find(pName, "Steal", 1, true) or string.find(pName, "Carry", 1, true) then
                local parent = prompt.Parent
                local promptPos
                if parent and parent:IsA("Attachment") then
                    promptPos = parent.WorldPosition
                elseif parent and parent:IsA("BasePart") then
                    promptPos = parent.Position
                elseif parent and parent:IsA("Model") then
                    promptPos = parent:GetPivot().Position
                end
                if promptPos then
                    local distance = (promptPos - pos).Magnitude
                    if distance < bestDistance then
                        best, bestDistance = prompt, distance
                    end
                end
            end
        end
    end
    return best
end

local function HookProximityPrompts()
    pcall(function()
        local PPS = game:GetService("ProximityPromptService")
        for _, pp in ipairs(Workspace:GetDescendants()) do
            if pp:IsA("ProximityPrompt") then
                pp.HoldDuration = 0
                pp.MaxActivationDistance = 9999
                pp.RequiresLineOfSight = false
                pp.Enabled = true
            end
        end
        PPS.PromptShown:Connect(function(pp)
            if pp and pp:IsA("ProximityPrompt") then
                pp.HoldDuration = 0
                pp.MaxActivationDistance = 9999
                pp.RequiresLineOfSight = false
                pp.Enabled = true
            end
        end)
    end)
end
HookProximityPrompts()

local function triggerPrompt(prompt)
    if not prompt or not prompt:IsA("ProximityPrompt") then return false end
    pcall(function()
        prompt.HoldDuration = 0
        prompt.MaxActivationDistance = 9999
        prompt.RequiresLineOfSight = false
        prompt.Enabled = true
    end)

    local firePP = (typeof(fireproximityprompt) == "function" and fireproximityprompt)
        or (typeof(getgenv) == "function" and getgenv().fireproximityprompt)

    if firePP then
        pcall(firePP, prompt, 0)
        pcall(firePP, prompt)
    end
    pcall(function()
        if prompt.InputHoldBegan then
            prompt:InputHoldBegan()
            task.wait(0.01)
            prompt:InputHoldEnded()
        end
    end)
    return true
end

----------------------------------------------------------------
-- Egg ESP Engine
----------------------------------------------------------------
local ESP_FOLDER = nil
local function getESPFolder()
    if ESP_FOLDER and ESP_FOLDER.Parent then return ESP_FOLDER end
    local p = LocalPlayer:FindFirstChildOfClass("PlayerGui") or game:GetService("CoreGui")
    local folder = p:FindFirstChild("EggESP_Container")
    if not folder then
        folder = Instance.new("Folder")
        folder.Name = "EggESP_Container"
        folder.Parent = p
    end
    ESP_FOLDER = folder
    return folder
end

local RARITY_COLORS = {
    Basic       = Color3.fromRGB(200, 200, 200),
    Common      = Color3.fromRGB(180, 180, 180),
    Uncommon    = Color3.fromRGB(100, 220, 100),
    Rare        = Color3.fromRGB(60, 160, 255),
    SuperRare   = Color3.fromRGB(30, 120, 255),
    Superior    = Color3.fromRGB(140, 100, 255),
    Epic        = Color3.fromRGB(180, 80, 255),
    Legendary   = Color3.fromRGB(255, 200, 40),
    Mythic      = Color3.fromRGB(255, 60, 120),
    Mythical    = Color3.fromRGB(255, 40, 160),
    Celestial   = Color3.fromRGB(120, 220, 255),
    Cosmic      = Color3.fromRGB(0, 230, 255),
    Divine      = Color3.fromRGB(255, 255, 255),
    Secret      = Color3.fromRGB(255, 35, 35),
    Eternal     = Color3.fromRGB(255, 215, 0),
    Exclusive   = Color3.fromRGB(255, 105, 180),
    Exotic      = Color3.fromRGB(255, 140, 0),
    Limited     = Color3.fromRGB(0, 250, 154),
    Rainbow     = Color3.fromRGB(255, 80, 220),
    Titan       = Color3.fromRGB(160, 32, 240),
    BrainrotGod = Color3.fromRGB(255, 0, 0),
}

local function clearEggESP()
    pcall(function()
        local p = LocalPlayer:FindFirstChildOfClass("PlayerGui") or game:GetService("CoreGui")
        local folder = p:FindFirstChild("EggESP_Container")
        if folder then folder:Destroy() end
    end)
end

local function updateEggESP()
    clearEggESP()
end

local function unequipCharacterTools()
    pcall(function()
        local hum = humanoid()
        if hum then hum:UnequipTools() end
    end)
end

local function isCarryingEgg()
    local p = LocalPlayer:FindFirstChild("PlayerGui")
    if p then
        local dhe = p:FindFirstChild("DropHeldEgg")
        if dhe then
            if dhe:IsA("ScreenGui") and dhe.Enabled == true then return true end
            if dhe:IsA("GuiObject") and dhe.Visible == true then return true end
        end
    end
    if Runtime.carryUid ~= nil then return true end

    local c = char()
    if c then
        for _, child in ipairs(c:GetChildren()) do
            if (string.find(child.Name, "Carry", 1, true) or (string.find(child.Name, "Egg", 1, true) and not child:IsA("Tool")))
               and (child:IsA("Model") or child:IsA("Accoutrement")) then
                return true
            end
        end
    end
    return false
end

local carryBusy = false
function Guardless.carry(rec)
    if carryBusy then return false, "carry request already pending" end
    if not (rec and rec.BottomCFrame) then return false, "egg has no position" end

    unequipCharacterTools()
    carryBusy = true
    HookProximityPrompts()

    local root = hrp()
    local eggPos = rec.BottomCFrame.Position

    if root then
        root.AssemblyLinearVelocity = Vector3.zero
        root.AssemblyAngularVelocity = Vector3.zero
        if (root.Position - eggPos).Magnitude > 50 then
            warpChain(eggPos, 4, { guardGap = Cfg.GuardSafeGap })
        else
            root.CFrame = CFrame.new(eggPos.X, eggPos.Y + 2, eggPos.Z)
        end
        task.wait(0.08)
    end

    local carried = false
    for attempt = 1, 12 do
        if carried then break end

        local prompt = (rec and rec.Prompt) or nearestCarryPrompt(eggPos)
        if prompt then
            triggerPrompt(prompt)
        end

        local t0 = os.clock()
        while os.clock() - t0 < 0.3 do
            if isCarryingEgg() then carried = true break end
            task.wait(0.04)
        end
        local r = hrp()
        if not carried and attempt % 4 == 0 and r and (r.Position - eggPos).Magnitude > 6 then
            r.CFrame = CFrame.new(eggPos.X, eggPos.Y + 2, eggPos.Z)
            r.AssemblyLinearVelocity = Vector3.zero
        end
    end

    carryBusy = false
    return carried
end

local function stealRoutine(rec)
    unequipCharacterTools()
    local conn
    local claimed = false
    pcall(function() conn = EggCmds.AreaEggClaimed:Connect(function() claimed = true end) end)

    local function finish(ok, err)
        if conn then pcall(function() conn:Disconnect() end) end
        Runtime.carryUid = nil
        if not ok then Runtime.skip[rec.Uid] = os.clock() + 8 end
        toAnchor()
        unequipCharacterTools()
        return ok, err
    end

    if not Nav.waitCurtain(90) then return finish(false, "ม่านยังไม่เปิด") end

    local root = hrp()
    if root and math.abs(root.Position.Z - ANCHOR.Z) > 18 then
        warpTo(Vector3.new(root.Position.X, root.Position.Y, ANCHOR.Z), 4, { ignoreGuards = true, timeout = 6 })
    end
    warpTo(GATE_IN, 4, { ignoreGuards = true, timeout = 8 })
    if not insideGameplay() then return finish(false, "เข้าโซนไม่สำเร็จ") end

    if EggCmds and EggCmds.RequestAreaEggSnapshot then pcall(EggCmds.RequestAreaEggSnapshot) end
    task.wait(0.05)
    for _, live in pairs(areaEggRecords()) do
        if live.Uid == rec.Uid and live.BottomCFrame then egg = live.BottomCFrame.Position break end
    end
    if nearestGuardDist(egg) < 10 then return finish(false, "ยามเฝ้ารังอยู่") end

    local moved, moveErr = warpChain(egg, 2, { guardGap = Cfg.GuardSafeGap })
    if not moved then return finish(false, moveErr or "ไปไม่ถึงไข่") end

    local unpin = pin(egg + Vector3.new(0, 3, 0))
    if not waitPinned(egg + Vector3.new(0, 3, 0), 8, 0.35) then
        unpin()
        return finish(false, "egg position was reset")
    end

    local grabbed, gerr
    local deadline = os.clock() + 2.5
    repeat
        grabbed, gerr = Guardless.carry(rec)
        if grabbed then break end
        local msg = tostring(gerr)
        if msg:find("not found") or msg:find("unavailable") or msg:find("full") then break end
        task.wait(0.1)
    until os.clock() > deadline
    unpin()
    if not grabbed then return finish(false, tostring(gerr)) end

    Runtime.carryUid = rec.Uid
    warpChain(GATE_IN, 4, { guardGap = 14 })
    toAnchor()
    unequipCharacterTools()

    local homeUnpin = pin(ANCHOR)
    waitPinned(ANCHOR, 8, 0.35)

    local t0 = os.clock()
    while not claimed and os.clock() - t0 < 4 do task.wait(0.05) end
    homeUnpin()
    if conn then pcall(function() conn:Disconnect() end) end
    Runtime.carryUid = nil
    if not claimed then toAnchor() return false, "ไข่หลุดระหว่างทาง" end

    Runtime.stats.eggs += 1
    local cat = rec.AssetCategory
    logLine("steal", ("ขโมย %s"):format(cat))
    if Cfg.WebhookSteal then
        webhook("🥷 ขโมยไข่สำเร็จ", ("**%s** (%s)"):format(cat, RARITY_TH[rarityOf(cat)] or rarityOf(cat)))
    end
    toAnchor()
    unequipCharacterTools()
    return true
end

local function GetLegalWalkSpeed()
    local defaultSpeed = 16
    local chr = char()
    if not chr then return defaultSpeed end
    local hum = chr:FindFirstChildOfClass("Humanoid")
    if not hum then return defaultSpeed end

    local okSave, save = pcall(Save.Get, LocalPlayer)
    if okSave and type(save) == "table" then
        local sp = save.SpeedPower or 0
        if TreadmillUtil and type(TreadmillUtil.SpeedPowerToWalkSpeed) == "function" then
            local okCalc, ws = pcall(TreadmillUtil.SpeedPowerToWalkSpeed, sp)
            if okCalc and type(ws) == "number" and ws > 0 then return ws end
        end
    end
    return hum.WalkSpeed > 0 and hum.WalkSpeed or defaultSpeed
end

local function GetTargetStealSpeed()
    local legal = GetLegalWalkSpeed()
    return math.max(legal * 3, 48)
end

local function FastWalk(targetPos, timeout, speedOverride)
    local chr = char()
    if not chr then return false end
    local root = hrp()
    local hum = humanoid()
    if not root or not hum then return false end

    local maxSpeed = speedOverride or GetTargetStealSpeed()
    local t0 = os.clock()
    local maxTime = timeout or 30

    while os.clock() - t0 < maxTime do
        if not chr.Parent or not root.Parent or hum.Health <= 0 then return false end
        local pos = root.Position
        local dist = (Vector3.new(pos.X, 0, pos.Z) - Vector3.new(targetPos.X, 0, targetPos.Z)).Magnitude

        if dist <= 4.5 then
            hum:MoveTo(pos)
            root.AssemblyLinearVelocity = Vector3.zero
            hum.WalkSpeed = math.min(maxSpeed, 40)
            return true
        end

        local speed = (dist < 25) and math.clamp(dist * 7, 24, maxSpeed) or maxSpeed
        hum.WalkSpeed = speed
        hum:MoveTo(targetPos)
        task.wait(0.03)
    end

    local finalPos = root.Position
    local finalDist = (Vector3.new(finalPos.X, 0, finalPos.Z) - Vector3.new(targetPos.X, 0, targetPos.Z)).Magnitude
    if finalDist <= 6 then
        hum:MoveTo(finalPos)
        root.AssemblyLinearVelocity = Vector3.zero
        return true
    end
    return false
end

-- ใช้ Warp 100% เคลื่อนที่ความเร็วสูง แม่นยำ ไม่วิ่งเลยไข่ (MoveMode=Instant/Warp)
local function instantSteal(rec)
    unequipCharacterTools()
    if not rec then rec = bestEgg() end
    if not rec then return false, "ไม่มีไข่ตรงตัวกรอง" end
    local eggPos = rec.BottomCFrame and rec.BottomCFrame.Position
    if not eggPos then return false, "ไม่มีตำแหน่งไข่" end

    local hum = humanoid()
    if not hrp() or (hum and hum:GetState() == Enum.HumanoidStateType.None) then
        task.wait(0.3)
        return false, "ตัวละครยังไม่พร้อม"
    end

    if not Nav.waitCurtain(90) then return false, "ม่านยังไม่เปิด" end

    local targetY = groundYAt(eggPos)
    local safeEggPos = targetY and Vector3.new(eggPos.X, targetY + MIN_HOVER, eggPos.Z) or (eggPos + Vector3.new(0, 2, 0))

    if insideGameplay() then warpChain(GATE_IN, 4, { guardGap = 14 }) end
    local okWarp, wErr = warpChain(safeEggPos, 2.5, { guardGap = Cfg.GuardSafeGap })
    if not okWarp then
        warpChain(GATE_IN, 4, { guardGap = 14 })
        toAnchor()
        unequipCharacterTools()
        return false, wErr or "ไปไม่ถึงไข่"
    end

    local root = hrp()
    if root and (root.Position - safeEggPos).Magnitude > 6 then
        root.CFrame = CFrame.new(safeEggPos)
        root.AssemblyLinearVelocity = Vector3.zero
    end

    local carried = false
    for attempt = 1, 12 do
        if carried then break end
        local pp = (rec and rec.Prompt) or nearestCarryPrompt(eggPos)
        if pp then triggerPrompt(pp) end
        local t0 = os.clock()
        while os.clock() - t0 < 0.3 do
            if isCarryingEgg() then carried = true break end
            task.wait(0.04)
        end
        local r = hrp()
        if not carried and attempt % 4 == 0 and r and (r.Position - safeEggPos).Magnitude > 6 then
            r.CFrame = CFrame.new(safeEggPos)
            r.AssemblyLinearVelocity = Vector3.zero
        end
    end

    if not carried then
        warpChain(GATE_IN, 4, { guardGap = 14 })
        toAnchor()
        unequipCharacterTools()
        return false, "อุ้มไข่ไม่สำเร็จ"
    end

    Runtime.carryUid = rec.Uid
    warpChain(GATE_IN, 4, { guardGap = 14 })
    toAnchor()
    unequipCharacterTools()

    local claimed = false
    local claimT0 = os.clock()
    while not claimed and os.clock() - claimT0 < 4 do
        for _, live in pairs(areaEggRecords()) do
            if live.Uid == rec.Uid and live.State == "Claimed" then claimed = true break end
        end
        if not isCarryingEgg() then claimed = true break end
        task.wait(0.1)
    end

    Runtime.carryUid = nil
    unequipCharacterTools()
    if not claimed then return false, "ไข่หลุดระหว่างทาง" end

    Runtime.stats.eggs += 1
    local cat = rec.AssetCategory
    logLine("steal", ("ขโมย %s สำเร็จ"):format(cat))
    if Cfg.WebhookSteal then
        webhook("🥷 ขโมยไข่สำเร็จ", ("**%s** (%s)"):format(cat, RARITY_TH[rarityOf(cat)] or rarityOf(cat)))
    end
    return true
end

-- ใช้ warpSteal (warpChain เดิน+เด้งตำแหน่ง ชัวร์ ไม่หลุด)
local function warpSteal(rec)
    return stealRoutine(rec or bestEgg())
end

local function stealOnce(rec)
    unequipCharacterTools()
    local hum = humanoid()
    if not hrp() or (hum and hum:GetState() == Enum.HumanoidStateType.None) then
        task.wait(0.3)
        return false, "ตัวละครยังไม่พร้อม"
    end

    rec = rec or bestEgg()
    if not rec then return false, "ไม่มีไข่ตรงตัวกรอง" end

    local eggPos = rec.BottomCFrame and rec.BottomCFrame.Position
    if not eggPos then return false, "ไม่มีตำแหน่งไข่" end

    if insideGameplay() then warpChain(GATE_IN, 4, { guardGap = 14 }) end
    local okOut, wErr = warpChain(eggPos, 2.5, { guardGap = Cfg.GuardSafeGap })
    if not okOut then
        warpChain(GATE_IN, 4, { guardGap = 14 })
        toAnchor()
        unequipCharacterTools()
        return false, wErr or "ไปไม่ถึงไข่"
    end

    local carried = false

    local root = hrp()
    if root and (root.Position - eggPos).Magnitude > 6 then
        root.CFrame = CFrame.new(eggPos.X, eggPos.Y + 2, eggPos.Z)
        root.AssemblyLinearVelocity = Vector3.zero
    end

    for attempt = 1, 12 do
        if carried then break end
        local pp = (rec and rec.Prompt) or nearestCarryPrompt(eggPos)
        if pp then triggerPrompt(pp) end

        local t0 = os.clock()
        while os.clock() - t0 < 0.3 do
            if isCarryingEgg() then carried = true break end
            task.wait(0.04)
        end
        local r = hrp()
        if not carried and attempt % 4 == 0 and r and (r.Position - eggPos).Magnitude > 6 then
            r.CFrame = CFrame.new(eggPos.X, eggPos.Y + 2, eggPos.Z)
            r.AssemblyLinearVelocity = Vector3.zero
        end
    end

    if not carried then
        warpChain(GATE_IN, 4, { guardGap = 14 })
        toAnchor()
        unequipCharacterTools()
        return false, "อุ้มไข่ไม่สำเร็จ"
    end

    Runtime.carryUid = rec.Uid
    warpChain(GATE_IN, 4, { guardGap = 14 })
    toAnchor()
    unequipCharacterTools()

    local claimed = false
    local claimT0 = os.clock()
    while not claimed and os.clock() - claimT0 < 4 do
        for _, live in pairs(areaEggRecords()) do
            if live.Uid == rec.Uid and live.State == "Claimed" then
                claimed = true
                break
            end
        end
        if not isCarryingEgg() then
            claimed = true
            break
        end
        task.wait(0.1)
    end

    Runtime.carryUid = nil
    unequipCharacterTools()
    Runtime.stats.eggs += 1
    local cat = rec.AssetCategory
    logLine("steal", ("ขโมย %s สำเร็จ"):format(cat))
    if Cfg.WebhookSteal then
        webhook("🥷 ขโมยไข่สำเร็จ", ("**%s** (%s)"):format(cat, RARITY_TH[rarityOf(cat)] or rarityOf(cat)))
    end
    return true
end

----------------------------------------------------------------
-- Bag Pressure & Progression Engine
----------------------------------------------------------------
local EGG_BAG_CAP = 115

local function bagCount()
    local n = 0
    for _ in pairs(eggInventory()) do n += 1 end
    return n
end

local function relieveBag(force)
    local bag = bagCount()
    if not force and bag < EGG_BAG_CAP - (Cfg.SellEggKeepBag or 20) then return false end
    hatchReady()
    if Cfg.AutoSellEggs then sellEggs(25) end
    if Cfg.AutoPlace then placeEggs() end
    return true
end

local function baseUpgradeStep()
    if not BasesDir or not BasesDir.BASES then return end
    local data = saveData()
    local lvl  = tonumber(data.BaseUpgradeLevel) or 0
    local maxLvl = 99
    if BasesDir.GetMaxBaseLevel then
        local ok, m = pcall(BasesDir.GetMaxBaseLevel)
        if ok and tonumber(m) then maxLvl = tonumber(m) end
    end
    if lvl >= maxLvl then return end

    local nextCfg = BasesDir.BASES[lvl + 1]
    local cost = nextCfg and tonumber(nextCfg.Cost)
    if not cost then return end
    if moneyNow() < cost then return end

    fire(NET.Plots.REQUEST_BASE_UPGRADE)
    logLine("base", ("อัปฐานเป็นเลเวล %d (%s)"):format(lvl + 1, shortNum(cost)))
    saveCache, petCache = nil, nil
end

local function treadmillUpgradeStep()
    if not TreadDir or not TreadDir.GetByUpgradeLevel then return end
    local data = saveData()
    local lvl  = tonumber(data.TreadmillUpgradeLevel) or 0

    local ok, nextTm = pcall(TreadDir.GetByUpgradeLevel, lvl + 1)
    if not ok or type(nextTm) ~= "table" then return end
    local price = tonumber(nextTm.Price) or math.huge
    if moneyNow() < price then return end

    invoke(NET.Treadmills.REQUEST_UPGRADE, nextTm._id)
    logLine("treadmill", ("อัปลู่วิ่งเป็น %s (%s)"):format(nextTm.DisplayName or nextTm._id, shortNum(price)))
    saveCache, petCache = nil, nil
end

local function trailsStep()
    if not TrailsDir or not TrailsDir.Directory then return end
    local data  = saveData()
    local owned = data.TrailInventory or {}
    local money = tonumber(data.Money) or 0

    local list = {}
    for id, cfg in pairs(TrailsDir.Directory) do
        if type(cfg) == "table" then
            list[#list + 1] = { id = cfg._id or id, price = tonumber(cfg.Price) or math.huge, speed = tonumber(cfg.SpeedMultiplier) or 0 }
        end
    end
    table.sort(list, function(a, b) return a.speed > b.speed end)

    for _, item in ipairs(list) do
        if not owned[item.id] and item.price > 0 and item.price <= money then
            invoke(NET.Trails.REQUEST_PURCHASE, item.id)
            logLine("trail", ("ซื้อเทรล %s"):format(item.id))
            saveCache, petCache = nil, nil
            task.wait(0.3)
            owned = saveData(true).TrailInventory or owned
            break
        end
    end

    for _, item in ipairs(list) do
        if owned[item.id] then
            if data.EquippedTrail ~= item.id then invoke(NET.Trails.REQUEST_SELECT, item.id) end
            return
        end
    end
end

-- the machine keeps whatever was loaded into it, and a fuse pays out an egg: so
-- clear the bag if needed, finish or empty what is loaded, then load a fresh trio
local function fuseFinish(label)
    local ok, err = invoke(NET.FuseMachine.START_FUSE)
    if not ok then return false, err end
    task.wait(0.4)
    invoke(NET.FuseMachine.COMPLETE_REVEAL)
    invoke(NET.FuseMachine.ACKNOWLEDGE_INFO)
    logLine("fuse", ("ผสม %s"):format(label))
    saveCache, petCache = nil, nil
    markSellDirty()
    return true
end

local function fuseStep()
    if bagCount() >= EGG_BAG_CAP - 1 then relieveBag(true) end

    local pets = petItems()
    local loaded, byCat = {}, {}
    for _, p in ipairs(pets) do
        if p.inFuse then
            loaded[#loaded + 1] = p
        elseif not (p.equipped or p.favorite) then
            byCat[p.cat] = byCat[p.cat] or {}
            table.insert(byCat[p.cat], p.uid)
        end
    end

    if #loaded >= 3 then
        local ok, err = fuseFinish(loaded[1].cat)
        if ok then return true end
        Runtime.lastError = "ผสมไม่ได้: " .. tostring(err)
        return false
    elseif #loaded > 0 then
        for _, p in ipairs(loaded) do invoke(NET.FuseMachine.REMOVE_MOB, p.uid) end
        saveCache, petCache = nil, nil
    end

    for cat, uids in pairs(byCat) do
        if #uids >= 3 then
            local inserted = 0
            for i = 1, 3 do
                if invoke(NET.FuseMachine.INSERT_MOB, uids[i]) then inserted += 1 end
            end
            if inserted == 3 then
                local ok, err = fuseFinish(cat .. " x3")
                if ok then return true end
                for i = 1, 3 do invoke(NET.FuseMachine.REMOVE_MOB, uids[i]) end
                Runtime.lastError = "ผสมไม่ได้: " .. tostring(err)
                return false
            end
            for i = 1, 3 do invoke(NET.FuseMachine.REMOVE_MOB, uids[i]) end
            return false
        end
    end
    return false
end

local function claimStep()
    invoke(NET.OfflineAssets.REQUEST_REDEEM)
    invoke(NET.Index.REQUEST_CLAIM_ALL)
end

----------------------------------------------------------------
-- Anti-Lag Engine
----------------------------------------------------------------
local KILL_CLASSES = {
    ParticleEmitter = true, Trail = true, Smoke = true, Fire = true,
    Sparkles = true, Beam = true, Explosion = true, Highlight = true,
}

local function stripInstance(inst)
    local cls = inst.ClassName
    if KILL_CLASSES[cls] then
        inst:Destroy()
        return
    end
    if Cfg.LowFXOnly then return end
    if cls == "Texture" or cls == "Decal" then
        inst:Destroy()
    elseif inst:IsA("BasePart") then
        inst.Material = Enum.Material.SmoothPlastic
        inst.Reflectance = 0
        inst.CastShadow = false
        -- RenderFidelity is not writable while the client is running.  Trying
        -- to set it once for every MeshPart spams the console and costs time;
        -- the material/shadow changes above are still sufficient for anti-lag.
    end
end

local function antiLagGlobals()
    pcall(function()
        Lighting.GlobalShadows = false
        Lighting.FogEnd = 1e6
        Lighting.Brightness = 1
        for _, fx in ipairs(Lighting:GetChildren()) do
            if fx:IsA("PostEffect") or fx:IsA("Atmosphere") then fx:Destroy() end
        end
        local terrain = Workspace:FindFirstChildOfClass("Terrain")
        if terrain then
            terrain.WaterWaveSize = 0
            terrain.WaterWaveSpeed = 0
            terrain.WaterReflectance = 0
            terrain.WaterTransparency = 0
        end
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
        settings().Rendering.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level01
    end)
end

local fullPassRunning = false
local FULL_PASS_MS = 0.004

local function antiLagFullPass()
    if fullPassRunning then return end
    fullPassRunning = true
    task.spawn(function()
        antiLagGlobals()
        local all = Workspace:GetDescendants()
        local slice = os.clock()
        for i = 1, #all do
            local inst = all[i]
            if inst.Parent then pcall(stripInstance, inst) end
            if os.clock() - slice > FULL_PASS_MS then
                task.wait()
                slice = os.clock()
            end
        end
        all = nil
        Runtime.lastError = nil
        logLine("lag", ("แก้แลค: กวาดครบทั้งแมพแล้ว"))
        fullPassRunning = false
    end)
end

local fxQueue = {}

Runtime.conns.fx = Workspace.DescendantAdded:Connect(function(inst)
    if not Cfg.AntiLag then return end
    fxQueue[#fxQueue + 1] = inst
end)

local function hideOtherPlayers()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            for _, part in ipairs(p.Character:GetDescendants()) do
                if part:IsA("BasePart") or part:IsA("Decal") then
                    pcall(function() part.LocalTransparencyModifier = 1 end)
                elseif part:IsA("ParticleEmitter") or part:IsA("Trail") then
                    pcall(function() part:Destroy() end)
                end
            end
        end
    end
end

local function antiLagSweep()
    if not Cfg.AntiLag then
        if #fxQueue > 0 then table.clear(fxQueue) end
        return
    end

    antiLagGlobals()

    local slice    = os.clock()
    local deadline = slice + 0.75
    while #fxQueue > 0 and os.clock() < deadline do
        local inst = table.remove(fxQueue)
        if inst and inst.Parent then pcall(stripInstance, inst) end
        if os.clock() - slice > 0.003 then
            RunService.Heartbeat:Wait()
            slice = os.clock()
        end
    end
    if #fxQueue > 4000 then table.clear(fxQueue) end

    if Cfg.HideOtherPlayers then pcall(hideOtherPlayers) end
end

local function gcSweep()
    local now = os.clock()
    for uid, until_ in pairs(Runtime.skip) do
        if now > until_ then Runtime.skip[uid] = nil end
    end
    for i = #Runtime.log, 41, -1 do Runtime.log[i] = nil end
    saveCache, petCache = nil, nil
    table.clear(fxQueue)
    pcall(function() collectgarbage("count") end)
end

----------------------------------------------------------------
-- Server Hop Engine (SAE_Nyx)
----------------------------------------------------------------
do
    local HOP_FILE = CFG_DIR .. "/hopstate.json"
    local AFK_FILE = CFG_DIR .. "/afkstate.json"
    local HOP_MEMORY_WINDOW = 300

    function Runtime.readHopState()
        local data = hasFS and readJson(HOP_FILE) or nil
        if type(data) ~= "table" then
            return { count = 0, at = 0, seen = {}, resetAt = os.time() }
        end

        local now     = os.time()
        local resetAt = tonumber(data.resetAt) or 0
        local raw     = type(data.seen) == "table" and data.seen or {}

        if resetAt <= 0 or (now - resetAt) >= HOP_MEMORY_WINDOW then
            return { count = tonumber(data.count) or 0, at = tonumber(data.at) or 0, seen = {}, resetAt = now }
        end

        local seen = {}
        for jobId, stamp in pairs(raw) do
            if type(jobId) == "string" and jobId ~= "" and type(stamp) == "number" then
                seen[jobId] = stamp
            end
        end

        local count = tonumber(data.count) or 0
        if (now - (tonumber(data.at) or 0)) > HOP_MEMORY_WINDOW then count = 0 end
        return { count = count, at = tonumber(data.at) or 0, seen = seen, resetAt = resetAt }
    end

    function Runtime.writeHopState(count, seen, resetAt)
        if not hasFS then return end
        local cur = Runtime.readHopState()
        pcall(function()
            writefile(HOP_FILE, HttpService:JSONEncode({
                count   = count or cur.count,
                at      = os.time(),
                seen    = seen or cur.seen,
                resetAt = resetAt or cur.resetAt or os.time(),
            }))
        end)
    end

    function Runtime.rememberServer(seen, jobId)
        seen  = seen or Runtime.readHopState().seen
        jobId = jobId or game.JobId
        if type(jobId) == "string" and jobId ~= "" then seen[jobId] = os.time() end
        return seen
    end

    function Runtime.hopMemorySize()
        local n = 0
        for _ in pairs(Runtime.readHopState().seen) do n += 1 end
        return n
    end

    function Runtime.writeAfkResume()
        if not hasFS then return end
        pcall(function() writefile(AFK_FILE, HttpService:JSONEncode({ at = os.time() })) end)
    end

    function Runtime.clearAfkResume()
        if not hasFS then return end
        pcall(function()
            if typeof(delfile) == "function" then delfile(AFK_FILE) else writefile(AFK_FILE, "{}") end
        end)
    end

    function Runtime.consumeAfkResume()
        if not hasFS then return false end
        local data = readJson(AFK_FILE)
        Runtime.clearAfkResume()
        if type(data) ~= "table" then return false end
        return (os.time() - (tonumber(data.at) or 0)) <= 180
    end

    function Runtime.zoneHasWanted()
        pcall(EggCmds.RequestAreaEggSnapshot)
        task.wait(0.25)

        local seen = 0
        for _ in pairs(areaEggRecords()) do seen += 1 end

        if seen > 0 then
            Runtime.lastEggsSeenAt = os.clock()
            return matchingEggCount() > 0, seen
        end

        local quietFor = os.clock() - (Runtime.lastEggsSeenAt or Runtime.stats.startedAt or 0)
        if (Runtime.eggTick or 0) > 0 and quietFor > 15 then
            return false, 0
        end
        return nil
    end

    function Runtime.hopIfTrulyEmpty()
        if not Cfg.AutoHop then return false end
        if Runtime.carryUid then return false end
        if Runtime.hopStopped then return false end
        if Runtime.hopRestUntil and os.clock() < Runtime.hopRestUntil then return false end

        if os.clock() - (Runtime.stats.startedAt or 0) < 12 then return false end
        if Runtime.lastHopAt and os.clock() - Runtime.lastHopAt < 20 then return false end

        local checks = math.clamp(tonumber(Cfg.HopChecks) or 3, 2, 6)
        local seenAny = 0
        for i = 1, checks do
            local has, seen = Runtime.zoneHasWanted()
            if has == nil then return false end
            if has then return false end
            seenAny = math.max(seenAny, seen or 0)
            if i < checks then task.wait(1) end
        end

        local chain = (Runtime.hopChain or 0) + 1
        Runtime.hopChain = chain
        Runtime.writeHopState(chain)

        local maxTries = math.clamp(tonumber(Cfg.HopMaxTries) or 6, 1, 30)
        if chain > maxTries then
            Runtime.hopRestUntil = os.clock() + 180
            Runtime.hopChain = 0
            Runtime.writeHopState(0)
            logLine("hop", ("ย้ายเซิร์ฟ %d ครั้งติดยังไม่เจอไข่ตรงตัวกรอง — พัก 3 นาทีแล้วค่อยย้ายต่อ"):format(chain - 1))
            Notify("ย้ายเซิร์ฟเวอร์",
                ("ย้ายมา %d ครั้งติดยังไม่เจอไข่ที่ตั้งไว้ — พัก 3 นาทีแล้วเริ่มย้ายใหม่ (ถ้าอยากได้เร็วกว่านี้ เพิ่มระดับไข่หรือโซนในตัวกรอง)"):format(chain - 1), 8)
            return false
        end

        Runtime.lastHopAt = os.clock()
        logLine("hop", ("โซนนี้ไม่มีไข่ตรงตัวกรอง (เช็ค %d ครั้ง, เห็นไข่ในแมพ %d ใบ) — ย้ายเซิร์ฟ ครั้งที่ %d")
            :format(checks, seenAny, chain))
        return Runtime.serverHop()
    end

    function Runtime.serverHop()
        if Runtime.afkOn then Runtime.writeAfkResume() end

        local state = Runtime.readHopState()
        local seen  = Runtime.rememberServer(state.seen)
        Runtime.writeHopState(state.count, seen, state.resetAt)

        local candidates = {}
        local pages, cursor = 0, nil

        repeat
            local url = ("https://games.roblox.com/v1/games/%d/servers/Public?sortOrder=Asc&limit=100"):format(game.PlaceId)
            if cursor then url = url .. "&cursor=" .. cursor end

            local ok, body = pcall(function() return HttpService:JSONDecode(game:HttpGet(url)) end)
            if not (ok and body and body.data) then break end

            for _, s in ipairs(body.data) do
                if s.id and s.id ~= game.JobId and not seen[s.id]
                    and s.playing and s.maxPlayers and s.playing < s.maxPlayers
                then
                    candidates[#candidates + 1] = s.id
                end
            end

            cursor = body.nextPageCursor
            pages += 1
        until not cursor or pages >= 3 or #candidates >= 60

        if #candidates == 0 then
            logLine("hop", "เซิร์ฟที่ยังไม่เคยเข้าหมดแล้ว — ล้างรายชื่อที่เคยไปแล้วเริ่มจำใหม่")
            Runtime.writeHopState(state.count, {}, os.time())
            pcall(TeleportService.Teleport, TeleportService, game.PlaceId, LocalPlayer)
            return false
        end

        local pick = candidates[math.random(1, #candidates)]
        Runtime.writeHopState(state.count, Runtime.rememberServer(seen, pick), state.resetAt)
        logLine("hop", ("ย้ายเซิร์ฟ: เลือกจาก %d ห้องที่ยังไม่เคยเข้า (จำไว้แล้ว %d ห้อง)")
            :format(#candidates, Runtime.hopMemorySize()))
        local ok = pcall(TeleportService.TeleportToPlaceInstance, TeleportService, game.PlaceId, pick, LocalPlayer)
        if not ok then
            pcall(TeleportService.Teleport, TeleportService, game.PlaceId, LocalPlayer)
            return false
        end
        return true
    end
end

do
    local st = Runtime.readHopState()
    Runtime.writeHopState(st.count, Runtime.rememberServer(st.seen), st.resetAt)
end

Runtime.hopChain = Runtime.readHopState().count

----------------------------------------------------------------
-- Job Scheduler Engine
----------------------------------------------------------------
local Jobs = {}

-- each job opens on its own slot so the first scheduler pass does not run all of
-- them inside one frame
function job(name, interval, fn)
    local slot = #Jobs
    Jobs[slot + 1] = {
        name = name, interval = interval, fn = fn,
        nextAt = os.clock() + 0.45 * slot,
    }
end

function withLock(fn, ...)
    if Runtime.lock or Runtime.stealing then return false end
    Runtime.lock = true
    local ok, err = pcall(fn, ...)
    Runtime.lock = false
    if not ok then Runtime.lastError = tostring(err) end
    return ok
end

job("respawn", 3, function()
    local c = char()
    local orphan = (c == nil) or (c.Parent == nil) or (not c:IsDescendantOf(Workspace))
    if not orphan then
        Runtime.orphanSince = nil
        return
    end

    Runtime.orphanSince = Runtime.orphanSince or os.clock()
    local stuckFor = os.clock() - Runtime.orphanSince

    local askedAgo = Runtime.resetAskedAt and (os.clock() - Runtime.resetAskedAt) or math.huge
    if stuckFor > 5 and askedAgo > 10 then
        Runtime.resetAskedAt = os.clock()
        logLine("move", ("ตัวละครหาย %ds — ขอเกิดใหม่"):format(math.floor(stuckFor)))
        pcall(function()
            local net = ReplicatedStorage:FindFirstChild("Network")
            local rr  = net and net:FindFirstChild("ClientCharacter: RequestCharacterReset")
            if rr then rr:FireServer() end
        end)
    end

    if stuckFor > 30 and not Runtime.orphanHopped then
        Runtime.orphanHopped = true
        logLine("hop", "ตัวละครไม่เกิดใหม่ — ย้ายเซิร์ฟ")
        pcall(Runtime.serverHop)
    end
end)

job("upkeep", 2, function()
    if Cfg.AutoSteal and Nav.onTreadmill() then
        logLine("move", "ติดลู่วิ่ง — ออกจากลู่")
        Nav.leaveTreadmill()
    end
    enforceWalkSpeed()
    if Cfg.GodMode then
        local hum = humanoid()
        if hum and hum.Health < hum.MaxHealth then hum.Health = hum.MaxHealth end
        if hum and not Runtime.conns.health then hardenCharacter() end
    end
    Runtime.instantReady = killClientIntegrity()
end)

-- Hatching and placing were starving. The steal thread holds Runtime.stealing
-- for a chain of up to 20 grabs and only drops it for ~0.35s between chains, so
-- a 4 second timer almost never found the lock free — measured 2026-08-17 with
-- 14 eggs in the bag and exactly 1 of them placed. The work is now a named
-- function that BOTH the timer and the steal thread can run, and the steal
-- thread hands the lock over itself at the end of every chain.
Runtime.eggsPending = false
Runtime.eggsRanAt   = 0

function eggUpkeep()
    if Cfg.AutoHatch then
        if hatchReady() > 0 then markSellDirty() end
    end
    if Cfg.AutoPlace then placeEggs() end
    if Cfg.AutoEquipBest then equipBest() end
    toAnchor()
    Runtime.eggsPending, Runtime.eggsRanAt = false, os.clock()
end

function eggUpkeepDue()
    if not (Cfg.AutoHatch or Cfg.AutoPlace or Cfg.AutoEquipBest) then return false end
    return Runtime.eggsPending or (os.clock() - Runtime.eggsRanAt) >= 20
end

job("eggs", 4, function()
    if not (Cfg.AutoHatch or Cfg.AutoPlace or Cfg.AutoEquipBest) then return end
    -- lock busy means the steal chain owns it; flag it and let that thread run
    -- the upkeep on its own way out instead of silently skipping the tick
    if not withLock(eggUpkeep) then Runtime.eggsPending = true end
end)

job("bag", 6, function()
    if not Cfg.AutoSteal then return end
    if bagCount() >= EGG_BAG_CAP - 3 then withLock(relieveBag, true) end
end)

-- the queue is rebuilt on change, not on a timer; this keeps it honest
job("sellScan", 4, function()
    if #Runtime.sellQueue == 0 or os.clock() - Runtime.sellLastScan > 12 then markSellDirty() end
end)

job("progress", 1, function()
    if Cfg.AutoBaseUpgrade then pcall(baseUpgradeStep) end
    if Cfg.AutoTreadmillUp then pcall(treadmillUpgradeStep) end
end)

-- ESP background job disabled to optimize game performance and remove lag

job("slowProgress", 15, function()
    if Cfg.AutoTrails then pcall(trailsStep) end
    if Cfg.AutoClaim then pcall(claimStep) end
    if Cfg.AutoFuse then withLock(fuseStep) end
end)

job("antilag", 30, function() pcall(antiLagSweep) end)
job("gc", 60, gcSweep)

task.spawn(function()
    while alive() do
        local now = os.clock()
        for _, j in ipairs(Jobs) do
            if not alive() then return end
            if now >= j.nextAt then
                j.nextAt = now + j.interval
                local ok, err = pcall(j.fn)
                if not ok then Runtime.lastError = j.name .. ": " .. tostring(err) end
                -- one job per frame: two of these landing together is the stutter
                RunService.Heartbeat:Wait()
                now = os.clock()
            end
        end
        task.wait(0.3)
    end
end)

----------------------------------------------------------------
-- Seller Thread (self-tuning pace)
----------------------------------------------------------------
task.spawn(function()
    local eggQueue, eggQueueAt = {}, 0

    local frameMax, frameConn = 0, nil
    local pace  = math.max(Cfg.SellPace or 0.05, 0.03)
    local lanes = math.clamp(Cfg.SellLanes or 3, 1, 6)
    local nextTune = 0
    local baseline, launched = nil, 0

    local function watchFrames(on)
        if on and not frameConn then
            frameConn = RunService.Heartbeat:Connect(function(dt)
                if dt > frameMax then frameMax = dt end
            end)
            Runtime.conns.frameWatch = frameConn
        elseif not on and frameConn then
            frameConn:Disconnect()
            frameConn = nil
            Runtime.conns.frameWatch = nil
            frameMax = 0
        end
    end

    while alive() do
        local paced = false

        if Runtime.sellDirty and Runtime.sellFlight == 0 then
            local ok, err = pcall(refillSellQueue)
            if not ok then
                Runtime.sellDirty = false
                Runtime.lastError = "sell: " .. tostring(err)
            end
        end

        if Cfg.AutoSellPets and #Runtime.sellQueue > 0 then
            if Runtime.sellFlight < lanes then
                local p = table.remove(Runtime.sellQueue)
                if p then
                    Runtime.sellFlight += 1
                    paced = true
                    launched += 1
                    task.spawn(function()
                        local ok = invoke(NET.ActiveAssets.REQUEST_SELL, p.uid)
                        Runtime.sellFlight -= 1
                        if ok then
                            Runtime.stats.soldPets += 1
                            Runtime.sellEarned = (Runtime.sellEarned or 0) + (p.price or 0)
                        end
                    end)
                end
            end
        end

        -- never mid-steal: holding an egg tool while carrying a stolen one is
        -- asking for a dropped egg
        if Cfg.AutoSellEggs and not Runtime.stealing then
            if #eggQueue == 0 and os.clock() - eggQueueAt > 3 then
                eggQueueAt = os.clock()
                local ok, plan = pcall(eggSellPlan)
                if ok and plan then
                    for i = #plan, 1, -1 do eggQueue[#eggQueue + 1] = plan[i] end
                end
            end
            if #eggQueue > 0 and not paced then
                local egg = table.remove(eggQueue)
                if egg and invoke(NET.Eggs.REQUEST_EQUIP_TOOL, egg.uid) then
                    fire(NET.AssetInventory.SELL_ASSET, { egg.uid })
                    Runtime.stats.soldEggs += 1
                    paced = true
                    launched += 1
                    if #eggQueue == 0 then
                        pcall(function() invoke(NET.Eggs.REQUEST_UNEQUIP_TOOL) end)
                        saveCache = nil
                    end
                end
            end
        elseif #eggQueue > 0 then
            table.clear(eggQueue)
        end

        local armed = Cfg.AutoSellPets or Cfg.AutoSellEggs
        watchFrames(armed)

        if armed and os.clock() >= nextTune then
            nextTune = os.clock() + 0.5
            local worst = frameMax
            frameMax = 0

            if worst > 0 then
                if launched == 0 then
                    baseline = baseline and (baseline * 0.8 + worst * 0.2) or worst
                elseif baseline then
                    local floorPace = math.max(Cfg.SellPace or 0.05, 0.03)
                    local hitch  = worst > math.max(baseline * 1.8, baseline + 0.03)
                    local smooth = worst <= baseline * 1.25

                    if hitch then
                        pace  = math.min(pace * 1.7, 0.8)
                        lanes = 1
                    elseif smooth then
                        pace  = math.max(pace * 0.8, floorPace)
                        lanes = math.min(lanes + 1, math.clamp(Cfg.SellLanes or 3, 1, 6))
                    end
                end
            end

            launched = 0
            Runtime.sellPace     = pace
            Runtime.sellLanes    = lanes
            Runtime.sellWorstMs  = math.floor(worst * 1000)
            Runtime.sellBaseMs   = baseline and math.floor(baseline * 1000) or nil
        end

        task.wait(paced and pace or 0.25)
    end
end)

----------------------------------------------------------------
-- Event-Driven Chained Steal Loop
----------------------------------------------------------------
Runtime.eggTick = 0
do
    local eggEvs = { "AreaEggSnapshotUpdated", "AreaEggUpdated", "AreaEggRemoved" }
    for i = 1, #eggEvs do
        local sig = EggCmds and EggCmds[eggEvs[i]]
        if type(sig) == "table" and sig.Connect then
            pcall(function()
                Runtime.conns["egg_" .. eggEvs[i]] = sig:Connect(function() Runtime.eggTick = Runtime.eggTick + 1 end)
            end)
        end
    end
end

local function waitForEggChange(timeout)
    local mark = Runtime.eggTick
    local t0 = os.clock()
    repeat
        task.wait(0.05)
    until Runtime.eggTick ~= mark or os.clock() - t0 > timeout or not alive() or not Cfg.AutoSteal
end

task.spawn(function()
    while alive() do
        if Cfg.AutoSteal and not Runtime.stealing and not Runtime.lock then
            Runtime.stealing = true
            local misses, chain, empty = 0, 0, false
            repeat
                local target = bestEgg()
                local ok, err
                if not target then
                    ok, err = false, "ไม่มีไข่ตรงตัวกรอง"
                else
                    -- Immediately interrupt treadmill if on one
                    if Nav.onTreadmill() then Nav.leaveTreadmill() end
                    
                    if instantOK() or Cfg.MoveMode == "Instant" then
                        ok, err = instantSteal(target)
                    elseif Cfg.MoveMode == "Warp" then
                        ok, err = warpSteal(target)
                    else
                        ok, err = stealOnce(target)
                    end
                end

                misses = ok and 0 or (misses + 1)
                chain += 1
                if not ok then
                    Runtime.lastError = tostring(err)
                    empty = tostring(err):find("ตัวกรอง") ~= nil
                    if tostring(err):find("เต็ม") then relieveBag(true) misses = 0 end
                    -- the server refuses a new grab while a stolen egg is still
                    -- in hand; walking it home banks it, dropping is the last
                    -- resort. Without this the chain jams on one error forever.
                    if tostring(err):find("carrying") or tostring(err):find("Already") then
                        Runtime.carryUid = nil
                        stepTo(GATE_IN, 6)
                        stepTo(ANCHOR, 6)
                        task.wait(0.25)
                        if tostring(err):find("carrying") then pcall(EggCmds.RequestDropHeldAreaEgg) end
                        misses = 0
                    end
                    if empty then Runtime.hopIfTrulyEmpty() end
                else
                    empty = false
                    if (Runtime.hopChain or 0) > 0 then
                        Runtime.hopChain = 0
                        Runtime.writeHopState(0)
                    end
                    Runtime.hopStopped = false
                end
            until misses >= 3 or chain >= 20 or eggUpkeepDue() or not Cfg.AutoSteal or not alive()
            Runtime.stealing = false
            toAnchor()

            -- the handover: the lock is free for exactly this call, so hatching
            -- and placing get a guaranteed turn after every chain
            if eggUpkeepDue() then withLock(eggUpkeep) end

            if empty then
                waitForEggChange(2)
            else
                task.wait(0.35)
            end
        end
        task.wait(0.15)
    end
end)

-----------------------------------------------------------------------------------------
-- 💎 OBSIDIAN GLASSMORPHIC 2 UI ENGINE (INTEGRATED MASTER SPECIFICATION)
-----------------------------------------------------------------------------------------
local ObsidianGlassEngine = (function()
    local CoreGui = game:GetService("CoreGui")
    local UserInputService = game:GetService("UserInputService")
    local TweenService = game:GetService("TweenService")
    local RunService = game:GetService("RunService")
    local Stats = game:GetService("Stats")
    local SoundService = game:GetService("SoundService")

    local COLORS = {
        backdrop = Color3.fromRGB(12, 5, 8),
        shell = Color3.fromRGB(20, 10, 14),
        glass = Color3.fromRGB(32, 14, 20),
        glassDeep = Color3.fromRGB(25, 11, 16),
        glassRaised = Color3.fromRGB(48, 18, 28),
        userPanel = Color3.fromRGB(28, 12, 18),
        surface = Color3.fromRGB(42, 18, 26),
        surfaceRaised = Color3.fromRGB(58, 24, 34),
        surfaceHover = Color3.fromRGB(78, 30, 44),
        surfacePressed = Color3.fromRGB(34, 14, 20),
        input = Color3.fromRGB(20, 9, 13),
        inputFocus = Color3.fromRGB(36, 14, 22),
        divider = Color3.fromRGB(140, 40, 60),
        primary = Color3.fromRGB(255, 45, 85),
        primaryHover = Color3.fromRGB(255, 75, 110),
        primaryPressed = Color3.fromRGB(210, 30, 65),
        secondary = Color3.fromRGB(52, 18, 28),
        text = Color3.fromRGB(255, 255, 255),
        textMuted = Color3.fromRGB(242, 218, 228),
        textFaint = Color3.fromRGB(210, 168, 182),
        cyan = Color3.fromRGB(255, 55, 85),
        success = Color3.fromRGB(46, 224, 140),
        warning = Color3.fromRGB(255, 185, 70),
        danger = Color3.fromRGB(255, 60, 60),
        disabled = Color3.fromRGB(50, 25, 32),
    }

    local Engine = { Options = {} }
    local ObsidianGlassEngine = Engine

local function playClickSound()
    pcall(function()
        local sound = Instance.new("Sound")
        sound.SoundId = "rbxassetid://6895079853"
        sound.Volume = 0.3
        sound.Parent = SoundService
        sound:Play()
        sound.Ended:Connect(function() sound:Destroy() end)
    end)
end

local customAvatarAsset = nil
local function loadCustomAvatarImage()
    if customAvatarAsset then return customAvatarAsset end
    local avatarUrl = "https://raw.githubusercontent.com/aslamdunk7/paypmboygang/main/543199739_2812856088914181_3062917809445648175_n.jpg"
    local fileName = "payomboyz_avatar.jpg"
    
    pcall(function()
        if typeof(writefile) == "function" and (typeof(getcustomasset) == "function" or typeof(getsynasset) == "function") then
            local getAsset = getcustomasset or getsynasset
            local isFileExist = (typeof(isfile) == "function" and isfile(fileName))
            if not isFileExist then
                local imageBytes = game:HttpGet(avatarUrl)
                if imageBytes and #imageBytes > 0 then
                    writefile(fileName, imageBytes)
                end
            end
            if typeof(isfile) == "function" and isfile(fileName) then
                customAvatarAsset = getAsset(fileName)
            end
        end
    end)

    if not customAvatarAsset then
        local userId = (LocalPlayer and LocalPlayer.UserId) or 1
        customAvatarAsset = "rbxthumb://type=AvatarHeadShot&id=" .. tostring(userId) .. "&w=150&h=150"
    end
    return customAvatarAsset
end

function ObsidianGlassEngine:Notify(cfg)
    pcall(function()
        local title = cfg.Title or "System"
        local content = cfg.Content or cfg.Text or ""
        local duration = cfg.Duration or 4
        
        local parentGui = (typeof(gethui) == "function") and gethui() or CoreGui
        local notifHolder = parentGui:FindFirstChild("ObsidianGlass_NotifHolder")
        if not notifHolder then
            notifHolder = Instance.new("ScreenGui")
            notifHolder.Name = "ObsidianGlass_NotifHolder"
            notifHolder.ResetOnSpawn = false
            notifHolder.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
            notifHolder.Parent = parentGui
        end
        
        local toast = Instance.new("Frame")
        toast.Size = UDim2.new(0, 300, 0, 65)
        toast.Position = UDim2.new(1, 20, 1, -85)
        toast.BackgroundColor3 = COLORS.glass
        toast.BackgroundTransparency = 0.15
        toast.BorderSizePixel = 0
        toast.Parent = notifHolder
        
        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 10)
        corner.Parent = toast
        
        local stroke = Instance.new("UIStroke")
        stroke.Color = COLORS.cyan
        stroke.Thickness = 1.5
        stroke.Parent = toast
        
        local tTitle = Instance.new("TextLabel")
        tTitle.Size = UDim2.new(1, -20, 0, 22)
        tTitle.Position = UDim2.new(0, 10, 0, 6)
        tTitle.BackgroundTransparency = 1
        tTitle.Text = title
        tTitle.TextColor3 = COLORS.cyan
        tTitle.Font = Enum.Font.GothamBold
        tTitle.TextSize = 13
        tTitle.TextXAlignment = Enum.TextXAlignment.Left
        tTitle.Parent = toast
        
        local tDesc = Instance.new("TextLabel")
        tDesc.Size = UDim2.new(1, -20, 0, 32)
        tDesc.Position = UDim2.new(0, 10, 0, 26)
        tDesc.BackgroundTransparency = 1
        tDesc.Text = content
        tDesc.TextColor3 = COLORS.text
        tDesc.Font = Enum.Font.Gotham
        tDesc.TextSize = 11
        tDesc.TextWrapped = true
        tDesc.TextXAlignment = Enum.TextXAlignment.Left
        tDesc.Parent = toast
        
        playClickSound()
        TweenService:Create(toast, TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), { Position = UDim2.new(1, -320, 1, -85) }):Play()
        task.delay(duration, function()
            if toast and toast.Parent then
                local tw = TweenService:Create(toast, TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.In), { Position = UDim2.new(1, 20, 1, -85) })
                tw:Play()
                tw.Completed:Connect(function() toast:Destroy() end)
            end
        end)
    end)
end

function ObsidianGlassEngine:CreateWindow(cfg)
    cfg = cfg or {}
    local parentGui = (typeof(gethui) == "function") and gethui() or CoreGui
    if parentGui:FindFirstChild("ObsidianGlass2_UI") then
        parentGui.ObsidianGlass2_UI:Destroy()
    end

    local gui = Instance.new("ScreenGui")
    gui.Name = "ObsidianGlass2_UI"
    gui.ResetOnSpawn = false
    gui.IgnoreGuiInset = true
    gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    gui.DisplayOrder = 99999
    gui.Parent = parentGui

    local uiScale = Instance.new("UIScale")
    local camera = workspace.CurrentCamera
    local function updateScale()
        if camera and camera.ViewportSize then
            local vp = camera.ViewportSize
            local targetWidth, targetHeight = 920, 600
            local scaleX = (vp.X - 24) / targetWidth
            local scaleY = (vp.Y - 24) / targetHeight
            local calcScale = math.clamp(math.min(scaleX, scaleY), 0.45, 1.0)
            uiScale.Scale = calcScale
        end
    end
    updateScale()
    if camera then
        camera:GetPropertyChangedSignal("ViewportSize"):Connect(updateScale)
    end
    uiScale.Parent = gui

    local shell = Instance.new("Frame")
    shell.Name = "MainShell"
    shell.Size = UDim2.fromOffset(920, 600)
    shell.AnchorPoint = Vector2.new(0.5, 0.5)
    shell.Position = UDim2.new(0.5, 0, 0.5, 0)
    shell.BackgroundColor3 = COLORS.shell
    shell.BackgroundTransparency = 0.20
    shell.BorderSizePixel = 0
    shell.ClipsDescendants = true
    shell.Parent = gui

    local shellCorner = Instance.new("UICorner")
    shellCorner.CornerRadius = UDim.new(0, 18)
    shellCorner.Parent = shell

    local shellStroke = Instance.new("UIStroke")
    shellStroke.Color = COLORS.cyan
    shellStroke.Thickness = 1.5
    shellStroke.Transparency = 0.3
    shellStroke.Parent = shell

    local snowLayer = Instance.new("Frame")
    snowLayer.Name = "SnowLayer"
    snowLayer.Size = UDim2.fromScale(1, 1)
    snowLayer.BackgroundTransparency = 1
    snowLayer.ZIndex = 2
    snowLayer.Parent = shell

    task.spawn(function()
        local dots = {}
        for i = 1, 35 do
            local dot = Instance.new("Frame")
            dot.Size = UDim2.fromOffset(math.random(2, 4), math.random(2, 4))
            dot.Position = UDim2.new(math.random(), 0, math.random(), 0)
            dot.BackgroundColor3 = Color3.fromRGB(220, 240, 255)
            dot.BackgroundTransparency = math.random(30, 70) / 100
            dot.BorderSizePixel = 0
            dot.Parent = snowLayer

            local dCorner = Instance.new("UICorner")
            dCorner.CornerRadius = UDim.new(1, 0)
            dCorner.Parent = dot

            dots[#dots + 1] = {
                frame = dot,
                speed = math.random(15, 40) / 10000,
                drift = math.random(-10, 10) / 10000,
                pos = dot.Position.Y.Scale
            }
        end

        while task.wait(0.03) do
            if not gui or not gui.Parent then break end
            for _, data in ipairs(dots) do
                data.pos = data.pos + data.speed
                if data.pos > 1.05 then data.pos = -0.05 end
                local newX = (data.frame.Position.X.Scale + data.drift) % 1.0
                data.frame.Position = UDim2.new(newX, 0, data.pos, 0)
            end
        end
    end)

    local toggleCapsule = Instance.new("Frame")
    toggleCapsule.Name = "ObsidianToggleCapsule"
    toggleCapsule.Size = UDim2.fromOffset(230, 58)
    toggleCapsule.Position = UDim2.new(0, 15, 0.5, -29)
    toggleCapsule.BackgroundColor3 = COLORS.shell
    toggleCapsule.BackgroundTransparency = 0.18
    toggleCapsule.BorderSizePixel = 0
    toggleCapsule.ClipsDescendants = true
    toggleCapsule.ZIndex = 99999
    toggleCapsule.Parent = gui

    local tcCorner = Instance.new("UICorner")
    tcCorner.CornerRadius = UDim.new(0, 16)
    tcCorner.Parent = toggleCapsule

    local tcStroke = Instance.new("UIStroke")
    tcStroke.Color = COLORS.cyan
    tcStroke.Thickness = 1.5
    tcStroke.Transparency = 0.2
    tcStroke.Parent = toggleCapsule

    local capsuleSnowLayer = Instance.new("Frame")
    capsuleSnowLayer.Name = "CapsuleSnowLayer"
    capsuleSnowLayer.Size = UDim2.fromScale(1, 1)
    capsuleSnowLayer.BackgroundTransparency = 1
    capsuleSnowLayer.ZIndex = 1
    capsuleSnowLayer.Parent = toggleCapsule

    task.spawn(function()
        local dots = {}
        for i = 1, 12 do
            local dot = Instance.new("Frame")
            dot.Size = UDim2.fromOffset(math.random(2, 3), math.random(2, 3))
            dot.Position = UDim2.new(math.random(), 0, math.random(), 0)
            dot.BackgroundColor3 = Color3.fromRGB(220, 240, 255)
            dot.BackgroundTransparency = math.random(30, 70) / 100
            dot.BorderSizePixel = 0
            dot.ZIndex = 1
            dot.Parent = capsuleSnowLayer

            local dCorner = Instance.new("UICorner")
            dCorner.CornerRadius = UDim.new(1, 0)
            dCorner.Parent = dot

            dots[#dots + 1] = {
                frame = dot,
                speed = math.random(15, 35) / 10000,
                drift = math.random(-10, 10) / 10000,
                pos = dot.Position.Y.Scale
            }
        end

        while task.wait(0.03) do
            if not gui or not gui.Parent or not toggleCapsule or not toggleCapsule.Parent then break end
            for _, data in ipairs(dots) do
                data.pos = data.pos + data.speed
                if data.pos > 1.05 then data.pos = -0.05 end
                local newX = (data.frame.Position.X.Scale + data.drift) % 1.0
                data.frame.Position = UDim2.new(newX, 0, data.pos, 0)
            end
        end
    end)

    local capAvatarFrame = Instance.new("Frame")
    capAvatarFrame.Size = UDim2.fromOffset(42, 42)
    capAvatarFrame.Position = UDim2.new(0, 8, 0.5, -21)
    capAvatarFrame.BackgroundColor3 = COLORS.glassDeep
    capAvatarFrame.BorderSizePixel = 0
    capAvatarFrame.ZIndex = 3
    capAvatarFrame.Parent = toggleCapsule

    local caCorner = Instance.new("UICorner")
    caCorner.CornerRadius = UDim.new(1, 0)
    caCorner.Parent = capAvatarFrame

    local caStroke = Instance.new("UIStroke")
    caStroke.Color = COLORS.primary
    caStroke.Thickness = 1.5
    caStroke.Parent = capAvatarFrame

    local capAvatarImg = Instance.new("ImageLabel")
    capAvatarImg.Size = UDim2.fromScale(1, 1)
    capAvatarImg.BackgroundTransparency = 1
    capAvatarImg.Image = loadCustomAvatarImage()
    capAvatarImg.ZIndex = 4
    capAvatarImg.Parent = capAvatarFrame

    local caiCorner = Instance.new("UICorner")
    caiCorner.CornerRadius = UDim.new(1, 0)
    caiCorner.Parent = capAvatarImg

    local playerNameStr = LocalPlayer and LocalPlayer.Name or "Player"
    local playerDispStr = LocalPlayer and LocalPlayer.DisplayName or "Player"

    local capUserLabel = Instance.new("TextLabel")
    capUserLabel.Size = UDim2.new(1, -58, 0, 18)
    capUserLabel.Position = UDim2.new(0, 56, 0, 10)
    capUserLabel.BackgroundTransparency = 1
    capUserLabel.Text = "@" .. playerNameStr
    capUserLabel.TextColor3 = COLORS.text
    capUserLabel.Font = Enum.Font.GothamBold
    capUserLabel.TextSize = 12
    capUserLabel.TextXAlignment = Enum.TextXAlignment.Left
    capUserLabel.ZIndex = 3
    capUserLabel.Parent = toggleCapsule

    local capMetricsLabel = Instance.new("TextLabel")
    capMetricsLabel.Size = UDim2.new(1, -58, 0, 16)
    capMetricsLabel.Position = UDim2.new(0, 56, 0, 28)
    capMetricsLabel.BackgroundTransparency = 1
    capMetricsLabel.Text = "⚡ 60 FPS  •  📡 0 ms"
    capMetricsLabel.TextColor3 = COLORS.cyan
    capMetricsLabel.Font = Enum.Font.GothamBold
    capMetricsLabel.TextSize = 10
    capMetricsLabel.TextXAlignment = Enum.TextXAlignment.Left
    capMetricsLabel.ZIndex = 3
    capMetricsLabel.Parent = toggleCapsule

    task.spawn(function()
        local frameCount = 0
        local lastFpsTime = tick()
        local fpsVal = 60

        local renderConn
        renderConn = RunService.RenderStepped:Connect(function()
            frameCount = frameCount + 1
            local now = tick()
            if now - lastFpsTime >= 1 then
                fpsVal = frameCount
                frameCount = 0
                lastFpsTime = now
            end
        end)

        while task.wait(0.8) do
            if not gui or not gui.Parent or not toggleCapsule or not toggleCapsule.Parent then
                if renderConn then renderConn:Disconnect() end
                break
            end
            local pingVal = 0
            pcall(function() pingVal = math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue()) end)
            capMetricsLabel.Text = string.format("⚡ %d FPS  •  📡 %d ms", fpsVal, pingVal)
        end
    end)

    local capBtn = Instance.new("TextButton")
    capBtn.Size = UDim2.fromScale(1, 1)
    capBtn.BackgroundTransparency = 1
    capBtn.Text = ""
    capBtn.ZIndex = 10
    capBtn.Parent = toggleCapsule

    local tDragging, tDragInput, tDragStart, tStartPos
    local hasDragged = false

    capBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            tDragging = true
            hasDragged = false
            tDragStart = input.Position
            tStartPos = toggleCapsule.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    tDragging = false
                end
            end)
        end
    end)

    capBtn.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            tDragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == tDragInput and tDragging then
            local delta = input.Position - tDragStart
            if math.abs(delta.X) > 3 or math.abs(delta.Y) > 3 then
                hasDragged = true
            end
            toggleCapsule.Position = UDim2.new(tStartPos.X.Scale, tStartPos.X.Offset + delta.X, tStartPos.Y.Scale, tStartPos.Y.Offset + delta.Y)
        end
    end)

    capBtn.MouseButton1Click:Connect(function()
        if not hasDragged then
            playClickSound()
            shell.Visible = not shell.Visible
        end
    end)

    local userPanel = Instance.new("Frame")
    userPanel.Name = "UserPanel"
    userPanel.Size = UDim2.new(0, 240, 1, 0)
    userPanel.BackgroundColor3 = COLORS.userPanel
    userPanel.BackgroundTransparency = 0.20
    userPanel.BorderSizePixel = 0
    userPanel.ZIndex = 5
    userPanel.Parent = shell

    local userDiv = Instance.new("Frame")
    userDiv.Size = UDim2.new(0, 1, 1, 0)
    userDiv.Position = UDim2.new(1, -1, 0, 0)
    userDiv.BackgroundColor3 = COLORS.glassRaised
    userDiv.BorderSizePixel = 0
    userDiv.ZIndex = 10
    userDiv.Parent = userPanel

    local avatarFrame = Instance.new("Frame")
    avatarFrame.Size = UDim2.fromOffset(44, 44)
    avatarFrame.Position = UDim2.new(0, 14, 0, 14)
    avatarFrame.BackgroundColor3 = COLORS.glassDeep
    avatarFrame.BorderSizePixel = 0
    avatarFrame.ZIndex = 10
    avatarFrame.Parent = userPanel

    local avCorner = Instance.new("UICorner")
    avCorner.CornerRadius = UDim.new(1, 0)
    avCorner.Parent = avatarFrame

    local avStroke = Instance.new("UIStroke")
    avStroke.Color = COLORS.cyan
    avStroke.Thickness = 1.5
    avStroke.Parent = avatarFrame

    local avatarImg = Instance.new("ImageLabel")
    avatarImg.Size = UDim2.fromScale(1, 1)
    avatarImg.BackgroundTransparency = 1
    avatarImg.Image = loadCustomAvatarImage()
    avatarImg.ZIndex = 11
    avatarImg.Parent = avatarFrame

    local avImgCorner = Instance.new("UICorner")
    avImgCorner.CornerRadius = UDim.new(1, 0)
    avImgCorner.Parent = avatarImg

    local onlineDot = Instance.new("Frame")
    onlineDot.Size = UDim2.fromOffset(10, 10)
    onlineDot.Position = UDim2.new(1, -8, 1, -8)
    onlineDot.BackgroundColor3 = COLORS.success
    onlineDot.BorderSizePixel = 0
    onlineDot.ZIndex = 12
    onlineDot.Parent = avatarFrame

    local onlineCorner = Instance.new("UICorner")
    onlineCorner.CornerRadius = UDim.new(1, 0)
    onlineCorner.Parent = onlineDot

    local displayNameLabel = Instance.new("TextLabel")
    displayNameLabel.Size = UDim2.new(1, -75, 0, 18)
    displayNameLabel.Position = UDim2.new(0, 66, 0, 15)
    displayNameLabel.BackgroundTransparency = 1
    displayNameLabel.Text = playerDispStr
    displayNameLabel.TextColor3 = COLORS.text
    displayNameLabel.Font = Enum.Font.GothamBold
    displayNameLabel.TextSize = 13
    displayNameLabel.TextXAlignment = Enum.TextXAlignment.Left
    displayNameLabel.ZIndex = 10
    displayNameLabel.Parent = userPanel

    local usernameLabel = Instance.new("TextLabel")
    usernameLabel.Size = UDim2.new(1, -75, 0, 14)
    usernameLabel.Position = UDim2.new(0, 66, 0, 33)
    usernameLabel.BackgroundTransparency = 1
    usernameLabel.Text = "@" .. playerNameStr
    usernameLabel.TextColor3 = COLORS.textMuted
    usernameLabel.Font = Enum.Font.Gotham
    usernameLabel.TextSize = 10
    usernameLabel.TextXAlignment = Enum.TextXAlignment.Left
    usernameLabel.ZIndex = 10
    usernameLabel.Parent = userPanel

    local metricsBox = Instance.new("Frame")
    metricsBox.Size = UDim2.new(1, -28, 0, 24)
    metricsBox.Position = UDim2.new(0, 14, 0, 64)
    metricsBox.BackgroundColor3 = COLORS.glassDeep
    metricsBox.BorderSizePixel = 0
    metricsBox.ZIndex = 10
    metricsBox.Parent = userPanel

    local mCorner = Instance.new("UICorner")
    mCorner.CornerRadius = UDim.new(0, 6)
    mCorner.Parent = metricsBox

    local metricsLabel = Instance.new("TextLabel")
    metricsLabel.Size = UDim2.fromScale(1, 1)
    metricsLabel.BackgroundTransparency = 1
    metricsLabel.Text = "⏱️ 00:00  •  📡 0 ms"
    metricsLabel.TextColor3 = COLORS.cyan
    metricsLabel.Font = Enum.Font.GothamBold
    metricsLabel.TextSize = 10
    metricsLabel.ZIndex = 11
    metricsLabel.Parent = metricsBox

    task.spawn(function()
        local startTime = os.time()
        while task.wait(1) do
            if not gui or not gui.Parent then break end
            local elapsed = os.time() - startTime
            local mins = math.floor(elapsed / 60)
            local secs = elapsed % 60
            local ping = 0
            pcall(function() ping = math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue()) end)
            metricsLabel.Text = string.format("⏱️ %02d:%02d  •  📡 %d ms", mins, secs, ping)
        end
    end)

    local sideDiv = Instance.new("Frame")
    sideDiv.Size = UDim2.new(1, -28, 0, 1)
    sideDiv.Position = UDim2.new(0, 14, 0, 96)
    sideDiv.BackgroundColor3 = COLORS.glassRaised
    sideDiv.BorderSizePixel = 0
    sideDiv.ZIndex = 10
    sideDiv.Parent = userPanel

    local navHeader = Instance.new("TextLabel")
    navHeader.Size = UDim2.new(1, -28, 0, 18)
    navHeader.Position = UDim2.new(0, 16, 0, 104)
    navHeader.BackgroundTransparency = 1
    navHeader.Text = "SYSTEM MODULES / หมวดหมู่"
    navHeader.TextColor3 = Color3.fromRGB(255, 255, 255)
    navHeader.Font = Enum.Font.GothamBold
    navHeader.TextSize = 11
    navHeader.TextXAlignment = Enum.TextXAlignment.Left
    navHeader.ZIndex = 10
    navHeader.Parent = userPanel

    local tabScroll = Instance.new("ScrollingFrame")
    tabScroll.Name = "VerticalTabScroll"
    tabScroll.Size = UDim2.new(1, -20, 1, -178)
    tabScroll.Position = UDim2.new(0, 10, 0, 126)
    tabScroll.BackgroundTransparency = 1
    tabScroll.ScrollBarThickness = 3
    tabScroll.ScrollBarImageColor3 = COLORS.cyan
    tabScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    tabScroll.ZIndex = 10
    tabScroll.Parent = userPanel

    local tabLayout = Instance.new("UIListLayout")
    tabLayout.FillDirection = Enum.FillDirection.Vertical
    tabLayout.Padding = UDim.new(0, 5)
    tabLayout.SortOrder = Enum.SortOrder.LayoutOrder
    tabLayout.Parent = tabScroll

    tabLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        tabScroll.CanvasSize = UDim2.new(0, 0, 0, tabLayout.AbsoluteContentSize.Y + 10)
    end)

    local statusCard = Instance.new("Frame")
    statusCard.Size = UDim2.new(1, -24, 0, 36)
    statusCard.Position = UDim2.new(0, 12, 1, -44)
    statusCard.BackgroundColor3 = COLORS.glassDeep
    statusCard.BorderSizePixel = 0
    statusCard.ZIndex = 10
    statusCard.Parent = userPanel

    local stCorner = Instance.new("UICorner")
    stCorner.CornerRadius = UDim.new(0, 8)
    stCorner.Parent = statusCard

    local stTitle = Instance.new("TextLabel")
    stTitle.Size = UDim2.fromScale(1, 1)
    stTitle.BackgroundTransparency = 1
    stTitle.Text = "✅ Connected & Active"
    stTitle.TextColor3 = COLORS.success
    stTitle.Font = Enum.Font.GothamBold
    stTitle.TextSize = 12
    stTitle.Parent = statusCard

    local mainPanel = Instance.new("Frame")
    mainPanel.Name = "MainPanel"
    mainPanel.Size = UDim2.new(1, -240, 1, 0)
    mainPanel.Position = UDim2.new(0, 240, 0, 0)
    mainPanel.BackgroundTransparency = 1
    mainPanel.ZIndex = 5
    mainPanel.Parent = shell

    local headerBar = Instance.new("Frame")
    headerBar.Size = UDim2.new(1, 0, 0, 48)
    headerBar.BackgroundTransparency = 1
    headerBar.Parent = mainPanel

    local mainTitle = Instance.new("TextLabel")
    mainTitle.Size = UDim2.new(0, 300, 0, 22)
    mainTitle.Position = UDim2.new(0, 20, 0, 8)
    mainTitle.BackgroundTransparency = 1
    mainTitle.Text = cfg.Title or "PayomboyZ HUB"
    mainTitle.TextColor3 = COLORS.text
    mainTitle.Font = Enum.Font.GothamBold
    mainTitle.TextSize = 18
    mainTitle.TextXAlignment = Enum.TextXAlignment.Left
    mainTitle.Parent = headerBar

    local mainSubTitle = Instance.new("TextLabel")
    mainSubTitle.Size = UDim2.new(0, 350, 0, 16)
    mainSubTitle.Position = UDim2.new(0, 20, 0, 28)
    mainSubTitle.BackgroundTransparency = 1
    mainSubTitle.Text = cfg.SubTitle or cfg.Author or "Obsidian Glassmorphic 2 Engine"
    mainSubTitle.TextColor3 = COLORS.textMuted
    mainSubTitle.Font = Enum.Font.Gotham
    mainSubTitle.TextSize = 11
    mainSubTitle.TextXAlignment = Enum.TextXAlignment.Left
    mainSubTitle.Parent = headerBar

    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.fromOffset(28, 28)
    closeBtn.Position = UDim2.new(1, -38, 0, 10)
    closeBtn.BackgroundColor3 = COLORS.glass
    closeBtn.BackgroundTransparency = 0.20
    closeBtn.Text = "X"
    closeBtn.TextColor3 = COLORS.textMuted
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.TextSize = 14
    closeBtn.Parent = headerBar

    local closeCorner = Instance.new("UICorner")
    closeCorner.CornerRadius = UDim.new(0, 8)
    closeCorner.Parent = closeBtn

    closeBtn.MouseButton1Click:Connect(function()
        playClickSound()
        shell.Visible = false
    end)

    local minBtn = Instance.new("TextButton")
    minBtn.Size = UDim2.fromOffset(28, 28)
    minBtn.Position = UDim2.new(1, -72, 0, 10)
    minBtn.BackgroundColor3 = COLORS.glass
    minBtn.BackgroundTransparency = 0.20
    minBtn.Text = "─"
    minBtn.TextColor3 = COLORS.textMuted
    minBtn.Font = Enum.Font.GothamBold
    minBtn.TextSize = 13
    minBtn.Parent = headerBar

    local minCorner = Instance.new("UICorner")
    minCorner.CornerRadius = UDim.new(0, 8)
    minCorner.Parent = minBtn

    minBtn.MouseButton1Click:Connect(function()
        playClickSound()
        shell.Visible = false
    end)

    local dragging, dragInput, dragStart, startPos
    headerBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = shell.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)

    headerBar.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            shell.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)

    local serviceBanner = Instance.new("Frame")
    serviceBanner.Size = UDim2.new(1, -40, 0, 65)
    serviceBanner.Position = UDim2.new(0, 20, 0, 48)
    serviceBanner.BackgroundColor3 = COLORS.glassDeep
    serviceBanner.BackgroundTransparency = 0.18
    serviceBanner.BorderSizePixel = 0
    serviceBanner.Parent = mainPanel

    local sCorner = Instance.new("UICorner")
    sCorner.CornerRadius = UDim.new(0, 10)
    sCorner.Parent = serviceBanner

    local sStroke = Instance.new("UIStroke")
    sStroke.Color = COLORS.glassRaised
    sStroke.Thickness = 1
    sStroke.Parent = serviceBanner

    local sBadge = Instance.new("TextLabel")
    sBadge.Size = UDim2.new(0, 120, 0, 16)
    sBadge.Position = UDim2.new(0, 14, 0, 10)
    sBadge.BackgroundTransparency = 1
    sBadge.Text = "ACTIVE SERVICE"
    sBadge.TextColor3 = COLORS.success
    sBadge.Font = Enum.Font.GothamBold
    sBadge.TextSize = 10
    sBadge.TextXAlignment = Enum.TextXAlignment.Left
    sBadge.Parent = serviceBanner

    local sTitle = Instance.new("TextLabel")
    sTitle.Size = UDim2.new(0, 250, 0, 20)
    sTitle.Position = UDim2.new(0, 14, 0, 26)
    sTitle.BackgroundTransparency = 1
    sTitle.Text = cfg.Title or "PayomboyZ Studios"
    sTitle.TextColor3 = COLORS.text
    sTitle.Font = Enum.Font.GothamBold
    sTitle.TextSize = 14
    sTitle.TextXAlignment = Enum.TextXAlignment.Left
    sTitle.Parent = serviceBanner

    local sSub = Instance.new("TextLabel")
    sSub.Size = UDim2.new(0, 250, 0, 14)
    sSub.Position = UDim2.new(0, 14, 0, 44)
    sSub.BackgroundTransparency = 1
    sSub.Text = "Verified client delivery • Premium Automation"
    sSub.TextColor3 = COLORS.textMuted
    sSub.Font = Enum.Font.Gotham
    sSub.TextSize = 10
    sSub.TextXAlignment = Enum.TextXAlignment.Left
    sSub.Parent = serviceBanner

    local vBadge = Instance.new("Frame")
    vBadge.Size = UDim2.fromOffset(150, 32)
    vBadge.Position = UDim2.new(1, -160, 0.5, -16)
    vBadge.BackgroundColor3 = COLORS.userPanel
    vBadge.BackgroundTransparency = 0.20
    vBadge.BorderSizePixel = 0
    vBadge.Parent = serviceBanner

    local vCorner = Instance.new("UICorner")
    vCorner.CornerRadius = UDim.new(0, 8)
    vCorner.Parent = vBadge

    local vStroke = Instance.new("UIStroke")
    vStroke.Color = COLORS.success
    vStroke.Thickness = 1
    vStroke.Parent = vBadge

    local vText = Instance.new("TextLabel")
    vText.Size = UDim2.fromScale(1, 1)
    vText.BackgroundTransparency = 1
    vText.Text = "🛡️ SYSTEM VERIFIED"
    vText.TextColor3 = COLORS.success
    vText.Font = Enum.Font.GothamBold
    vText.TextSize = 10
    vText.Parent = vBadge

    local pagesFolder = Instance.new("Frame")
    pagesFolder.Name = "PagesFolder"
    pagesFolder.Size = UDim2.new(1, -40, 1, -128)
    pagesFolder.Position = UDim2.new(0, 20, 0, 120)
    pagesFolder.BackgroundTransparency = 1
    pagesFolder.Parent = mainPanel

    local WindowObj = { Tabs = {}, Closed = false }

    function WindowObj:Close()
        shell.Visible = false
        self.Closed = true
    end

    function WindowObj:Open()
        shell.Visible = true
        self.Closed = false
    end

    function WindowObj:Toggle()
        shell.Visible = not shell.Visible
        self.Closed = not shell.Visible
    end

    function WindowObj:AddTab(tabCfg)
        local tabTitle = (type(tabCfg) == "table" and (tabCfg.Title or tabCfg.Name)) or tostring(tabCfg or "Tab")
        local tabIndex = #WindowObj.Tabs + 1

        local tabBtn = Instance.new("TextButton")
        tabBtn.Size = UDim2.new(1, -6, 0, 40)
        tabBtn.Position = UDim2.new(0, 3, 0, 0)
        tabBtn.BackgroundColor3 = (tabIndex == 1) and COLORS.primary or Color3.fromRGB(38, 16, 24)
        tabBtn.BackgroundTransparency = (tabIndex == 1) and 0.15 or 0.22
        tabBtn.Text = "    " .. tabTitle
        tabBtn.TextColor3 = (tabIndex == 1) and Color3.fromRGB(255, 255, 255) or COLORS.textMuted
        tabBtn.Font = Enum.Font.GothamBold
        tabBtn.TextSize = 14
        tabBtn.TextXAlignment = Enum.TextXAlignment.Left
        tabBtn.AutoButtonColor = false
        tabBtn.ZIndex = 12
        tabBtn.Parent = tabScroll

        local tbCorner = Instance.new("UICorner")
        tbCorner.CornerRadius = UDim.new(0, 8)
        tbCorner.Parent = tabBtn

        local tbStroke = Instance.new("UIStroke")
        tbStroke.Color = (tabIndex == 1) and COLORS.primary or Color3.fromRGB(70, 30, 45)
        tbStroke.Thickness = 1
        tbStroke.Transparency = (tabIndex == 1) and 0 or 0.3
        tbStroke.Parent = tabBtn

        local activeIndicator = Instance.new("Frame")
        activeIndicator.Size = UDim2.new(0, 4, 0, 20)
        activeIndicator.Position = UDim2.new(0, 4, 0.5, -10)
        activeIndicator.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        activeIndicator.BorderSizePixel = 0
        activeIndicator.Visible = (tabIndex == 1)
        activeIndicator.Parent = tabBtn

        local indCorner = Instance.new("UICorner")
        indCorner.CornerRadius = UDim.new(1, 0)
        indCorner.Parent = activeIndicator

        local pageScroll = Instance.new("ScrollingFrame")
        pageScroll.Name = "Page_" .. tabTitle
        pageScroll.Size = UDim2.fromScale(1, 1)
        pageScroll.BackgroundTransparency = 1
        pageScroll.ScrollBarThickness = 4
        pageScroll.ScrollBarImageColor3 = COLORS.primary
        pageScroll.Visible = (tabIndex == 1)
        pageScroll.Parent = pagesFolder

        local pageLayout = Instance.new("UIListLayout")
        pageLayout.Padding = UDim.new(0, 8)
        pageLayout.SortOrder = Enum.SortOrder.LayoutOrder
        pageLayout.Parent = pageScroll

        pageLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            pageScroll.CanvasSize = UDim2.new(0, 0, 0, pageLayout.AbsoluteContentSize.Y + 20)
        end)

        local function activateTab()
            playClickSound()
            for _, t in ipairs(WindowObj.Tabs) do
                t.btn.BackgroundColor3 = Color3.fromRGB(38, 16, 24)
                t.btn.BackgroundTransparency = 0.22
                t.btn.TextColor3 = COLORS.textMuted
                t.stroke.Color = Color3.fromRGB(70, 30, 45)
                t.stroke.Transparency = 0.3
                if t.indicator then t.indicator.Visible = false end
                t.page.Visible = false
            end
            tabBtn.BackgroundColor3 = COLORS.primary
            tabBtn.BackgroundTransparency = 0.15
            tabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            tbStroke.Color = COLORS.primary
            tbStroke.Transparency = 0
            activeIndicator.Visible = true
            pageScroll.Visible = true
        end

        tabBtn.MouseButton1Click:Connect(activateTab)

        local TabObj = {
            btn = tabBtn,
            stroke = tbStroke,
            indicator = activeIndicator,
            page = pageScroll,
            Title = tabTitle,
            Select = activateTab
        }

        function TabObj:AddToggle(id, tCfg)
            if type(id) == "table" then
                tCfg = id
                id = tCfg.Title or "Toggle"
            end
            tCfg = tCfg or {}
            local title = tCfg.Title or id
            local desc = tCfg.Desc or ""
            local defaultVal = (tCfg.Default ~= nil and tCfg.Default) or (tCfg.Value ~= nil and tCfg.Value) or false

            local frame = Instance.new("Frame")
            frame.Size = UDim2.new(1, -10, 0, desc ~= "" and 55 or 44)
            frame.BackgroundColor3 = COLORS.glassDeep
            frame.BackgroundTransparency = 0.18
            frame.BorderSizePixel = 0
            frame.Parent = pageScroll

            local fCorner = Instance.new("UICorner")
            fCorner.CornerRadius = UDim.new(0, 8)
            fCorner.Parent = frame

            local fStroke = Instance.new("UIStroke")
            fStroke.Color = COLORS.surface
            fStroke.Thickness = 1
            fStroke.Parent = frame

            local lbl = Instance.new("TextLabel")
            lbl.Size = UDim2.new(1, -70, 0, 22)
            lbl.Position = UDim2.new(0, 12, 0, desc ~= "" and 8 or 11)
            lbl.BackgroundTransparency = 1
            lbl.Text = title
            lbl.TextColor3 = COLORS.text
            lbl.Font = Enum.Font.GothamBold
            lbl.TextSize = 14
            lbl.TextXAlignment = Enum.TextXAlignment.Left
            lbl.Parent = frame

            if desc ~= "" then
                local dLbl = Instance.new("TextLabel")
                dLbl.Size = UDim2.new(1, -70, 0, 18)
                dLbl.Position = UDim2.new(0, 12, 0, 30)
                dLbl.BackgroundTransparency = 1
                dLbl.Text = desc
                dLbl.TextColor3 = COLORS.textMuted
                dLbl.Font = Enum.Font.Gotham
                dLbl.TextSize = 11
                dLbl.TextXAlignment = Enum.TextXAlignment.Left
                dLbl.Parent = frame
            end

            local switch = Instance.new("TextButton")
            switch.Size = UDim2.fromOffset(46, 24)
            switch.Position = UDim2.new(1, -56, 0.5, -12)
            switch.BackgroundColor3 = defaultVal and COLORS.cyan or COLORS.surface
            switch.Text = ""
            switch.Parent = frame

            local swCorner = Instance.new("UICorner")
            swCorner.CornerRadius = UDim.new(1, 0)
            swCorner.Parent = switch

            local knob = Instance.new("Frame")
            knob.Size = UDim2.fromOffset(20, 20)
            knob.Position = defaultVal and UDim2.new(1, -22, 0.5, -10) or UDim2.new(0, 2, 0.5, -10)
            knob.BackgroundColor3 = COLORS.text
            knob.BorderSizePixel = 0
            knob.Parent = switch

            local kCorner = Instance.new("UICorner")
            kCorner.CornerRadius = UDim.new(1, 0)
            kCorner.Parent = knob

            local OptionObj = {
                Value = defaultVal,
                Callback = tCfg.Callback or function() end,
                ChangedCallbacks = {}
            }

            local function updateToggle(val)
                playClickSound()
                OptionObj.Value = val
                switch.BackgroundColor3 = val and COLORS.cyan or COLORS.surface
                knob.Position = val and UDim2.new(1, -22, 0.5, -10) or UDim2.new(0, 2, 0.5, -10)
                pcall(function() OptionObj.Callback(val) end)
                for _, cb in ipairs(OptionObj.ChangedCallbacks) do pcall(function() cb(val) end) end
            end

            function OptionObj:OnChanged(cb) table.insert(OptionObj.ChangedCallbacks, cb) end
            function OptionObj:SetValue(val) updateToggle(val == true) end
            function OptionObj:Set(val) updateToggle(val == true) end
            function OptionObj:UpdateValue(val) updateToggle(val == true) end

            switch.MouseButton1Click:Connect(function() updateToggle(not OptionObj.Value) end)
            ObsidianGlassEngine.Options[id] = OptionObj
            return OptionObj
        end

        function TabObj:AddSlider(id, sCfg)
            if type(id) == "table" then
                sCfg = id
                id = sCfg.Title or "Slider"
            end
            sCfg = sCfg or {}
            local title = sCfg.Title or id
            local minVal = sCfg.Min or (type(sCfg.Value) == "table" and sCfg.Value.Min) or 0
            local maxVal = sCfg.Max or (type(sCfg.Value) == "table" and sCfg.Value.Max) or 100
            local defaultVal = sCfg.Default or (type(sCfg.Value) == "table" and sCfg.Value.Default) or minVal

            local frame = Instance.new("Frame")
            frame.Size = UDim2.new(1, -10, 0, 52)
            frame.BackgroundColor3 = COLORS.glassDeep
            frame.BackgroundTransparency = 0.18
            frame.BorderSizePixel = 0
            frame.Parent = pageScroll

            local fCorner = Instance.new("UICorner")
            fCorner.CornerRadius = UDim.new(0, 8)
            fCorner.Parent = frame

            local fStroke = Instance.new("UIStroke")
            fStroke.Color = COLORS.surface
            fStroke.Thickness = 1
            fStroke.Parent = frame

            local lbl = Instance.new("TextLabel")
            lbl.Size = UDim2.new(0.7, 0, 0, 22)
            lbl.Position = UDim2.new(0, 12, 0, 6)
            lbl.BackgroundTransparency = 1
            lbl.Text = title
            lbl.TextColor3 = COLORS.text
            lbl.Font = Enum.Font.GothamBold
            lbl.TextSize = 14
            lbl.TextXAlignment = Enum.TextXAlignment.Left
            lbl.Parent = frame

            local valLbl = Instance.new("TextLabel")
            valLbl.Size = UDim2.new(0.3, -12, 0, 22)
            valLbl.Position = UDim2.new(0.7, 0, 0, 6)
            valLbl.BackgroundTransparency = 1
            valLbl.Text = tostring(defaultVal)
            valLbl.TextColor3 = COLORS.cyan
            valLbl.Font = Enum.Font.GothamBold
            valLbl.TextSize = 14
            valLbl.TextXAlignment = Enum.TextXAlignment.Right
            valLbl.Parent = frame

            local bar = Instance.new("TextButton")
            bar.Size = UDim2.new(1, -24, 0, 8)
            bar.Position = UDim2.new(0, 12, 0, 34)
            bar.BackgroundColor3 = COLORS.surface
            bar.Text = ""
            bar.Parent = frame

            local bCorner = Instance.new("UICorner")
            bCorner.CornerRadius = UDim.new(1, 0)
            bCorner.Parent = bar

            local fill = Instance.new("Frame")
            local pct = (defaultVal - minVal) / math.max(maxVal - minVal, 1)
            fill.Size = UDim2.new(pct, 0, 1, 0)
            fill.BackgroundColor3 = COLORS.cyan
            fill.BorderSizePixel = 0
            fill.Parent = bar

            local fillCorner = Instance.new("UICorner")
            fillCorner.CornerRadius = UDim.new(1, 0)
            fillCorner.Parent = fill

            local OptionObj = {
                Value = defaultVal,
                Callback = sCfg.Callback or function() end,
                ChangedCallbacks = {}
            }

            local function updateSlider(val)
                val = math.clamp(val, minVal, maxVal)
                if sCfg.Rounding then val = math.floor(val * (10 ^ sCfg.Rounding) + 0.5) / (10 ^ sCfg.Rounding) else val = math.floor(val + 0.5) end
                OptionObj.Value = val
                valLbl.Text = tostring(val)
                local newPct = (val - minVal) / math.max(maxVal - minVal, 1)
                fill.Size = UDim2.new(newPct, 0, 1, 0)
                pcall(function() OptionObj.Callback(val) end)
                for _, cb in ipairs(OptionObj.ChangedCallbacks) do pcall(function() cb(val) end) end
            end

            function OptionObj:OnChanged(cb) table.insert(OptionObj.ChangedCallbacks, cb) end
            function OptionObj:SetValue(val) updateSlider(val) end

            local isDragging = false
            bar.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    isDragging = true
                    local relX = input.Position.X - bar.AbsolutePosition.X
                    updateSlider(minVal + (relX / bar.AbsoluteSize.X) * (maxVal - minVal))
                end
            end)

            UserInputService.InputEnded:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    isDragging = false
                end
            end)

            UserInputService.InputChanged:Connect(function(input)
                if isDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                    local relX = input.Position.X - bar.AbsolutePosition.X
                    updateSlider(minVal + (relX / bar.AbsoluteSize.X) * (maxVal - minVal))
                end
            end)

            ObsidianGlassEngine.Options[id] = OptionObj
            return OptionObj
        end

        function TabObj:AddDropdown(id, dCfg)
            if type(id) == "table" then
                dCfg = id
                id = dCfg.Title or "Dropdown"
            end
            dCfg = dCfg or {}
            local title = dCfg.Title or id
            local values = dCfg.Values or {}
            local defaultVal = dCfg.Value or dCfg.Default or (values[1] or "")
            local isMulti = dCfg.Multi == true

            local frame = Instance.new("Frame")
            frame.Size = UDim2.new(1, -10, 0, 52)
            frame.BackgroundColor3 = COLORS.glassDeep
            frame.BackgroundTransparency = 0.18
            frame.BorderSizePixel = 0
            frame.Parent = pageScroll

            local fCorner = Instance.new("UICorner")
            fCorner.CornerRadius = UDim.new(0, 8)
            fCorner.Parent = frame

            local fStroke = Instance.new("UIStroke")
            fStroke.Color = COLORS.surfaceRaised
            fStroke.Thickness = 1
            fStroke.Parent = frame

            local lbl = Instance.new("TextLabel")
            lbl.Size = UDim2.new(0.5, 0, 1, 0)
            lbl.Position = UDim2.new(0, 12, 0, 0)
            lbl.BackgroundTransparency = 1
            lbl.Text = title
            lbl.TextColor3 = COLORS.text
            lbl.Font = Enum.Font.GothamBold
            lbl.TextSize = 14
            lbl.TextXAlignment = Enum.TextXAlignment.Left
            lbl.Parent = frame

            local dBtn = Instance.new("TextButton")
            dBtn.Size = UDim2.new(0.46, 0, 0, 34)
            dBtn.Position = UDim2.new(0.52, 0, 0.5, -17)
            dBtn.BackgroundColor3 = COLORS.surface
            dBtn.BackgroundTransparency = 0.20

            local function formatValText(val)
                if type(val) == "table" then
                    if #val == 0 then return "[ กดเพื่อเลือกรายการ ]" end
                    return table.concat(val, ", ")
                end
                return tostring(val)
            end

            dBtn.Text = formatValText(defaultVal)
            dBtn.TextColor3 = COLORS.text
            dBtn.Font = Enum.Font.GothamBold
            dBtn.TextSize = 13
            dBtn.TextTruncate = Enum.TextTruncate.AtEnd
            dBtn.Parent = frame

            local dbCorner = Instance.new("UICorner")
            dbCorner.CornerRadius = UDim.new(0, 6)
            dbCorner.Parent = dBtn

            local dbStroke = Instance.new("UIStroke")
            dbStroke.Color = COLORS.cyan
            dbStroke.Thickness = 1
            dbStroke.Transparency = 0.4
            dbStroke.Parent = dBtn

            local OptionObj = {
                Value = defaultVal,
                Values = values,
                Callback = dCfg.Callback or function() end,
                ChangedCallbacks = {}
            }

            local function updateDropdown(val)
                OptionObj.Value = val
                dBtn.Text = formatValText(val)
                pcall(function() OptionObj.Callback(val) end)
                for _, cb in ipairs(OptionObj.ChangedCallbacks) do pcall(function() cb(val) end) end
            end

            function OptionObj:OnChanged(cb) table.insert(OptionObj.ChangedCallbacks, cb) end
            function OptionObj:SetValue(val) updateDropdown(val) end
            function OptionObj:SetValues(newVals) OptionObj.Values = newVals end

            dBtn.MouseButton1Click:Connect(function()
                playClickSound()
                local mainGui = shell.Parent
                if not mainGui then return end

                local existingModal = mainGui:FindFirstChild("DropdownModalOverlay")
                if existingModal then
                    local isSameDropdown = (existingModal:GetAttribute("DropdownId") == id)
                    existingModal:Destroy()
                    if isSameDropdown then return end
                end

                local modalOverlay = Instance.new("Frame")
                modalOverlay.Name = "DropdownModalOverlay"
                modalOverlay:SetAttribute("DropdownId", id)
                modalOverlay.Size = UDim2.fromScale(1, 1)
                modalOverlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
                modalOverlay.BackgroundTransparency = 0.55
                modalOverlay.ZIndex = 999999
                modalOverlay.Parent = mainGui

                local bgDismissBtn = Instance.new("TextButton")
                bgDismissBtn.Size = UDim2.fromScale(1, 1)
                bgDismissBtn.BackgroundTransparency = 1
                bgDismissBtn.Text = ""
                bgDismissBtn.ZIndex = 999999
                bgDismissBtn.Parent = modalOverlay

                local modalFrame = Instance.new("Frame")
                modalFrame.Size = UDim2.fromOffset(380, 440)
                modalFrame.AnchorPoint = Vector2.new(0.5, 0.5)
                modalFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
                modalFrame.BackgroundColor3 = COLORS.shell
                modalFrame.BackgroundTransparency = 0.15
                modalFrame.BorderSizePixel = 0
                modalFrame.ZIndex = 1000000
                modalFrame.Parent = modalOverlay

                local mCorner = Instance.new("UICorner")
                mCorner.CornerRadius = UDim.new(0, 14)
                mCorner.Parent = modalFrame

                local mStroke = Instance.new("UIStroke")
                mStroke.Color = COLORS.primary
                mStroke.Thickness = 2
                mStroke.Parent = modalFrame

                local mHeader = Instance.new("TextLabel")
                mHeader.Size = UDim2.new(1, -50, 0, 24)
                mHeader.Position = UDim2.new(0, 16, 0, 14)
                mHeader.BackgroundTransparency = 1
                mHeader.Text = "📌 " .. title
                mHeader.TextColor3 = COLORS.text
                mHeader.Font = Enum.Font.GothamBold
                mHeader.TextSize = 15
                mHeader.TextXAlignment = Enum.TextXAlignment.Left
                mHeader.ZIndex = 1000001
                mHeader.Parent = modalFrame

                local mSub = Instance.new("TextLabel")
                mSub.Size = UDim2.new(1, -50, 0, 18)
                mSub.Position = UDim2.new(0, 16, 0, 38)
                mSub.BackgroundTransparency = 1
                mSub.Text = isMulti and "คำแนะนำ: คลิกเลือก/ยกเลิกได้หลายรายการ" or "คำแนะนำ: คลิก 1 รายการเพื่อเลือก"
                mSub.TextColor3 = COLORS.textMuted
                mSub.Font = Enum.Font.Gotham
                mSub.TextSize = 11
                mSub.TextXAlignment = Enum.TextXAlignment.Left
                mSub.ZIndex = 1000001
                mSub.Parent = modalFrame

                local closeModBtn = Instance.new("TextButton")
                closeModBtn.Size = UDim2.fromOffset(26, 26)
                closeModBtn.Position = UDim2.new(1, -36, 0, 14)
                closeModBtn.BackgroundColor3 = COLORS.glass
                closeModBtn.Text = "✕"
                closeModBtn.TextColor3 = COLORS.textMuted
                closeModBtn.Font = Enum.Font.GothamBold
                closeModBtn.TextSize = 13
                closeModBtn.ZIndex = 1000002
                closeModBtn.Parent = modalFrame

                local cmCorner = Instance.new("UICorner")
                cmCorner.CornerRadius = UDim.new(0, 6)
                cmCorner.Parent = closeModBtn

                closeModBtn.MouseButton1Click:Connect(function()
                    playClickSound()
                    modalOverlay:Destroy()
                end)

                bgDismissBtn.MouseButton1Click:Connect(function() modalOverlay:Destroy() end)

                local optScroll = Instance.new("ScrollingFrame")
                optScroll.Size = UDim2.new(1, -28, 1, -124)
                optScroll.Position = UDim2.new(0, 14, 0, 64)
                optScroll.BackgroundTransparency = 1
                optScroll.ScrollBarThickness = 4
                optScroll.ScrollBarImageColor3 = COLORS.primary
                optScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
                optScroll.ZIndex = 1000001
                optScroll.Parent = modalFrame

                local optLayout = Instance.new("UIListLayout")
                optLayout.Padding = UDim.new(0, 5)
                optLayout.SortOrder = Enum.SortOrder.LayoutOrder
                optLayout.Parent = optScroll

                optLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
                    optScroll.CanvasSize = UDim2.new(0, 0, 0, optLayout.AbsoluteContentSize.Y + 10)
                end)

                local currentSelected = {}
                if isMulti then
                    if type(OptionObj.Value) == "table" then
                        for _, v in ipairs(OptionObj.Value) do table.insert(currentSelected, v) end
                    end
                else
                    table.insert(currentSelected, OptionObj.Value)
                end

                local optionButtons = {}
                local function renderOptions()
                    for _, btnObj in ipairs(optionButtons) do btnObj:Destroy() end
                    optionButtons = {}

                    local currentValues = (type(OptionObj.Values) == "function" and OptionObj.Values()) or OptionObj.Values or values
                    if type(currentValues) ~= "table" then currentValues = {} end

                    for _, optVal in ipairs(currentValues) do
                        local isSelected = isMulti and (table.find(currentSelected, optVal) ~= nil) or (currentSelected[1] == optVal)

                        local itemBtn = Instance.new("TextButton")
                        itemBtn.Size = UDim2.new(1, -6, 0, 38)
                        itemBtn.BackgroundColor3 = isSelected and COLORS.primary or COLORS.glassDeep
                        itemBtn.BackgroundTransparency = isSelected and 0.1 or 0.2
                        itemBtn.Text = (isSelected and "   ✓  " or "       ") .. tostring(optVal)
                        itemBtn.TextColor3 = isSelected and Color3.fromRGB(255, 255, 255) or COLORS.text
                        itemBtn.Font = Enum.Font.GothamBold
                        itemBtn.TextSize = 13
                        itemBtn.TextXAlignment = Enum.TextXAlignment.Left
                        itemBtn.ZIndex = 1000002
                        itemBtn.Parent = optScroll

                        local ibCorner = Instance.new("UICorner")
                        ibCorner.CornerRadius = UDim.new(0, 8)
                        ibCorner.Parent = itemBtn

                        local ibStroke = Instance.new("UIStroke")
                        ibStroke.Color = isSelected and COLORS.primary or COLORS.surfaceRaised
                        ibStroke.Thickness = 1
                        ibStroke.Parent = itemBtn

                        itemBtn.MouseButton1Click:Connect(function()
                            playClickSound()
                            if isMulti then
                                local foundIdx = table.find(currentSelected, optVal)
                                if foundIdx then table.remove(currentSelected, foundIdx) else table.insert(currentSelected, optVal) end
                                updateDropdown(currentSelected)
                                renderOptions()
                            else
                                currentSelected = { optVal }
                                updateDropdown(optVal)
                                modalOverlay:Destroy()
                            end
                        end)

                        table.insert(optionButtons, itemBtn)
                    end
                end

                renderOptions()

                local confirmBtn = Instance.new("TextButton")
                confirmBtn.Size = UDim2.new(1, -28, 0, 40)
                confirmBtn.Position = UDim2.new(0, 14, 1, -50)
                confirmBtn.BackgroundColor3 = COLORS.primary
                confirmBtn.BackgroundTransparency = 0.15
                confirmBtn.Text = "✓ ตกลง / ยืนยันการเลือก (Confirm)"
                confirmBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                confirmBtn.Font = Enum.Font.GothamBold
                confirmBtn.TextSize = 13
                confirmBtn.ZIndex = 10001
                confirmBtn.Parent = modalFrame

                local cfCorner = Instance.new("UICorner")
                cfCorner.CornerRadius = UDim.new(0, 8)
                cfCorner.Parent = confirmBtn

                confirmBtn.MouseButton1Click:Connect(function()
                    playClickSound()
                    if isMulti then updateDropdown(currentSelected) end
                    modalOverlay:Destroy()
                end)
            end)

            ObsidianGlassEngine.Options[id] = OptionObj
            return OptionObj
        end

        function TabObj:AddButton(id, bCfg)
            if type(id) == "table" then
                bCfg = id
                id = bCfg.Title or "Button"
            end
            bCfg = bCfg or {}
            local title = bCfg.Title or id
            local cb = bCfg.Callback or function() end

            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(1, -10, 0, 40)
            btn.BackgroundColor3 = COLORS.surfaceRaised
            btn.BackgroundTransparency = 0.18
            btn.Text = title
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            btn.Font = Enum.Font.GothamBold
            btn.TextSize = 14
            btn.Parent = pageScroll

            local bCorner = Instance.new("UICorner")
            bCorner.CornerRadius = UDim.new(0, 8)
            bCorner.Parent = btn

            local bStroke = Instance.new("UIStroke")
            bStroke.Color = COLORS.primary
            bStroke.Thickness = 1
            bStroke.Parent = btn

            btn.MouseButton1Click:Connect(function()
                playClickSound()
                pcall(cb)
            end)
            return btn
        end

        function TabObj:AddInput(id, iCfg)
            if type(id) == "table" then
                iCfg = id
                id = iCfg.Title or "Input"
            end
            iCfg = iCfg or {}
            local title = iCfg.Title or id
            local defaultVal = iCfg.Default or iCfg.Value or ""

            local frame = Instance.new("Frame")
            frame.Size = UDim2.new(1, -10, 0, 48)
            frame.BackgroundColor3 = COLORS.glassDeep
            frame.BackgroundTransparency = 0.18
            frame.BorderSizePixel = 0
            frame.Parent = pageScroll

            local fCorner = Instance.new("UICorner")
            fCorner.CornerRadius = UDim.new(0, 8)
            fCorner.Parent = frame

            local lbl = Instance.new("TextLabel")
            lbl.Size = UDim2.new(0.5, 0, 1, 0)
            lbl.Position = UDim2.new(0, 12, 0, 0)
            lbl.BackgroundTransparency = 1
            lbl.Text = title
            lbl.TextColor3 = COLORS.text
            lbl.Font = Enum.Font.GothamBold
            lbl.TextSize = 14
            lbl.TextXAlignment = Enum.TextXAlignment.Left
            lbl.Parent = frame

            local box = Instance.new("TextBox")
            box.Size = UDim2.new(0.45, 0, 0, 30)
            box.Position = UDim2.new(0.52, 0, 0.5, -15)
            box.BackgroundColor3 = COLORS.input
            box.BackgroundTransparency = 0.20
            box.Text = tostring(defaultVal)
            box.TextColor3 = COLORS.cyan
            box.Font = Enum.Font.Gotham
            box.TextSize = 13
            box.Parent = frame

            local bxCorner = Instance.new("UICorner")
            bxCorner.CornerRadius = UDim.new(0, 6)
            bxCorner.Parent = box

            local OptionObj = {
                Value = defaultVal,
                Callback = iCfg.Callback or function() end,
                ChangedCallbacks = {}
            }

            box.FocusLost:Connect(function()
                OptionObj.Value = box.Text
                pcall(function() OptionObj.Callback(box.Text) end)
                for _, cb in ipairs(OptionObj.ChangedCallbacks) do pcall(function() cb(box.Text) end) end
            end)

            function OptionObj:OnChanged(cb) table.insert(OptionObj.ChangedCallbacks, cb) end
            function OptionObj:SetValue(val) box.Text = tostring(val); OptionObj.Value = tostring(val) end

            ObsidianGlassEngine.Options[id] = OptionObj
            return OptionObj
        end

        function TabObj:AddSection(id, title)
            local secTitle = title
            if type(id) == "table" then
                secTitle = id.Title or id.Text or ""
            elseif title == nil then
                secTitle = id
            end
            local sec = Instance.new("TextLabel")
            sec.Size = UDim2.new(1, -10, 0, 30)
            sec.BackgroundTransparency = 1
            sec.Text = "──  " .. tostring(secTitle) .. "  ──"
            sec.TextColor3 = COLORS.cyan
            sec.Font = Enum.Font.GothamBold
            sec.TextSize = 14
            sec.Parent = pageScroll
            return sec
        end

        function TabObj:AddParagraph(id, pCfg)
            if type(id) == "table" then
                pCfg = id
                id = pCfg.Title or "Paragraph"
            end
            pCfg = pCfg or {}
            local title = pCfg.Title or ""
            local desc = pCfg.Desc or ""

            local frame = Instance.new("Frame")
            frame.Size = UDim2.new(1, -10, 0, 54)
            frame.BackgroundColor3 = COLORS.glassDeep
            frame.BackgroundTransparency = 0.18
            frame.BorderSizePixel = 0
            frame.Parent = pageScroll

            local fCorner = Instance.new("UICorner")
            fCorner.CornerRadius = UDim.new(0, 8)
            fCorner.Parent = frame

            local fStroke = Instance.new("UIStroke")
            fStroke.Color = COLORS.surface
            fStroke.Thickness = 1
            fStroke.Parent = frame

            local pTitle = Instance.new("TextLabel")
            pTitle.Size = UDim2.new(1, -20, 0, 22)
            pTitle.Position = UDim2.new(0, 10, 0, 6)
            pTitle.BackgroundTransparency = 1
            pTitle.Text = title
            pTitle.TextColor3 = COLORS.text
            pTitle.Font = Enum.Font.GothamBold
            pTitle.TextSize = 13
            pTitle.TextXAlignment = Enum.TextXAlignment.Left
            pTitle.Parent = frame

            local pDesc = Instance.new("TextLabel")
            pDesc.Size = UDim2.new(1, -20, 0, 24)
            pDesc.Position = UDim2.new(0, 10, 0, 26)
            pDesc.BackgroundTransparency = 1
            pDesc.Text = desc
            pDesc.TextColor3 = COLORS.textMuted
            pDesc.Font = Enum.Font.Gotham
            pDesc.TextSize = 11
            pDesc.TextWrapped = true
            pDesc.TextXAlignment = Enum.TextXAlignment.Left
            pDesc.Parent = frame

            local ParaObj = {
                Frame = frame,
                TitleLabel = pTitle,
                DescLabel = pDesc,
            }
            function ParaObj:SetTitle(t)
                pTitle.Text = tostring(t)
            end
            function ParaObj:SetDesc(d)
                pDesc.Text = tostring(d)
                local TextService = game:GetService("TextService")
                local ok, bounds = pcall(function()
                    return TextService:GetTextSize(tostring(d), 11, Enum.Font.Gotham, Vector2.new(math.max(frame.AbsoluteSize.X - 20, 200), 2000))
                end)
                local textH = ok and bounds.Y or 24
                pDesc.Size = UDim2.new(1, -20, 0, textH)
                frame.Size = UDim2.new(1, -10, 0, textH + 32)
            end
            function ParaObj:SetText(t, d)
                if t then self:SetTitle(t) end
                if d then self:SetDesc(d) end
            end
            ParaObj:SetDesc(desc)
            return ParaObj
        end

        TabObj.Toggle = TabObj.AddToggle
        TabObj.Slider = TabObj.AddSlider
        TabObj.Dropdown = TabObj.AddDropdown
        TabObj.Input = TabObj.AddInput
        TabObj.Button = TabObj.AddButton
        TabObj.Section = TabObj.AddSection
        TabObj.Paragraph = TabObj.AddParagraph

        table.insert(WindowObj.Tabs, TabObj)
        return TabObj
    end

    WindowObj.Tab = WindowObj.AddTab

    UserInputService.InputBegan:Connect(function(input, gpe)
        if gpe then return end
        if input.KeyCode == Enum.KeyCode.K then
            playClickSound()
            shell.Visible = not shell.Visible
        elseif input.KeyCode == Enum.KeyCode.F then
            playClickSound()
            uiScale.Scale = (uiScale.Scale == 1.0) and 0.85 or 1.0
        end
    end)

    return WindowObj
end

    return Engine
end)()

----------------------------------------------------------------
-- UI Interface Loader (Obsidian Glassmorphic 2 Engine)
----------------------------------------------------------------
task.spawn(function()
    local function keyFromName(name)
        local ok, key = pcall(function() return Enum.KeyCode[tostring(name)] end)
        return ok and key or Enum.KeyCode.RightControl
    end

    local function deepCopy(t)
        local out = {}
        for k, v in pairs(t) do out[k] = (type(v) == "table") and deepCopy(v) or v end
        return out
    end
    local cfgAtBuild = deepCopy(Cfg)
    uiBuilding = true

    local okWin, winInstance = pcall(function()
        return ObsidianGlassEngine:CreateWindow({
            Title    = HUB_TITLE,
            SubTitle = HUB_AUTHOR,
        })
    end)

    if not (okWin and winInstance) then
        warn("[" .. HUB_TAG .. "] ไม่สามารถสร้างหน้าต่าง Obsidian Glass Engine ได้")
        return
    end

    UIWindow = winInstance

    Notify = function(title, text, dur)
        pcall(function() ObsidianGlassEngine:Notify({ Title = title, Content = text, Duration = dur or 4 }) end)
    end

    local function isOn(v) return v == true end

    local function toSet(value)
        local set = {}
        if type(value) == "table" then
            for k, v in pairs(value) do
                if type(k) == "number" then
                    if type(v) == "string" then set[v] = true end
                elseif v then
                    set[k] = true
                end
            end
        elseif type(value) == "string" then
            set[value] = true
        end
        return set
    end

    local TabFarm  = UIWindow:Tab({ Title = "ฟาร์ม",  Icon = "egg" })
    local TabSell  = UIWindow:Tab({ Title = "ขาย",    Icon = "coins" })
    local TabProg  = UIWindow:Tab({ Title = "พัฒนา",  Icon = "trending-up" })
    local TabSys   = UIWindow:Tab({ Title = "ระบบ",   Icon = "settings" })

    ----------------------------------------------------------------
    -- Tab: Farm
    ----------------------------------------------------------------
    local StatusBox = TabFarm:Paragraph({ Title = "สถานะ", Desc = "กำลังเริ่ม..." })

    TabFarm:Toggle({
        Title = "ขโมยไข่อัตโนมัติ",
        Desc  = "วาปไปหยิบแล้ววาปกลับจุดยืนประจำ วนไม่หยุด",
        Value = Cfg.AutoSteal,
        Callback = function(v) Cfg.AutoSteal = isOn(v) saveConfig() end,
    })

    TabFarm:Dropdown({
        Title = "ระดับไข่ที่จะขโมย",
        Desc  = "เลือกได้หลายระดับพร้อมกัน",
        Values = RARITY_ORDER,
        Value = setToList(Cfg.StealRarities),
        Multi = true,
        AllowNone = true,
        Callback = function(v) Cfg.StealRarities = toSet(v) saveConfig() end,
    })

    TabFarm:Dropdown({
        Title = "โซนที่จะเข้า",
        Values = AREAS,
        Value = (function() local t = {} for _, a in ipairs(AREAS) do if Cfg.StealAreas[a] then t[#t + 1] = a end end return t end)(),
        Multi = true,
        AllowNone = true,
        Callback = function(v)
            Cfg.StealAreas = toSet(v)
            saveConfig()
        end,
    })

    TabFarm:Dropdown({
        Title = "วิธีเลือกไข่",
        Desc  = "Efficient = คิดระยะทางด้วย ได้ไข่ถี่กว่า | BestOnly = ไล่ล่าใบแรร์สุดเสมอ",
        Values = { "Efficient", "BestOnly" },
        Value = Cfg.PickMode,
        Callback = function(v) Cfg.PickMode = v saveConfig() end,
    })

    TabFarm:Slider({
        Title = "การกลายพันธุ์ขั้นต่ำ",
        Value = { Min = 0, Max = 5, Default = Cfg.MinMutations },
        Callback = function(v) Cfg.MinMutations = v saveConfigSoon() end,
    })

    TabFarm:Section({ Title = "ไข่ในฐาน" })

    TabFarm:Dropdown({
        Title = "จัดการไข่อัตโนมัติ",
        Desc  = "ฟักเมื่อพร้อม / วางลงแปลง",
        Values = { "ฟัก", "วาง" },
        Value = (function()
            local t = {}
            if Cfg.AutoHatch then t[#t + 1] = "ฟัก" end
            if Cfg.AutoPlace then t[#t + 1] = "วาง" end
            return t
        end)(),
        Multi = true,
        AllowNone = true,
        Callback = function(v)
            local set = toSet(v)
            Cfg.AutoHatch = set["ฟัก"] == true
            Cfg.AutoPlace = set["วาง"] == true
            saveConfig()
        end,
    })

    TabFarm:Toggle({
        Title = "สลับสัตว์ตัวเก่งเข้าคอก (Equip Best)",
        Desc  = "จัดอันดับทั้งกระเป๋าแล้วสลับตัวรายได้สูงสุดเข้าคอกให้เต็มทุก 4 วินาที",
        Value = Cfg.AutoEquipBest,
        Callback = function(v)
            Cfg.AutoEquipBest = isOn(v)
            saveConfig()
            if isOn(v) then task.spawn(function() withLock(equipBest) end) end
        end,
    })

    TabFarm:Dropdown({
        Title = "ระดับไข่ที่จะวางลงแปลง",
        Values = RARITY_ORDER,
        Value = setToList(Cfg.PlaceRarities),
        Multi = true,
        AllowNone = true,
        Callback = function(v) Cfg.PlaceRarities = toSet(v) saveConfig() end,
    })

    ----------------------------------------------------------------
    -- Tab: Sell
    ----------------------------------------------------------------
    local SellPreview = TabSell:Paragraph({
        Title = "ตัวอย่างรายการที่จะขาย",
        Desc  = "อัปเดตอัตโนมัติทุก 5 วินาที — ไม่มีอะไรถูกขายจนกว่าจะเปิดสวิตช์",
    })

    TabSell:Toggle({
        Title = "ขายสัตว์เลี้ยงอัตโนมัติ",
        Value = Cfg.AutoSellPets,
        Callback = function(v) Cfg.AutoSellPets = isOn(v) markSellDirty() saveConfig() end,
    })

    TabSell:Dropdown({
        Title = "เงื่อนไขการขายสัตว์",
        Desc  = "เลือกอย่างเดียวก็พอ ถ้าเลือก \"เข้าทั้งสองเงื่อนไข\" ต้องตั้งทั้งราคาและระดับ",
        Values = { "ราคาต่ำกว่าที่ตั้ง", "ระดับที่เลือก", "เข้าทั้งสองเงื่อนไข" },
        Value = Cfg.SellPetMode,
        Callback = function(v) Cfg.SellPetMode = v markSellDirty() saveConfig() end,
    })

    TabSell:Input({
        Title = "ขายสัตว์ที่ราคาต่ำกว่า",
        Desc  = "ใส่ราคาขายเป็นเงินเกม เช่น 100000 หรือ 1m / 500k ก็ได้ | 0 = ปิดเงื่อนไขนี้",
        Value = tostring(Cfg.SellPetBelow),
        Callback = function(v)
            local s = tostring(v):lower():gsub("[,%s]", "")
            local num = tonumber(s:match("^[%d%.]+")) or 0
            if s:find("k") then num = num * 1e3
            elseif s:find("m") then num = num * 1e6
            elseif s:find("b") then num = num * 1e9
            elseif s:find("t") then num = num * 1e12 end
            Cfg.SellPetBelow = num
            markSellDirty()
            saveConfig()
            Notify("ขาย", ("ขายสัตว์ที่ราคาต่ำกว่า %s"):format(shortNum(num)), 3)
        end,
    })

    TabSell:Dropdown({
        Title = "ระดับสัตว์ที่ยอมขาย",
        Values = RARITY_ORDER,
        Value = setToList(Cfg.SellPetRarities),
        Multi = true,
        AllowNone = true,
        Callback = function(v) Cfg.SellPetRarities = toSet(v) markSellDirty() saveConfig() end,
    })

    TabSell:Slider({
        Title = "เก็บตัวเก่งสุดไว้กี่ตัว",
        Desc  = "ตัวรายได้สูงสุดตามจำนวนนี้ไม่ถูกขายเด็ดขาด",
        Value = { Min = 0, Max = 60, Default = Cfg.SellKeepTop },
        Callback = function(v) Cfg.SellKeepTop = v markSellDirty() saveConfigSoon() end,
    })

    TabSell:Section({ Title = "ไข่" })

    TabSell:Toggle({
        Title = "ขายไข่อัตโนมัติ",
        Desc  = "ขายเฉพาะไข่ที่ยังไม่ได้วางลงแปลง",
        Value = Cfg.AutoSellEggs,
        Callback = function(v) Cfg.AutoSellEggs = isOn(v) markSellDirty() saveConfig() end,
    })

    TabSell:Dropdown({
        Title = "ระดับไข่ที่จะขายทิ้ง",
        Values = RARITY_ORDER,
        Value = setToList(Cfg.SellEggRarities),
        Multi = true,
        AllowNone = true,
        Callback = function(v) Cfg.SellEggRarities = toSet(v) markSellDirty() saveConfig() end,
    })

    TabSell:Input({
        Title = "หรือขายไข่ที่ราคาต่ำกว่า",
        Desc  = "0 = ปิดเงื่อนไขนี้ ใช้ระดับความหายากอย่างเดียว",
        Value = tostring(Cfg.SellEggBelow),
        Callback = function(v)
            local s = tostring(v):lower():gsub("[,%s]", "")
            local num = tonumber(s:match("^[%d%.]+")) or 0
            if s:find("k") then num = num * 1e3 elseif s:find("m") then num = num * 1e6 end
            Cfg.SellEggBelow = num
            markSellDirty()
            saveConfig()
        end,
    })

    TabSell:Section({ Title = "ความเร็ว" })

    TabSell:Slider({
        Title = "ความเร็วขาย",
        Desc  = "จำนวนคำสั่งที่ค้างได้พร้อมกัน — สูงขึ้น = ขายไวขึ้น แต่ยิงถี่เกินเริ่มกระตุก 3 คือจุดที่ลื่นและไวพอ",
        Value = { Min = 1, Max = 6, Default = Cfg.SellLanes },
        Callback = function(v) Cfg.SellLanes = v saveConfigSoon() end,
    })

    TabSell:Slider({
        Title = "เพดานต่อครั้งที่สั่งเอง (ตัว/ใบ)",
        Desc  = "ใช้กับการสั่งขายผ่าน API เท่านั้น โหมดอัตโนมัติขายเรื่อยๆ ไม่มีเพดาน",
        Value = { Min = 1, Max = 200, Default = Cfg.SellMaxPerRun },
        Callback = function(v) Cfg.SellMaxPerRun = v saveConfigSoon() end,
    })

    TabSell:Paragraph({
        Title = "กันพลาด",
        Desc  = "ตัวที่สวมใส่อยู่ / ติดดาว (Favorite) / อยู่ในเครื่องผสม จะไม่ถูกขายทุกกรณี",
    })

    ----------------------------------------------------------------
    -- Tab: Progression
    ----------------------------------------------------------------
    local ProgBox = TabProg:Paragraph({ Title = "ความคืบหน้า", Desc = "กำลังอ่านข้อมูล..." })

    TabProg:Dropdown({
        Title = "ระบบอัตโนมัติที่จะเปิด",
        Desc  = "อัปฐานกับลู่วิ่งจะซื้อทันทีที่เงินถึง ตรวจทุก 1 วินาที",
        Values = { "อัปเกรดฐาน", "อัปเกรดลู่วิ่ง", "ซื้อ/ใส่เทรล", "ผสมสัตว์", "รับรางวัล" },
        Value = (function()
            local t = {}
            if Cfg.AutoBaseUpgrade then t[#t + 1] = "อัปเกรดฐาน" end
            if Cfg.AutoTreadmillUp then t[#t + 1] = "อัปเกรดลู่วิ่ง" end
            if Cfg.AutoTrails then t[#t + 1] = "ซื้อ/ใส่เทรล" end
            if Cfg.AutoFuse then t[#t + 1] = "ผสมสัตว์" end
            if Cfg.AutoClaim then t[#t + 1] = "รับรางวัล" end
            return t
        end)(),
        Multi = true,
        AllowNone = true,
        Callback = function(v)
            local set = toSet(v)
            Cfg.AutoBaseUpgrade = set["อัปเกรดฐาน"] == true
            Cfg.AutoTreadmillUp = set["อัปเกรดลู่วิ่ง"] == true
            Cfg.AutoTrails      = set["ซื้อ/ใส่เทรล"] == true
            Cfg.AutoFuse        = set["ผสมสัตว์"] == true
            Cfg.AutoClaim       = set["รับรางวัล"] == true
            saveConfig()
        end,
    })

    ----------------------------------------------------------------
    -- Tab: System
    ----------------------------------------------------------------

    ----------------------------------------------------------------
    -- AFK Mode overlay ("ไก่ตัน")
    --
    -- A full-screen iOS-styled panel meant to be left running for hours, so
    -- every part of it obeys three rules:
    --   1. Nothing polls. Logs arrive through the existing logLine() call, money
    --      arrives through ActiveAssets: MoneyCollected, and the only per-frame
    --      work is one integer compare inside the pump.
    --   2. Every repeated row is pooled. Log rows, pet cards and egg rows are
    --      created once and reused, so an eight-hour session holds the same
    --      Instance count as the first minute.
    --   3. Opening it drops render load — 3D rendering off, quality floor, FPS
    --      cap — and closing it puts all three back exactly as they were.
    ----------------------------------------------------------------
    local AFK_MAX_LOGS      = 40  -- log pool size; also the hard cap on visible rows
    local AFK_MAX_EGGROWS   = 40  -- egg pool size
    local AFK_MAX_PETS      = 150 -- pet card pool size; allocated once at build
    local AFK_GRID_DEBOUNCE = 2   -- seconds between pet rebuilds, even on a hatch burst
    local AFK_GRID_REFRESH  = 15  -- safety re-read, in case an inventory change logs nothing
    local AFK_EGG_REFRESH   = 5   -- egg roster re-read; the countdown itself ticks locally
    local AFK_FPS_CAP       = 10

    -- iOS dark system palette: one flat black ground, two elevated greys, and a
    -- hairline separator. Colour is carried by text and 1px strokes only —
    -- filled accent blocks are what makes a panel stop looking like iOS.
    local AFK_COLOR = {
        bg        = Color3.fromRGB(0, 0, 0),
        panel     = Color3.fromRGB(28, 28, 30),
        raised    = Color3.fromRGB(44, 44, 46),
        separator = Color3.fromRGB(56, 56, 58),
        label     = Color3.fromRGB(255, 255, 255),
        label2    = Color3.fromRGB(152, 152, 158),
        label3    = Color3.fromRGB(110, 110, 115),
        blue      = Color3.fromRGB(10, 132, 255),
        green     = Color3.fromRGB(48, 209, 88),
        red       = Color3.fromRGB(255, 69, 58),
        orange    = Color3.fromRGB(255, 159, 10),
        yellow    = Color3.fromRGB(255, 214, 10),
        teal      = Color3.fromRGB(100, 210, 255),
    }

    local AFK_RARITY_COLOR = {
        Common    = Color3.fromRGB(152, 152, 158),
        Uncommon  = Color3.fromRGB(48, 209, 88),
        Rare      = Color3.fromRGB(10, 132, 255),
        Epic      = Color3.fromRGB(191, 90, 242),
        Legendary = Color3.fromRGB(255, 159, 10),
        Mythic    = Color3.fromRGB(255, 55, 95),
        Divine    = Color3.fromRGB(100, 210, 255),
        Secret    = Color3.fromRGB(255, 214, 10),
        Cosmic    = Color3.fromRGB(94, 92, 230),
        Eternal   = Color3.fromRGB(255, 69, 58),
    }

    -- Touch-only clients get a smaller scale and a warning strip. The mode still
    -- runs there; it is simply not what it was built for (see the toggle text).
    local AFK_TOUCH = (function()
        local ok, touch = pcall(function()
            local uis = game:GetService("UserInputService")
            return uis.TouchEnabled and not uis.KeyboardEnabled
        end)
        return ok and touch == true
    end)()

    local AFK = {
        on         = false,
        gui        = nil,
        conns      = {},
        startAt    = 0,
        lastSec    = -1,
        wantShow   = false,-- state changes are queued for the pump, never applied
        wantHide   = false,-- by whatever thread happened to call open/close
        wantSeed   = false,
        logDirty   = false,-- set by logLine, drained by the pump on our own thread
        gridDirty  = false,
        eggDirty   = false,
        pool       = {},   -- log rows, index 1..AFK_MAX_LOGS
        poolHead   = 0,    -- ring cursor
        cards      = {},   -- pet cards
        strokes    = {},   -- UIStroke per pet card, kept beside the frame not on it
        eggRows    = {},   -- egg rows
        eggStrokes = {},   -- band-coloured UIStroke per egg row
        eggData    = {},   -- per-row countdown state, parallel to eggRows
        eggAt      = 0,
        gridAt     = 0,
        toggleRef  = nil,
        hooked     = false,
        prevFps    = nil,
        prev3D     = nil,
        prevQuality= nil,
        stat       = {},   -- the four header value labels
        petTitle   = nil,
        eggTitle   = nil,
        logList    = nil,
        gridList   = nil,
        eggList    = nil,
        gridLayout = nil,
    }

    ----------------------------------------------------------------
    -- Small builders
    ----------------------------------------------------------------
    local function afkCorner(parent, r)
        local c = Instance.new("UICorner")
        c.CornerRadius = UDim.new(0, r or 12)
        c.Parent = parent
        return c
    end

    local function afkStroke(parent, color, thickness, transparency)
        local s = Instance.new("UIStroke")
        s.Color = color or AFK_COLOR.separator
        s.Thickness = thickness or 1
        s.Transparency = transparency or 0
        s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
        s.Parent = parent
        return s
    end

    local function afkPad(parent, all)
        local p = Instance.new("UIPadding")
        p.PaddingTop    = UDim.new(0, all)
        p.PaddingBottom = UDim.new(0, all)
        p.PaddingLeft   = UDim.new(0, all)
        p.PaddingRight  = UDim.new(0, all)
        p.Parent = parent
        return p
    end

    local function afkText(parent, props)
        local t = Instance.new("TextLabel")
        t.BackgroundTransparency = 1
        t.RichText = false
        t.Font = props.font or Enum.Font.Gotham
        t.TextSize = props.size or 13
        t.TextColor3 = props.color or AFK_COLOR.label
        t.TextXAlignment = props.align or Enum.TextXAlignment.Left
        t.TextYAlignment = Enum.TextYAlignment.Center
        t.Text = props.text or ""
        t.Size = props.sizeUDim or UDim2.new(1, 0, 1, 0)
        if props.pos then t.Position = props.pos end
        if props.truncate then t.TextTruncate = Enum.TextTruncate.AtEnd end
        t.Parent = parent
        return t
    end

    -- one elevated card, the shape every iOS grouped list is made of
    local function afkCard(parent, props)
        local f = Instance.new("Frame")
        f.BackgroundColor3 = props.color or AFK_COLOR.panel
        f.BorderSizePixel = 0
        f.Size = props.size or UDim2.new(1, 0, 1, 0)
        if props.pos then f.Position = props.pos end
        if props.anchor then f.AnchorPoint = props.anchor end
        afkCorner(f, props.radius or 14)
        if props.stroke ~= false then afkStroke(f, AFK_COLOR.separator, 1, 0.35) end
        f.Parent = parent
        return f
    end

    local function afkClock(sec)
        sec = math.max(0, math.floor(sec))
        return ("%02d:%02d:%02d"):format(math.floor(sec / 3600), math.floor(sec % 3600 / 60), sec % 60)
    end

    local function afkShort(sec)
        sec = math.floor(sec)
        if sec <= 0 then return "พร้อมฟัก" end
        if sec < 60 then return ("%d วิ"):format(sec) end
        if sec < 3600 then return ("%d:%02d"):format(math.floor(sec / 60), sec % 60) end
        return ("%d ชม %d น"):format(math.floor(sec / 3600), math.floor(sec % 3600 / 60))
    end

    local function afkAssetCfg(cat)
        return AssetsDir and AssetsDir.Directory and AssetsDir.Directory[cat] or nil
    end

    -- the egg has its own art and its own name in the directory (`cfg.Egg`);
    -- using the pet icon here showed the hatched animal instead of the shell
    local function afkEggArt(cat)
        local cfg = afkAssetCfg(cat)
        local egg = cfg and cfg.Egg
        local icon = (egg and egg.Icon) or (cfg and cfg.Icon) or ""
        local name = (egg and egg.DisplayName)
            or ((cfg and cfg.DisplayName) and (cfg.DisplayName .. " Egg"))
            or tostring(cat)
        return icon, name
    end

    -- Countdown bands, the same ones the legend explains to the player.
    -- green  : 10s or less  — about to hatch
    -- yellow : 11s .. 60s   — nearly there
    -- red    : over 60s     — still a long wait
    local function afkBandColor(remain, ready, placed)
        if ready then return AFK_COLOR.green end
        if not placed then return AFK_COLOR.label3 end
        if remain == nil then return AFK_COLOR.label2 end
        if remain <= 10 then return AFK_COLOR.green end
        if remain <= 60 then return AFK_COLOR.yellow end
        return AFK_COLOR.red
    end

    ----------------------------------------------------------------
    -- Render throttle
    ----------------------------------------------------------------
    -- Every call is guarded: executors disagree wildly about which of these
    -- exist, and a missing setfpscap must never take the overlay down with it.
    local function afkEnterLowPower()
        pcall(function()
            if getfpscap then AFK.prevFps = getfpscap() end
            if setfpscap then setfpscap(AFK_FPS_CAP) end
        end)
        pcall(function()
            AFK.prevQuality = settings().Rendering.QualityLevel
            settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
        end)
        pcall(function()
            if RunService.Set3dRenderingEnabled then
                AFK.prev3D = true
                RunService:Set3dRenderingEnabled(false)
            end
        end)
    end

    local function afkExitLowPower()
        pcall(function()
            if setfpscap then setfpscap((AFK.prevFps and AFK.prevFps > 0) and AFK.prevFps or 240) end
        end)
        pcall(function()
            if AFK.prevQuality then settings().Rendering.QualityLevel = AFK.prevQuality end
        end)
        pcall(function()
            if AFK.prev3D and RunService.Set3dRenderingEnabled then
                RunService:Set3dRenderingEnabled(true)
            end
        end)
        AFK.prevFps, AFK.prevQuality, AFK.prev3D = nil, nil, nil
    end

    ----------------------------------------------------------------
    -- Column 1: activity log (ring buffer, zero allocation)
    ----------------------------------------------------------------
    -- The newest line overwrites the oldest row's text and LayoutOrder alone
    -- re-sorts the list. No Instance is created or destroyed while running,
    -- which is what stops an all-night session from leaking rows.
    local function afkPushLog(text)
        if not AFK.on or not AFK.logList or not text then return end
        AFK.poolHead = AFK.poolHead % AFK_MAX_LOGS + 1
        local row = AFK.pool[AFK.poolHead]
        if not row then return end
        row.Text = text
        row.Visible = true
        for i = 1, AFK_MAX_LOGS do
            local r = AFK.pool[i]
            if r and r.Visible then
                r.LayoutOrder = (AFK.poolHead - i) % AFK_MAX_LOGS
            end
        end
    end

    local function afkSeedLogs()
        for i = 1, AFK_MAX_LOGS do
            local r = AFK.pool[i]
            if r then r.Visible = false end
        end
        AFK.poolHead = 0
        -- Runtime.log is already newest-first and already capped at 40
        for i = math.min(#Runtime.log, AFK_MAX_LOGS), 1, -1 do
            afkPushLog(Runtime.log[i])
        end
    end

    ----------------------------------------------------------------
    -- Column 2: pet inventory
    ----------------------------------------------------------------
    -- Both grids are fully preallocated in afkBuild and only ever have their
    -- properties written afterwards. That is not just a performance choice:
    -- creating or reparenting an Instance after the panel is up was throwing
    -- "lacking capability Plugin" whenever the call arrived on a thread other
    -- than the one that built the GUI, and a hidden executor container cannot be
    -- written to from a normal game thread at all. Allocate once, paint forever.
    local function afkPaintPets(force)
        if not AFK.on or not AFK.gridList then return end
        local now = os.clock()
        if not force and now - AFK.gridAt < AFK_GRID_DEBOUNCE then return end
        AFK.gridAt = now

        local list = {}
        for _, p in ipairs(petItems(false)) do list[#list + 1] = p end
        table.sort(list, function(a, b)
            local ra, rb = a.rate or 0, b.rate or 0
            if ra == rb then return tostring(a.cat) < tostring(b.cat) end
            return ra > rb
        end)

        local slots = #AFK.cards
        local shown = math.min(#list, slots)

        for i = 1, shown do
            local p      = list[i]
            local card   = AFK.cards[i]
            local stroke = AFK.strokes[i]
            local cfg    = afkAssetCfg(p.cat)
            local col    = AFK_RARITY_COLOR[p.rarity] or AFK_COLOR.label2

            card.Icon.Image      = (cfg and cfg.Icon) or ""
            card.PetName.Text    = (cfg and cfg.DisplayName) or tostring(p.cat)
            card.Tier.Text       = RARITY_TH[p.rarity] or tostring(p.rarity)
            card.Tier.TextColor3 = col
            card.Income.Text     = ("%s/วิ"):format(shortNum(p.rate or 0))
            if stroke then stroke.Color = col end
            card.Visible = true
        end

        for i = shown + 1, slots do AFK.cards[i].Visible = false end

        if AFK.petTitle then
            AFK.petTitle.Text = (#list > slots)
                and ("สัตว์เลี้ยง · แสดง %d จาก %d ตัว"):format(slots, #list)
                or  ("สัตว์เลี้ยง · %d ตัว"):format(#list)
        end
    end

    ----------------------------------------------------------------
    -- Column 3: eggs and countdowns
    ----------------------------------------------------------------
    -- The roster is re-read every few seconds; the countdown itself is local
    -- arithmetic against a stored end time, so the clock stays real-time without
    -- asking the game anything in between.
    local function afkPaintEggs(force)
        if not AFK.on or not AFK.eggList then return end
        local now = os.clock()
        if not force and now - AFK.eggAt < AFK_EGG_REFRESH then return end
        AFK.eggAt = now

        local ok, roster = pcall(eggRoster)
        if not ok or type(roster) ~= "table" then return end

        -- ready first, then the shortest wait, then everything not placed yet
        local function rank(e)
            if e.ready then return 0 end
            if e.placed then return 1 end
            return 2
        end
        table.sort(roster, function(a, b)
            local ra, rb = rank(a), rank(b)
            if ra ~= rb then return ra < rb end
            local wa = a.remain or math.huge
            local wb = b.remain or math.huge
            if wa ~= wb then return wa < wb end
            return tostring(a.cat) < tostring(b.cat)
        end)

        local slots = #AFK.eggRows
        local ready, growing, idle = 0, 0, 0
        local stamp = os.time()

        for i, e in ipairs(roster) do
            if e.ready then ready += 1
            elseif e.placed then growing += 1
            else idle += 1 end

            if i <= slots then
                local row = AFK.eggRows[i]
                local icon, name = afkEggArt(e.cat)
                local col = AFK_RARITY_COLOR[e.rarity] or AFK_COLOR.label2

                row.Icon.Image      = icon
                row.EggName.Text    = name
                row.Tier.Text       = RARITY_TH[e.rarity] or tostring(e.rarity)
                row.Tier.TextColor3 = col
                row.Visible         = true

                -- state, not text: afkPaintCountdowns repaints from this every
                -- second without touching the game again
                AFK.eggData[i] = {
                    ready  = e.ready,
                    placed = e.placed,
                    endAt  = (e.remain and (stamp + e.remain)) or nil,
                }
            end
        end

        for i = math.min(#roster, slots) + 1, slots do
            AFK.eggRows[i].Visible = false
            AFK.eggData[i] = nil
        end

        -- an empty column and a broken column used to look identical; now they
        -- do not, which is the difference between "no eggs" and "go read a log"
        if AFK.eggEmpty then AFK.eggEmpty.Visible = (#roster == 0) end

        if AFK.eggTitle then
            AFK.eggTitle.Text = ("ไข่ · พร้อม %d · ฟักอยู่ %d · ว่าง %d"):format(ready, growing, idle)
        end

        -- eggs finishing is worth a log line: it is the one thing that happens
        -- in this column that the activity log never mentioned before
        if AFK.lastReady and ready > AFK.lastReady then
            logLine("egg", ("ไข่พร้อมฟัก %d ใบ"):format(ready))
        end
        AFK.lastReady = ready
    end

    -- Called once a second from the pump. This is the real-time part: the roster
    -- is only re-read every few seconds, but the number on screen and the band
    -- colour are recomputed from the stored end time every single second.
    local function afkPaintCountdowns()
        local stamp = os.time()
        for i = 1, #AFK.eggRows do
            local row    = AFK.eggRows[i]
            local d      = AFK.eggData[i]
            local stroke = AFK.eggStrokes[i]
            if row and row.Visible and d then
                local remain, text

                if d.ready then
                    remain, text = 0, "พร้อมฟัก"
                elseif not d.placed then
                    remain, text = nil, "ยังไม่วาง"
                elseif d.endAt then
                    remain = d.endAt - stamp
                    text = (remain <= 0) and "พร้อมฟัก" or afkShort(remain)
                else
                    remain, text = nil, "กำลังฟัก"
                end

                local col = afkBandColor(remain, d.ready or (remain ~= nil and remain <= 0), d.placed)
                row.Status.Text = text
                row.Status.TextColor3 = col
                if stroke then stroke.Color = col end
            end
        end
    end

    ----------------------------------------------------------------
    -- Guarded entry points: one bad row can never blank a panel
    ----------------------------------------------------------------
    local function afkSafeGrid(force)
        local ok, err = pcall(afkPaintPets, force)
        if not ok then warn("[AFK] pets: " .. tostring(err)) end
    end

    local function afkSafeEggs(force)
        local ok, err = pcall(afkPaintEggs, force)
        if not ok then warn("[AFK] eggs: " .. tostring(err)) end
    end

    ----------------------------------------------------------------
    -- The pump: the ONLY place the panel is ever written to
    ----------------------------------------------------------------
    -- A GUI parented into the executor's hidden container can only be written
    -- to by a thread with the same reach as the one that built it. Opening the
    -- panel from a WindUI button, from a log callback, or from a remote handler
    -- all run on different threads, and every write from those died with
    -- "lacking capability Plugin" — which is exactly why the egg column stayed
    -- empty while the data behind it was fine.
    --
    -- So nothing outside this function touches an Instance. afkOpen, afkClose
    -- and the logLine hook only raise flags; this pump runs on one connection
    -- created at build time by the script's own thread and applies them.
    local function afkPump()
        -- a re-executed script leaves the old panel's pump running, and it keeps
        -- painting a GUI nobody owns any more; the session check retires it
        if not alive() then
            pcall(function() if AFK.gui then AFK.gui:Destroy() end end)
            for k, c in pairs(AFK.conns) do
                pcall(function() c:Disconnect() end)
                AFK.conns[k] = nil
            end
            AFK.on = false
            return
        end

        if AFK.wantShow then
            AFK.wantShow = false
            if AFK.gui then AFK.gui.Enabled = true end
            afkEnterLowPower()
        end
        if AFK.wantHide then
            AFK.wantHide = false
            if AFK.gui then AFK.gui.Enabled = false end
            afkExitLowPower()
        end

        if not AFK.on then return end

        if AFK.wantSeed then
            AFK.wantSeed = false
            afkSeedLogs()
        end

        -- log rows are cheap and must feel live, so they are flushed on any
        -- frame they are dirty; everything else waits for the second boundary
        if AFK.logDirty then
            AFK.logDirty = false
            afkPushLog(Runtime.log[1])
        end

        local sec = math.floor(os.clock() - AFK.startAt)
        if sec == AFK.lastSec then return end   -- one write per second, not per frame
        AFK.lastSec = sec

        if AFK.gridDirty then AFK.gridDirty = false afkSafeGrid(false) end
        if AFK.eggDirty  then AFK.eggDirty  = false afkSafeEggs(false) end

        local s = AFK.stat
        if s.eggs   then s.eggs.Text   = tostring(Runtime.stats.eggs) end
        if s.hatch  then s.hatch.Text  = tostring(Runtime.stats.hatched) end
        if s.income then s.income.Text = shortNum(incomePerSec()) end
        if s.time   then s.time.Text   = afkClock(sec) end
        if s.total  then s.total.Text  = ("รวม " .. shortNum(Runtime.income.total)) end

        afkPaintCountdowns()

        if sec > 0 and sec % AFK_EGG_REFRESH  == 0 then afkSafeEggs(true) end
        if sec > 0 and sec % AFK_GRID_REFRESH == 0 then afkSafeGrid(true) end
    end

    local afkOpen, afkClose  -- forward declaration for UI header button & API exports

    local function setAfkResume()
        pcall(function()
            if typeof(getgenv) == "function" then getgenv().SAE_AfkResume = true end
        end)
    end

    local function clearAfkResume()
        pcall(function()
            if typeof(getgenv) == "function" then getgenv().SAE_AfkResume = nil end
        end)
    end

    local function consumeAfkResume()
        local res = false
        pcall(function()
            if typeof(getgenv) == "function" and getgenv().SAE_AfkResume then
                res = true
                getgenv().SAE_AfkResume = nil
            end
        end)
        return res
    end

    local function afkSetToggle(val)
        if AFK.toggleRef then
            for _, m in ipairs({ "SetValue", "Set", "UpdateValue" }) do
                if pcall(function() AFK.toggleRef[m](AFK.toggleRef, val) end) then break end
            end
        end
    end

    ----------------------------------------------------------------
    -- Construction
    ----------------------------------------------------------------
    local function afkBuild()
        -- a re-executed script leaves its old panel behind: it is a ScreenGui,
        -- not a WindUI window, so the normal cleanup never touched it and the
        -- screen ended up with a dead panel stacked over the live one
        -- The previous panel is reached through a reference we kept, never by
        -- searching CoreGui: scanning a protected container raises a capability
        -- error on some threads, and an executor's hidden GUI folder cannot be
        -- enumerated at all. Holding the handle in getgenv() survives a
        -- re-execute, which is the only case that ever leaves a panel behind.
        pcall(function()
            local old = ENV[HUB_KEY .. "AfkGui"]
            if typeof(old) == "Instance" then old:Destroy() end
        end)

        local gui = Instance.new("ScreenGui")
        gui.Name = "SAE_AFK"
        gui.ResetOnSpawn = false
        gui.IgnoreGuiInset = true
        gui.DisplayOrder = 9999
        gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

        -- PlayerGui on purpose, NOT gethui(). A GUI inside the executor's hidden
        -- container can only be written to by a thread holding the executor's
        -- own capability: the WindUI toggle, the game's remote callbacks and the
        -- log hook all run on other threads, and every write from them died with
        -- "lacking capability Plugin" — which is exactly why the egg column kept
        -- coming up empty while its data was fine. PlayerGui has no such gate,
        -- and mobile executors that ship no gethui at all get the same path.
        local parented = false
        pcall(function()
            gui.Parent = LocalPlayer:WaitForChild("PlayerGui", 10)
            parented = gui.Parent ~= nil
        end)
        if not parented then
            pcall(function() if gethui then gui.Parent = gethui() parented = true end end)
        end
        if not parented then
            pcall(function() gui.Parent = game:GetService("CoreGui") end)
        end

        -- flat black ground: it covers the 3D view completely, which is what
        -- makes Set3dRenderingEnabled(false) invisible to the eye
        local root = Instance.new("Frame")
        root.Name = "Root"
        root.BackgroundColor3 = AFK_COLOR.bg
        root.BorderSizePixel = 0
        root.Size = UDim2.fromScale(1, 1)
        root.Parent = gui

        local scale = Instance.new("UIScale")
        scale.Scale = AFK_TOUCH and 0.8 or 1
        scale.Parent = root

        local body = Instance.new("Frame")
        body.Name = "Body"
        body.BackgroundTransparency = 1
        body.Size = UDim2.fromScale(1, 1)
        body.Parent = root
        afkPad(body, 20)

        ------------------------------------------------------------
        -- Header
        ------------------------------------------------------------
        local header = Instance.new("Frame")
        header.Name = "Header"
        header.BackgroundTransparency = 1
        header.Size = UDim2.new(1, 0, 0, 44)
        header.Parent = body

        afkText(header, {
            text = "โหมด AFK", font = Enum.Font.GothamBold, size = 26,
            sizeUDim = UDim2.new(1, -220, 0, 30), pos = UDim2.fromOffset(0, 0),
        })
        afkText(header, {
            text = HUB_NAME, font = Enum.Font.Gotham, size = 12, color = AFK_COLOR.label3,
            sizeUDim = UDim2.new(1, -220, 0, 14), pos = UDim2.fromOffset(2, 30),
        })

        local closeBtn = Instance.new("TextButton")
        -- "Close" is reserved by the game's console UI observer, which
        -- expects an ImageLabel at this path.  This is a text button, so give
        -- it a non-conflicting name to prevent the ConsoleController error.
        closeBtn.Name = "ExitButton"
        closeBtn.AnchorPoint = Vector2.new(1, 0)
        closeBtn.Position = UDim2.new(1, 0, 0, 4)
        closeBtn.Size = UDim2.fromOffset(156, 38)
        closeBtn.BackgroundColor3 = AFK_COLOR.panel
        closeBtn.BorderSizePixel = 0
        closeBtn.AutoButtonColor = true
        closeBtn.Font = Enum.Font.GothamMedium
        closeBtn.TextSize = 14
        closeBtn.TextColor3 = AFK_COLOR.red
        closeBtn.Text = "ปิดโหมด AFK"
        closeBtn.Parent = header
        afkCorner(closeBtn, 12)
        afkStroke(closeBtn, AFK_COLOR.red, 1, 0.55)

        ------------------------------------------------------------
        -- Stat row: four iOS grouped cards
        ------------------------------------------------------------
        local stats = Instance.new("Frame")
        stats.Name = "Stats"
        stats.BackgroundTransparency = 1
        stats.Position = UDim2.fromOffset(0, 56)
        stats.Size = UDim2.new(1, 0, 0, 72)
        stats.Parent = body

        local statLayout = Instance.new("UIListLayout")
        statLayout.FillDirection = Enum.FillDirection.Horizontal
        statLayout.SortOrder = Enum.SortOrder.LayoutOrder
        statLayout.Padding = UDim.new(0, 10)
        statLayout.Parent = stats

        local function statCard(order, caption, value, color, sub)
            local card = afkCard(stats, {
                size = UDim2.new(0.25, -8, 1, 0), radius = 16,
            })
            card.LayoutOrder = order
            afkText(card, {
                text = caption, font = Enum.Font.Gotham, size = 11, color = AFK_COLOR.label3,
                sizeUDim = UDim2.new(1, -28, 0, 13), pos = UDim2.fromOffset(14, 12),
            })
            local val = afkText(card, {
                text = value, font = Enum.Font.GothamBold, size = 24, color = color or AFK_COLOR.label,
                sizeUDim = UDim2.new(1, -28, 0, 28), pos = UDim2.fromOffset(14, 28),
            })
            local subLbl
            if sub ~= nil then
                subLbl = afkText(card, {
                    text = sub, font = Enum.Font.Gotham, size = 10, color = AFK_COLOR.label3,
                    sizeUDim = UDim2.new(1, -28, 0, 12), pos = UDim2.fromOffset(14, 55),
                })
            end
            return val, subLbl
        end

        AFK.stat.eggs   = statCard(1, "ขโมยไข่สะสม", "0", AFK_COLOR.label)
        AFK.stat.hatch  = statCard(2, "ฟักแล้ว", "0", AFK_COLOR.teal)
        AFK.stat.income, AFK.stat.total = statCard(3, "รายได้ต่อวินาที", "0", AFK_COLOR.green, "รวม 0")
        AFK.stat.time   = statCard(4, "เวลา AFK", "00:00:00", AFK_COLOR.label)

        ------------------------------------------------------------
        -- Three columns
        ------------------------------------------------------------
        local topOffset = AFK_TOUCH and 162 or 140
        if AFK_TOUCH then
            local warn_ = afkCard(body, {
                size = UDim2.new(1, 0, 0, 26), pos = UDim2.fromOffset(0, 134), radius = 8,
            })
            afkText(warn_, {
                text = "ตรวจพบมือถือ — โหมดนี้ออกแบบมาสำหรับคอม อ่านเหตุผลได้ที่คำอธิบายปุ่มไก่ตัน",
                font = Enum.Font.Gotham, size = 11, color = AFK_COLOR.orange,
                align = Enum.TextXAlignment.Center,
            })
        end

        local columns = Instance.new("Frame")
        columns.Name = "Columns"
        columns.BackgroundTransparency = 1
        columns.Position = UDim2.fromOffset(0, topOffset)
        columns.Size = UDim2.new(1, 0, 1, -topOffset)
        columns.Parent = body

        -- one panel builder for all three, so the chrome can never drift apart
        local function panel(order, widthScale, xScale, xOffset, title)
            local p = afkCard(columns, {
                size = UDim2.new(widthScale, -10, 1, 0),
                pos  = UDim2.new(xScale, xOffset, 0, 0),
                radius = 16,
            })
            p.LayoutOrder = order

            local head = afkText(p, {
                text = title, font = Enum.Font.GothamMedium, size = 12, color = AFK_COLOR.label2,
                sizeUDim = UDim2.new(1, -28, 0, 16), pos = UDim2.fromOffset(14, 12),
            })

            local line = Instance.new("Frame")
            line.BackgroundColor3 = AFK_COLOR.separator
            line.BackgroundTransparency = 0.4
            line.BorderSizePixel = 0
            line.Position = UDim2.fromOffset(14, 34)
            line.Size = UDim2.new(1, -28, 0, 1)
            line.Parent = p

            local scroll = Instance.new("ScrollingFrame")
            scroll.BackgroundTransparency = 1
            scroll.BorderSizePixel = 0
            scroll.Position = UDim2.fromOffset(12, 42)
            scroll.Size = UDim2.new(1, -24, 1, -54)
            scroll.ScrollBarThickness = 2
            scroll.ScrollBarImageColor3 = AFK_COLOR.separator
            scroll.ScrollBarImageTransparency = 0.35
            scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
            scroll.CanvasSize = UDim2.new()
            scroll.Parent = p

            return p, head, scroll
        end

        -- widths and offsets are picked so the three right edges land exactly on
        -- the body's right edge with an even 15px gutter between panels
        local _, _, logScroll        = panel(1, 0.30, 0,    0,  "ประวัติการทำงาน")
        local _, petHead, petScroll  = panel(2, 0.42, 0.30, 5,  "สัตว์เลี้ยง")
        local eggPanel, eggHead, eggScroll = panel(3, 0.28, 0.72, 10, "ไข่")

        ------------------------------------------------------------
        -- Colour legend: the bands mean nothing unless they are explained
        ------------------------------------------------------------
        local LEGEND_H = 104
        eggScroll.Size = UDim2.new(1, -24, 1, -54 - LEGEND_H)

        local legend = afkCard(eggPanel, {
            color = AFK_COLOR.bg, radius = 12,
            size = UDim2.new(1, -24, 0, LEGEND_H - 10),
            pos  = UDim2.new(0, 12, 1, -LEGEND_H),
        })

        afkText(legend, {
            text = "กรอบไข่บอกอะไร", font = Enum.Font.GothamMedium, size = 11,
            color = AFK_COLOR.label2,
            sizeUDim = UDim2.new(1, -20, 0, 14), pos = UDim2.fromOffset(10, 8),
        })

        local function legendRow(y, color, head, detail)
            local dot = Instance.new("Frame")
            dot.BackgroundColor3 = color
            dot.BorderSizePixel = 0
            dot.Position = UDim2.fromOffset(10, y + 5)
            dot.Size = UDim2.fromOffset(8, 8)
            dot.Parent = legend
            afkCorner(dot, 4)

            afkText(legend, {
                text = head, font = Enum.Font.GothamMedium, size = 10, color = color,
                sizeUDim = UDim2.new(0, 62, 0, 12), pos = UDim2.fromOffset(24, y),
            })
            afkText(legend, {
                text = detail, font = Enum.Font.Gotham, size = 10, color = AFK_COLOR.label3,
                sizeUDim = UDim2.new(1, -96, 0, 12), pos = UDim2.fromOffset(88, y), truncate = true,
            })
        end

        legendRow(26, AFK_COLOR.green,  "เขียว",  "เหลือ 10 วิ หรือน้อยกว่า — กำลังจะฟัก")
        legendRow(44, AFK_COLOR.yellow, "เหลือง", "เหลือ 11 วิ ถึง 1 นาที — ใกล้แล้ว")
        legendRow(62, AFK_COLOR.red,    "แดง",    "เหลือเกิน 1 นาที — ยังอีกนาน")
        legendRow(80, AFK_COLOR.label3, "เทา",    "ยังไม่ได้วางลงฐาน จึงยังไม่เริ่มนับ")

        AFK.logList, AFK.gridList, AFK.eggList = logScroll, petScroll, eggScroll
        AFK.petTitle, AFK.eggTitle = petHead, eggHead

        local logLayout = Instance.new("UIListLayout")
        logLayout.SortOrder = Enum.SortOrder.LayoutOrder
        logLayout.Padding = UDim.new(0, 4)
        logLayout.Parent = logScroll

        -- the entire log pool is allocated here, once, and never grows again
        for i = 1, AFK_MAX_LOGS do
            local row = Instance.new("TextLabel")
            row.Name = "Log" .. i
            row.BackgroundTransparency = 1
            row.Size = UDim2.new(1, -6, 0, 15)
            row.Font = Enum.Font.Code
            row.TextSize = 11
            row.TextColor3 = AFK_COLOR.label2
            row.TextXAlignment = Enum.TextXAlignment.Left
            row.TextTruncate = Enum.TextTruncate.AtEnd
            row.Visible = false
            row.Text = ""
            row.Parent = logScroll
            AFK.pool[i] = row
        end

        local grid = Instance.new("UIGridLayout")
        grid.CellSize = UDim2.fromOffset(112, 148)
        grid.CellPadding = UDim2.fromOffset(10, 10)
        grid.SortOrder = Enum.SortOrder.LayoutOrder
        grid.Parent = petScroll
        AFK.gridLayout = grid

        -- the whole pet pool, allocated here and never grown again
        for i = 1, AFK_MAX_PETS do
            local card = Instance.new("Frame")
            card.Name = "Pet" .. i
            card.BackgroundColor3 = AFK_COLOR.raised
            card.BorderSizePixel = 0
            card.LayoutOrder = i
            card.Visible = false
            afkCorner(card, 14)
            -- the UIStroke lives in a parallel table: an Instance rejects any
            -- field that is not a real property, so caching it on the frame
            -- itself throws and takes the whole paint down with it
            AFK.strokes[i] = afkStroke(card, AFK_COLOR.separator, 1, 0.2)

            local icon = Instance.new("ImageLabel")
            icon.Name = "Icon"
            icon.BackgroundTransparency = 1
            icon.AnchorPoint = Vector2.new(0.5, 0)
            icon.Position = UDim2.fromScale(0.5, 0)
            icon.Size = UDim2.new(1, -16, 0.44, 0)
            icon.ScaleType = Enum.ScaleType.Fit
            icon.Parent = card

            afkText(card, {
                text = "", font = Enum.Font.GothamMedium, size = 13,
                align = Enum.TextXAlignment.Center, truncate = true,
                sizeUDim = UDim2.new(1, -12, 0, 17), pos = UDim2.new(0, 6, 0.47, 0),
            }).Name = "PetName"

            afkText(card, {
                text = "", font = Enum.Font.Gotham, size = 11,
                align = Enum.TextXAlignment.Center, color = AFK_COLOR.label2,
                sizeUDim = UDim2.new(1, -12, 0, 15), pos = UDim2.new(0, 6, 0.64, 0),
            }).Name = "Tier"

            afkText(card, {
                text = "", font = Enum.Font.GothamMedium, size = 11,
                align = Enum.TextXAlignment.Center, color = AFK_COLOR.green,
                sizeUDim = UDim2.new(1, -12, 0, 15), pos = UDim2.new(0, 6, 0.80, 0),
            }).Name = "Income"

            card.Parent = petScroll
            AFK.cards[i] = card
        end

        local eggLayout = Instance.new("UIListLayout")
        eggLayout.SortOrder = Enum.SortOrder.LayoutOrder
        eggLayout.Padding = UDim.new(0, 6)
        eggLayout.Parent = eggScroll

        -- and the whole egg pool, same deal
        for i = 1, AFK_MAX_EGGROWS do
            local row = Instance.new("Frame")
            row.Name = "Egg" .. i
            row.BackgroundColor3 = AFK_COLOR.raised
            row.BorderSizePixel = 0
            row.Size = UDim2.new(1, 0, 0, 52)
            row.LayoutOrder = i
            row.Visible = false
            afkCorner(row, 12)
            -- this stroke is the band colour the legend explains
            AFK.eggStrokes[i] = afkStroke(row, AFK_COLOR.separator, 1.5, 0)

            local icon = Instance.new("ImageLabel")
            icon.Name = "Icon"
            icon.BackgroundTransparency = 1
            icon.Position = UDim2.fromOffset(8, 6)
            icon.Size = UDim2.fromOffset(40, 40)
            icon.ScaleType = Enum.ScaleType.Fit
            icon.Parent = row

            afkText(row, {
                text = "", font = Enum.Font.GothamMedium, size = 12, truncate = true,
                sizeUDim = UDim2.new(1, -148, 0, 15), pos = UDim2.fromOffset(56, 9),
            }).Name = "EggName"

            afkText(row, {
                text = "", font = Enum.Font.Gotham, size = 10, color = AFK_COLOR.label3,
                sizeUDim = UDim2.new(1, -148, 0, 13), pos = UDim2.fromOffset(56, 27),
            }).Name = "Tier"

            afkText(row, {
                text = "", font = Enum.Font.GothamBold, size = 14,
                align = Enum.TextXAlignment.Right, color = AFK_COLOR.label2,
                sizeUDim = UDim2.new(0, 84, 1, 0), pos = UDim2.new(1, -96, 0, 0),
            }).Name = "Status"

            row.Parent = eggScroll
            AFK.eggRows[i] = row
        end

        AFK.eggEmpty = afkText(eggScroll, {
            text = "ยังไม่มีไข่ในกระเป๋าหรือบนฐาน", font = Enum.Font.Gotham, size = 11,
            color = AFK_COLOR.label3, align = Enum.TextXAlignment.Center,
            sizeUDim = UDim2.new(1, 0, 0, 40),
        })
        AFK.eggEmpty.LayoutOrder = 9999
        AFK.eggEmpty.Visible = false

        closeBtn.MouseButton1Click:Connect(function() afkClose(true) end)

        AFK.gui = gui
        Runtime.afkGui = gui              -- so script cleanup takes the panel with it
        ENV[HUB_KEY .. "AfkGui"] = gui    -- and so a re-execute can find the old one
    end

    ----------------------------------------------------------------
    -- Open / close
    ----------------------------------------------------------------
    -- Both of these are callable from ANY thread: they only raise flags, and the
    -- pump applies them from the thread that owns the GUI.
    local function afkOpen()
        if AFK.on then return end
        AFK.on = true
        Runtime.afkOn = true   -- serverHop reads this and writes the resume flag
        AFK.startAt = os.clock()
        AFK.lastSec = -1
        AFK.gridAt, AFK.eggAt = 0, 0
        AFK.wantShow, AFK.wantHide = true, false
        AFK.wantSeed = true
        AFK.gridDirty, AFK.eggDirty, AFK.logDirty = true, true, false
        logLine("afk", "เปิดโหมด AFK")
    end

    afkClose = function(fromButton)
        if not AFK.on then return end
        AFK.on = false
        Runtime.afkOn = false
        clearAfkResume()   -- pressing the button ends it for good, hops included

        AFK.wantHide, AFK.wantShow = true, false

        if fromButton and AFK.toggleRef then
            -- WindUI exposes different setters across versions; try them quietly
            for _, m in ipairs({ "SetValue", "Set", "UpdateValue" }) do
                if pcall(function() AFK.toggleRef[m](AFK.toggleRef, false) end) then break end
            end
        end
        Notify("โหมด AFK", "ปิดโหมด AFK แล้ว — คืนค่ากราฟิกเรียบร้อย", 4)
    end

    ----------------------------------------------------------------
    -- Event feed
    ----------------------------------------------------------------
    -- logLine is rebound exactly once, so every existing caller in the script
    -- feeds the panel without a single polling loop being added anywhere.
    if not AFK.hooked then
        AFK.hooked = true
        local baseLogLine = logLine
        -- Only flags are set here. logLine is called from game callbacks and
        -- remote handlers, and those threads run at normal game identity, which
        -- cannot touch a GUI living in the executor's hidden container — every
        -- rebuild attempted from one of them died with "lacking capability
        -- Plugin". The pump owns every rebuild instead.
        logLine = function(kind, text)
            baseLogLine(kind, text)
            if AFK.on then
                AFK.logDirty = true
                if kind == "hatch" or kind == "sell" or kind == "fuse" then AFK.gridDirty = true end
                if kind == "hatch" or kind == "place" or kind == "steal" then AFK.eggDirty = true end
            end
        end
    end

    -- handles for the exported API, so the panel can be driven without the tab
    Runtime.afkOpen  = afkOpen
    Runtime.afkClose = afkClose
    Runtime.afkDiag  = function()
        return {
            gui = typeof(AFK.gui), eggList = typeof(AFK.eggList),
            eggRows = #AFK.eggRows, eggShown = #AFK.eggData,
            petCards = #AFK.cards, logRows = #AFK.pool, on = AFK.on,
        }
    end

    -- Built and connected HERE, on the script's own UI thread, not on first
    -- open. Every Instance in the panel is therefore created by one thread, and
    -- the pump that writes to them runs on a connection made by that same
    -- thread — the fix for the empty egg column. While the panel is off the
    -- pump costs one boolean test per frame.
    afkBuild()
    AFK.gui.Enabled = false
    AFK.conns.pump = RunService.Heartbeat:Connect(function() pcall(afkPump) end)
    Runtime.conns.afkPump = AFK.conns.pump

    local function afkSetToggle(state)
        if not AFK.toggleRef then return end
        for _, m in ipairs({ "SetValue", "Set", "UpdateValue" }) do
            if pcall(function() AFK.toggleRef[m](AFK.toggleRef, state) end) then return end
        end
    end

    AFK.toggleRef = TabSys:Toggle({
        Title = "ไก่ตัน",
        Desc  = "หน้าต่าง AFK เต็มจอ — ล็อกงาน / สัตว์เลี้ยง / ไข่นับถอยหลัง "
            .. "ปิดเรนเดอร์ 3D ลดคุณภาพภาพ จำกัด FPS เหลือ 10 แล้วคืนค่าทั้งหมดตอนปิด "
            .. "ติดไปกับการย้ายเซิร์ฟเอง แต่ rejoin เองจะไม่เปิด "
            .. "(ไม่เหมาะกับมือถือ: ตัวรันมือถือส่วนใหญ่ไม่มี setfpscap และ Set3dRenderingEnabled "
            .. "โหมดนี้เลยประหยัดเครื่องไม่ได้จริง เหลือแค่บังหน้าจอ; "
            .. "iOS/Android ฆ่าแอปที่พับหน้าจอทิ้งไว้ AFK ยาวจึงหลุดเอง; "
            .. "จอ 6 นิ้วอ่าน 3 คอลัมน์ไม่ออก; และรันยาวทำให้ร้อนจนเครื่องหรี่ความเร็วลงเอง)",
        Value = false,
        Callback = function(v)
            if isOn(v) then afkOpen() else afkClose(false) end
        end,
    })

    -- Resume after a hop, never after a manual rejoin: the flag only exists
    -- because serverHop wrote it on the way out, and it is eaten on read.
    if consumeAfkResume() then
        task.spawn(function()
            -- wait for the world to actually be there before drawing over it,
            -- so auto-steal picks up exactly as it did in the old server
            local deadline = os.clock() + 45
            repeat task.wait(0.5) until (hrp() and plotData()) or os.clock() > deadline or not alive()
            if not alive() then return end
            task.wait(1.5)
            afkOpen()
            afkSetToggle(true)
        end)
    end

    TabSys:Toggle({
        Title = "โหมดอมตะ (God Mode)",
        Desc  = "เปิดไว้ตลอด — ตายทีเดียวไข่หลุดแล้ววนตายซ้ำ",
        Value = Cfg.GodMode,
        Callback = function(v) Cfg.GodMode = isOn(v) saveConfig() end,
    })

    TabSys:Dropdown({
        Title = "โหมดการเดินทาง",
        Desc  = "Instant = วาปตรง (~0.7 วิ/ใบ) | Warp = วาปทีละสเต็ป ไม่แตะระบบของเกม",
        Values = { "Instant", "Warp" },
        Value = Cfg.MoveMode,
        Callback = function(v) Cfg.MoveMode = v saveConfig() end,
    })

    TabSys:Section({ Title = "แก้แลค" })

    TabSys:Toggle({
        Title = "เปิดระบบแก้แลค",
        Desc  = "ลบเอฟเฟกต์ ลบพื้นผิว ปิดเงา ลดคุณภาพภาพ — กวาดซ้ำทุก 30 วินาที",
        Value = Cfg.AntiLag,
        Callback = function(v)
            Cfg.AntiLag = isOn(v)
            saveConfig()
            if isOn(v) then antiLagFullPass() end
        end,
    })

    TabSys:Dropdown({
        Title = "ระดับการแก้แลค",
        Desc  = "เบา = ลบเฉพาะเอฟเฟกต์ | หนัก = ลบพื้นผิว ปิดเงา ปรับวัสดุทั้งแมพ",
        Values = { "หนัก (แนะนำ)", "เบา" },
        Value = Cfg.LowFXOnly and "เบา" or "หนัก (แนะนำ)",
        Callback = function(v) Cfg.LowFXOnly = (v == "เบา") saveConfig() end,
    })

    TabSys:Toggle({
        Title = "ซ่อนผู้เล่นคนอื่น",
        Desc  = "ตัวละครคนอื่นคือภาระเรนเดอร์ที่ใหญ่ที่สุดในเซิร์ฟเวอร์เต็ม",
        Value = Cfg.HideOtherPlayers,
        Callback = function(v) Cfg.HideOtherPlayers = isOn(v) saveConfig() end,
    })

    TabSys:Section({ Title = "อื่นๆ" })

    TabSys:Toggle({
        Title = "กันหลุด AFK",
        Value = Cfg.AntiAFK,
        Callback = function(v) Cfg.AntiAFK = isOn(v) saveConfig() end,
    })

    TabSys:Toggle({
        Title = "ย้ายเซิร์ฟเมื่อโซนนี้ไม่มีไข่ที่ต้องการ",
        Desc  = "เช็คซ้ำหลายรอบว่าไม่มีจริง แล้วย้ายทันที ไม่ต้องรอ — เข้าเซิร์ฟใหม่แล้วเช็คใหม่",
        Value = Cfg.AutoHop,
        Callback = function(v)
            Cfg.AutoHop = isOn(v)
            Runtime.hopStopped = false
            Runtime.hopChain   = 0
            saveConfig()
        end,
    })

    TabSys:Slider({
        Title = "เช็คซ้ำกี่ครั้งก่อนย้าย",
        Value = { Min = 2, Max = 6, Default = Cfg.HopChecks },
        Callback = function(v) Cfg.HopChecks = v saveConfigSoon() end,
    })

    TabSys:Slider({
        Title = "ย้ายติดกันได้กี่ครั้ง",
        Value = { Min = 1, Max = 30, Default = Cfg.HopMaxTries },
        Callback = function(v) Cfg.HopMaxTries = v saveConfigSoon() end,
    })

    TabSys:Input({
        Title = "Discord Webhook",
        Value = Cfg.WebhookURL,
        Callback = function(v) Cfg.WebhookURL = tostring(v) saveConfig() end,
    })

    TabSys:Toggle({
        Title = "แจ้งเตือนทุกครั้งที่ขโมยได้",
        Value = Cfg.WebhookSteal,
        Callback = function(v) Cfg.WebhookSteal = isOn(v) saveConfig() end,
    })

    ----------------------------------------------------------------
    -- Window Toggle & Floating Icon
    ----------------------------------------------------------------
    local UserInput = game:GetService("UserInputService")

    local function toggleWindow()
        pcall(function()
            if UIWindow and UIWindow.Closed then UIWindow:Open() elseif UIWindow then UIWindow:Close() end
        end)
    end
    Runtime.toggleUI = toggleWindow
    Runtime.window   = UIWindow

    do
        local oldGui = game:GetService("CoreGui"):FindFirstChild(HUB_TAG .. "Float")
            or (LocalPlayer and LocalPlayer:WaitForChild("PlayerGui"):FindFirstChild(HUB_TAG .. "Float"))
        if oldGui then oldGui:Destroy() end

        local gui = Instance.new("ScreenGui")
        gui.Name = HUB_TAG .. "Float"
        gui.ResetOnSpawn = false
        gui.IgnoreGuiInset = true
        gui.DisplayOrder = 999999
        gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
        pcall(function() gui.Parent = (typeof(gethui) == "function") and gethui() or (LocalPlayer and LocalPlayer:WaitForChild("PlayerGui")) end)

        local btn = Instance.new("TextButton")
        btn.Name = "Open"
        btn.Size = UDim2.fromOffset(44, 44)
        btn.Position = UDim2.fromOffset(tonumber(Cfg.FloatX) or 18, tonumber(Cfg.FloatY) or 260)
        btn.BackgroundColor3 = Color3.fromRGB(24, 24, 30)
        btn.BackgroundTransparency = 0.15
        btn.BorderSizePixel = 0
        btn.Text = "🥚"
        btn.TextSize = 22
        btn.TextColor3 = Color3.fromRGB(235, 235, 245)
        btn.AutoButtonColor = true
        btn.Visible = Cfg.ShowFloatIcon
        btn.Parent = gui

        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 10)
        corner.Parent = btn

        local stroke = Instance.new("UIStroke")
        stroke.Color = Color3.fromRGB(139, 92, 246)
        stroke.Thickness = 1.5
        stroke.Transparency = 0.25
        stroke.Parent = btn

        local dragging, dragStart, startPos, moved = false, nil, nil, false

        local function beginDrag(input)
            dragging, moved = true, false
            dragStart, startPos = input.Position, btn.Position
        end

        local function endDrag()
            if not dragging then return end
            dragging = false
            if moved then
                Cfg.FloatX = math.floor(btn.Position.X.Offset)
                Cfg.FloatY = math.floor(btn.Position.Y.Offset)
                saveConfig()
            else
                toggleWindow()
            end
        end

        btn.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1
                or input.UserInputType == Enum.UserInputType.Touch then
                beginDrag(input)
            end
        end)

        btn.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1
                or input.UserInputType == Enum.UserInputType.Touch then
                endDrag()
            end
        end)

        Runtime.conns.dragMove = UserInput.InputChanged:Connect(function(input)
            if not dragging then return end
            if input.UserInputType == Enum.UserInputType.MouseMovement
                or input.UserInputType == Enum.UserInputType.Touch then
                local d = input.Position - dragStart
                if math.abs(d.X) > 3 or math.abs(d.Y) > 3 then moved = true end
                btn.Position = UDim2.new(
                    startPos.X.Scale, startPos.X.Offset + d.X,
                    startPos.Y.Scale, startPos.Y.Offset + d.Y)
            end
        end)

        Runtime.conns.dragEnd = UserInput.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1
                or input.UserInputType == Enum.UserInputType.Touch then
                endDrag()
            end
        end)

        Runtime.floatIcon = btn
        Runtime.floatGui  = gui
    end

    local rebinding = false

    Runtime.conns.hotkey = UserInput.InputBegan:Connect(function(input, processed)
        if input.UserInputType ~= Enum.UserInputType.Keyboard then return end

        if rebinding then
            rebinding = false
            Cfg.ToggleKey = input.KeyCode.Name
            saveConfig()
            if Runtime.refreshKeyBox then Runtime.refreshKeyBox() end
            Notify("ปุ่มลัด", ("ตั้งเป็น %s แล้ว"):format(input.KeyCode.Name), 4)
            return
        end

        if processed then return end
        if input.KeyCode == keyFromName(Cfg.ToggleKey) then toggleWindow() end
    end)

    TabSys:Section({ Title = "ปุ่มลัด & การเข้าถึง" })

    local KeyBox = TabSys:Paragraph({
        Title = "ปุ่มเปิด/ปิดหน้าต่าง",
        Desc  = ("ตอนนี้: %s"):format(Cfg.ToggleKey),
    })

    Runtime.refreshKeyBox = function()
        pcall(function() KeyBox:SetDesc(("ตอนนี้: %s"):format(Cfg.ToggleKey)) end)
    end

    TabSys:Toggle({
        Title = "ตั้งปุ่มใหม่",
        Desc  = "เปิดสวิตช์นี้แล้วกดปุ่มที่ต้องการ 1 ครั้ง",
        Value = false,
        Callback = function(v)
            rebinding = isOn(v)
            if isOn(v) then
                Notify("ปุ่มลัด", "กดปุ่มที่ต้องการได้เลย", 4)
            end
            task.delay(0.1, function()
                pcall(function() KeyBox:SetDesc(("ตอนนี้: %s"):format(Cfg.ToggleKey)) end)
            end)
        end,
    })

    TabSys:Toggle({
        Title = "แสดงไอคอนลอย",
        Desc  = "สี่เหลี่ยมเล็กๆ ลากย้ายได้ กดเพื่อเปิดหน้าต่างตอนพับอยู่",
        Value = Cfg.ShowFloatIcon,
        Callback = function(v)
            Cfg.ShowFloatIcon = isOn(v)
            saveConfig()
            if Runtime.floatIcon then Runtime.floatIcon.Visible = isOn(v) end
        end,
    })

    ----------------------------------------------------------------
    -- Settings File Section
    ----------------------------------------------------------------
    TabSys:Section({ Title = "ไฟล์ตั้งค่า" })

    local CfgBox = TabSys:Paragraph({
        Title = "การบันทึกค่า",
        Desc  = tostring(Runtime.cfgStatus),
    })

    Runtime.refreshCfgBox = function()
        pcall(function()
            CfgBox:SetDesc(("%s\nไฟล์: %s"):format(tostring(Runtime.cfgStatus), CFG_FILE))
        end)
    end
    Runtime.refreshCfgBox()

    TabSys:Button({
        Title = "บันทึกค่าตอนนี้",
        Desc  = "เขียนทับไฟล์ทันที แล้วอ่านกลับมาตรวจว่าเขียนติดจริง",
        Callback = function()
            local ok = saveConfig()
            Runtime.refreshCfgBox()
            Notify("ตั้งค่า", ok and "บันทึกเรียบร้อย" or ("บันทึกไม่สำเร็จ: " .. tostring(Runtime.cfgStatus)), 4)
        end,
    })

    TabSys:Button({
        Title = "โหลดค่าจากไฟล์",
        Desc  = "ดึงค่าที่บันทึกไว้กลับมาใช้ (หน้าต่างต้องเปิดใหม่ถึงจะเห็นสวิตช์ขยับ)",
        Callback = function()
            local ok = loadConfig()
            Runtime.refreshCfgBox()
            Notify("ตั้งค่า", ok and tostring(Runtime.cfgStatus) or "ไม่มีไฟล์ให้โหลด", 4)
        end,
    })

    local LogBox = TabSys:Paragraph({ Title = "บันทึกล่าสุด", Desc = "-" })

    for k, v in pairs(cfgAtBuild) do Cfg[k] = v end
    cfgAtBuild = nil
    uiBuilding = false

    ----------------------------------------------------------------
    -- UI Refresh Job
    ----------------------------------------------------------------
    job("ui", 5, function()
        local data = saveData()
        local mins = math.max((os.clock() - Runtime.stats.startedAt) / 60, 0.01)
        local money = tonumber(data.Money) or 0
        local gain  = money - (Runtime.stats.startMoney or money)

        pcall(function()
            StatusBox:SetDesc(("โหมด: %s%s | ไข่ %d ใบ (%.1f/นาที) | ฟัก %d\nเงิน %s (+%s ตั้งแต่เริ่ม) | กระเป๋าไข่ %d/%d\n%s")
                :format(
                    Cfg.MoveMode,
                    Runtime.instantReady and "" or " (วาปตรงยังไม่พร้อม ใช้ Warp แทน)",
                    Runtime.stats.eggs, Runtime.stats.eggs / mins, Runtime.stats.hatched,
                    shortNum(money), shortNum(gain),
                    bagCount(), EGG_BAG_CAP,
                    Runtime.lastError and ("ล่าสุด: " .. tostring(Runtime.lastError)) or "ทำงานปกติ"))
        end)

        pcall(function()
            local pv = Runtime.sellPreview
            if not pv then
                SellPreview:SetDesc("กำลังอ่านกระเป๋า...")
            elseif pv.count == 0 then
                SellPreview:SetDesc("ตอนนี้ไม่มีสัตว์ตัวไหนเข้าตัวกรอง — เปิดสวิตช์ก็ไม่มีอะไรหาย")
            else
                SellPreview:SetDesc(("เข้าตัวกรอง %d ตัว รวมราว %s • คิวเหลือ %d\nจังหวะขายตอนนี้ %.1f ครั้ง/วิ (เฟรมแย่สุด %d ms)\n%s")
                    :format(pv.count, shortNum(pv.total), #sellQueue,
                        1 / math.max(Runtime.sellPace or (Cfg.SellPace or 0.05), 0.001),
                        Runtime.sellWorstMs or 0,
                        pv.head or ""))
            end
        end)

        pcall(function()
            local lvl = tonumber(data.BaseUpgradeLevel) or 0
            local tm  = tonumber(data.TreadmillUpgradeLevel) or 0
            local nextBase = BasesDir and BasesDir.BASES and BasesDir.BASES[lvl + 1]
            local nextTm
            if TreadDir and TreadDir.GetByUpgradeLevel then
                local ok, t = pcall(TreadDir.GetByUpgradeLevel, tm + 1)
                if ok then nextTm = t end
            end
            ProgBox:SetDesc(("ฐาน เลเวล %d → ขั้นถัดไป %s\nลู่วิ่ง เลเวล %d → ขั้นถัดไป %s\nสัตว์ในกระเป๋า %d ตัว")
                :format(
                    lvl, nextBase and shortNum(nextBase.Cost) or "สุดแล้ว",
                    tm, (type(nextTm) == "table") and ((nextTm.DisplayName or nextTm._id) .. " " .. shortNum(nextTm.Price)) or "สุดแล้ว",
                    Runtime.petCount or 0))
        end)

        pcall(function()
            LogBox:SetDesc(#Runtime.log == 0 and "-" or table.concat(Runtime.log, "\n", 1, math.min(8, #Runtime.log)))
        end)

        if Runtime.refreshCfgBox then Runtime.refreshCfgBox() end
    end)

    if Notify then Notify(HUB_NAME, "โหลดสำเร็จ • Steal An Egg", 4) end
end)

----------------------------------------------------------------
-- Global API Exports & Cleanup
----------------------------------------------------------------
ENV[K_API] = {
    Cfg = Cfg, Runtime = Runtime,
    steal = stealOnce, anchor = toAnchor,
    hatch = hatchReady, place = placeEggs,
    sellPets = sellPets, sellPetPlan = sellPetPlan, sellEggs = sellEggs,
    equipBest = equipBest, fuse = fuseStep,
    baseUp = baseUpgradeStep, treadUp = treadmillUpgradeStep,
    antiLag = antiLagFullPass, antiLagTick = antiLagSweep, hop = serverHop,
    bag = bagCount, eggRoster = eggRoster, eggClock = clockText,
    saveCfg = saveConfig, loadCfg = loadConfig,
    afkOpen  = function() if Runtime.afkOpen  then Runtime.afkOpen()  end end,
    afkClose = function() if Runtime.afkClose then Runtime.afkClose(false) end end,
    income   = function() return Runtime.income end,
}
ENV.NyxAPI = ENV[K_API]

ENV[K_CLEANUP] = function()
    for _, c in pairs(Runtime.conns) do pcall(function() c:Disconnect() end) end
    Runtime.conns = {}
    for _, c in ipairs(Runtime.pinConns or {}) do pcall(function() c:Disconnect() end) end
    Runtime.pinConns = nil
    Guardless.reattach()
    pcall(function() if UIWindow then UIWindow:Destroy() end end)
    UIWindow = nil
    pcall(function() if Runtime.floatGui then Runtime.floatGui:Destroy() end end)
    Runtime.floatGui, Runtime.floatIcon = nil, nil
    -- the AFK panel is a plain ScreenGui and outlives the WindUI window, so it
    -- has to be named here or a re-execute leaves a dead panel on screen
    pcall(function() if Runtime.afkGui then Runtime.afkGui:Destroy() end end)
    Runtime.afkGui, Runtime.afkOpen, Runtime.afkClose, Runtime.afkOn = nil, nil, nil, false
end
ENV.NyxCleanup = ENV[K_CLEANUP]

-- Character spawn handler
if LocalPlayer then
    Runtime.conns.charAdded = LocalPlayer.CharacterAdded:Connect(function(c)
        c:WaitForChild("HumanoidRootPart", 10)
        task.wait(0.5)
        hardenCharacter(c)
        Runtime.carryUid = nil
    end)

    Runtime.conns.afk = LocalPlayer.Idled:Connect(function()
        if not Cfg.AntiAFK then return end
        pcall(function()
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    end)

    Runtime.conns.promptInstant = ProximityPromptService.PromptButtonHoldBegan:Connect(function(prompt, player)
        if player == LocalPlayer and (tostring(prompt) == "CarryAreaEgg" or (prompt.Name and prompt.Name:find("Egg"))) then
            prompt.HoldDuration = 0
        end
    end)
end

Runtime.instantReady = true
hardenCharacter()
Runtime.stats.startMoney = moneyNow()

if Cfg.AntiLag then
    task.delay(5, function() if alive() and Cfg.AntiLag then antiLagFullPass() end end)
end
